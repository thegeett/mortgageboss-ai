# LP-119 — Applicability filter engine (thin slice, proven on AS-5 gift-letter)

**Type:** Story · **Epic:** Phase 3.5 / Epic B · **Depends:** LP-118 (rule table), LP-118.6 (fact snapshot), ADR-239
**Status:** complete · **Date:** 2026-07-08

## What this ticket is

The first data-driven engine piece that RUNS: the **applicability filter**. For a loan file, it
classifies each rule — by reading the rule's applicability (scope / triggers / required_inputs) from
`verification_rules` (LP-118) against the fact snapshot (LP-118.6) — into three states:

- **DOESN'T-APPLY** — scope or a trigger is FALSE → rule irrelevant → silently excluded.
- **COULDN'T-CHECK** — rule applies but data it needs is MISSING/UNKNOWN → surfaced, **never a
  silent pass**.
- **READY-TO-RUN** — applies AND required data present → eligible for the evaluator (LP-120).

A **vertical thin slice**: the engine is GENERAL (reads applicability as DATA, works for any rule),
proven on ONE rule — **AS-5** (gift-letter present) — plus synthetic cases. **Scope held:** no
evaluators, no findings, no runner wiring, no other rules; read-only classification; live path
unchanged.

**THE FALSE-GREEN GUARD (the core):** UNKNOWN (the data needed to DECIDE a condition is missing)
resolves to COULDN'T-CHECK — **never** collapses to doesn't-apply or ready-to-run. A file must never
look checked-and-clean when the data to decide was absent.

## Acceptance criteria — and how each was met

- [x] **A general, data-driven filter classifies a rule into the three states by reading its
  applicability against the snapshot.** → `app/verification/applicability/` — `classify()` /
  `classify_from_json()` (single rule) + `classify_rules()` (a set of `verification_rules` rows).
  No rule-specific logic; the engine reads the JSON shape.
- [x] **Three-valued logic correct** (scope/trigger FALSE → doesn't-apply; UNKNOWN → couldn't-check;
  required input absent → couldn't-check named; else ready-to-run). → `engine.classify` with
  Ternary combinators + FALSE-precedence. Tested across all branches.
- [x] **UNKNOWN NEVER collapses to doesn't-apply or ready-to-run (explicit false-green test).** →
  `test_unknown_trigger_never_collapses_to_apply_or_ready` (a trigger on the always-absent
  `documented.credit_tradelines` → couldn't-check, asserted `is not` doesn't-apply / ready).
- [x] **AS-5 classifies correctly across the three cases.** → gift present → READY-TO-RUN; assets
  present but no gift → DOESN'T-APPLY; no asset data (empty) → COULDN'T-CHECK. Tested (pure) + via
  a real assembled snapshot + a `verification_rules` row.
- [x] **Reads applicability as DATA — no rule-specific logic.** → the engine has no "if gift rule";
  AS-5's behaviour comes entirely from its authored applicability JSON (seeded + in the table via
  the LP-119 migration).
- [x] **couldn't-check records the reason (what's missing).** → `Classification.reasons` +
  `missing_inputs` (e.g. `document:credit_report`, `derived_field:computed.ltv`, `scope 'program'
  unknown`).
- [x] **SCOPE HELD; live path unchanged; read-only.** → new package + one data migration (authoring
  AS-5's applicability) + tests; no evaluator, no finding, no runner change. No test runs a rule.
- [x] **`docs/tickets/LP-119.md` written** (this file).

## What was built

- **`app/verification/applicability/schema.py`** — the parsed applicability shape (`Applicability`
  = `scope` / `triggers` / `required_inputs`), the condition types (`EntityExists`,
  `FieldCondition`), required-input types (`DataField`, `DocumentPresent`, `DerivedField`), and the
  `Ternary` / `ApplicabilityState` / `Classification` enums+models.
- **`engine.py`** — path resolution over the snapshot, the Fact→Ternary mapping, three-valued
  condition/trigger/scope evaluation, the required-input check, and `classify()` with FALSE
  precedence.
- **`__init__.py`** — `classify_rules(rules, snapshot) → ClassifiedRules` (the three groups; the
  LP-121 runner reads `ready_to_run`).
- **AS-5's authored applicability** — added to the seed generator (`generate_rule_seed.py`) +
  regenerated `rule_seed.json`, and a data migration (`a7c4e9f2b6d3`) setting the AS-5 row's
  applicability in `verification_rules` (reversible; verified up/down on Postgres).
- **Tests** (`tests/verification/test_applicability.py`, 13) — AS-5's three cases, scope
  (empty=all / mismatch / match / value-absent), required document + derived field absent, the
  explicit false-green guard, universal rule → ready, FALSE-precedence-over-UNKNOWN, and the
  table-reading integration.

## Assumptions and decisions

- **present/empty/absent → true/false/unknown mapping.** For a DECISION input (scope/trigger/
  field_condition), a Fact with a concrete value → its truth; **a Fact with no value — `absent` OR
  empty (`value is None`) — → UNKNOWN.** This is the conservative false-green reading ADR-239
  mandates ("unset enum → None → applicability unknown"): if we don't have a concrete value we
  cannot decide, so we must not guess. (The snapshot still distinguishes empty vs absent; the
  engine's *choice* is to treat both "no value" states as "can't decide".)
- **`entity_exists` is three-valued.** TRUE if any element matches; **UNKNOWN if the collection is
  EMPTY** (no data to confirm the entity is truly absent) or an element's field is unknown; FALSE
  only when there ARE elements and none matches. This is exactly what gives AS-5 its three cases:
  gift asset → ready; other assets, no gift → doesn't-apply; **no assets at all → couldn't-check**
  (we can't confirm there's no gift — the file may just have no asset data captured).
- **Empty scope constraint = applies to all** (a `{}` scope or an empty list for a dimension is no
  constraint) — tested to avoid the off-by-one.
- **FALSE precedence over UNKNOWN.** A definitively-false scope/trigger → doesn't-apply regardless
  of missing required inputs (the rule is genuinely irrelevant). Only when scope+triggers are TRUE
  do required inputs decide ready vs couldn't-check.
- **The check-target is NOT a required input.** AS-5's gift LETTER is what the rule checks (its
  absence is the finding, evaluated in LP-120) — so it is **not** in `required_inputs`. AS-5 with a
  gift but no letter is READY-TO-RUN, not couldn't-check. (Getting this wrong would turn a findable
  problem into a can't-check.)
- **AS-5's applicability lives in the table** (seed + migration), so the engine reads it as data;
  the test-suite `create_all` DB has no seed, so the integration test inserts the row itself to
  prove table-reading.
- **`property`-scoped dimensions** resolve through `property` (single); when the file has no
  property, the scope value is UNKNOWN → couldn't-check (honest).

## Follow-ups

- **LP-120 (evaluators):** run evaluators on the `ready_to_run` group; author more rules' evaluators
  + fill `confidence_mode`; the gift-letter check itself lives here.
- **LP-121 (runner):** assemble the snapshot (+ `assign_documents_to_borrowers`), call
  `classify_rules`, evaluate `ready_to_run`, and persist the three-state results.
- **LP-140 (trust surface):** surface `couldn't_check` (with `reasons`/`missing_inputs`) so a file
  is never shown clean when checks were skipped for missing data.
- **More rules = table rows, not engine changes:** author each rule's applicability
  (scope/triggers/required_inputs) into the seed; the engine is already general.
- **Note for LP-118.6 wiring:** `borrowers[].current_address` and `property.county` still read as
  ABSENT in the snapshot (builder not yet wired to the LP-118.7 columns) — scope/triggers on those
  will be couldn't-check until that wiring lands.
