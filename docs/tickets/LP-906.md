# LP-906 — reading known layouts with rules

- **Ticket:** LP-906 · Phase 4.5 Stage 1 · pure functions, no DB / network / AI
- **Date:** 2026-09-23
- **Branch:** `phase4.5-conditions`
- **Spec:** [`../phases/phase4.5-stage0-1-build-spec.md`](../phases/phase4.5-stage0-1-build-spec.md) §6, fixtures in §7
- **Depends on:** [`LP-904.md`](LP-904.md) (the enums the readers emit), [`LP-910.md`](LP-910.md) (the code map owner hints resolve through)
- **Migration:** none — nothing here touches a table.

## Built in three commits, reviewed between each

The largest ticket in the stage. Committed in sections on the product owner's decision (2026-09-23),
following LP-859's own rulebook — *"do not batch them into one commit; a single diff makes the
one-line fix impossible to see."* Each section is independently verifiable against fixtures that
already state their exact expected output.

| # | Section | Status |
|---|---|---|
| 1 | line model · normalisation · `detect_format` · the three UWM fixtures | done — `db55b566`, reviewed, fixes in `5f8dee7e` |
| 2 | the UWM reader, against §7.1–7.3 | **this commit** |
| 3 | the Champions reader · the generic fallback · the synthetic PDF | pending |

---

## Section 1 — the foundations every reader stands on

**`Line` / `Token`** (`readers/lines.py`). A reader never sees a PDF or a paste; it sees positioned
lines. That is not ceremony — the Champions centre rule and the UWM expiry table are both
**positional**, and a reader written against raw text cannot ask where a column begins.

**The two coordinate systems are different units, deliberately.** From a PDF, `y` is points from the
top and `x0` points from the left; from text, `y` is the line index and `x0` the character offset.
Nothing compares the two, and every rule that uses them is relative — "nearest column", "the line
above", "2 * number.y - bottom" — so both work unchanged. It is why the spec's Champions layout
sample parses as text at all.

**Normalisation is three substitutions and nothing else**: soft hyphen → `-`, non-breaking space →
space, trailing whitespace stripped. Spec §9.1 is what constrains it — the lender's words are stored
verbatim, so a normaliser that "tidied" anything would put a paraphrase into `verbatim_text` with no
way back to what was printed. **`rstrip` only**: leading spaces are load-bearing, because indentation
is what separates a bucket heading (≤ 3 spaces) from a continuation line (≥ 20).

## Decisions worth arguing with

**The readers import `ConditionSheetFormat`, not the spec's `SheetFormat`.** The spec's `ParsedSheet`
sketch names `SheetFormat` — a name appearing exactly once in the spec and nowhere in the code, while
LP-904 already defined `ConditionSheetFormat` with the four values the readers produce. Two names for
one fact is the drift this codebase warns about repeatedly (`_ROW_RULE`, `str_enum`, both
`activity_type` swaps). Recorded as a spec-vs-code difference; the survey's rule applies and the code
wins.

**`lines_from_pdf` goes through `page_ocr.words_for`, not `page.get_text("words")`.** That function
already answers "where are this page's words" for both a text layer and a scan, returns the same
`(x0, y0, x1, y1, word)` shape either way, and decides **per page** — six of the corpus's documents
are mixed. Calling the raw API here would be a second answer to one question, and would silently
return nothing for a scanned sheet.

**Words group into lines by vertical centre, not by pymupdf's block/line indices.** UWM's header is
two columns: `Contact Name:` on the left and `Senior UW:` on the right, at the same height and in
different blocks. Grouping by block would split one visual line in two and the header parser would
never pair a label with its value.

**Blank lines are kept, not filtered.** The Champions segmenter reasons about "the line above a row's
top"; a reader that never saw the blanks would compute a different line and mis-slice every row in
the segment.

## ⚠️ The fixtures are extracted mechanically, not transcribed

`tests/conditions/fixtures/*.txt` are copies of the fenced blocks in the spec, cut with `sed` from
lines 663–748, 769–833 and 848–920, and verified with `diff` against their source ranges. Three
files, 86 / 65 / 73 lines.

**They are identical to the spec except for trailing whitespace on one line**, and an earlier draft
of this ticket wrongly called them byte-for-byte. The repo's `trailing-whitespace` pre-commit hook
stripped the expiry header's padding after `Rate Lock Exp` in round 1, and **the strip stands** — no
hook exclusion was added. `normalise` rstrips every line, so no reader can observe the difference,
and only column *starts* carry meaning. The `.eml` corpus is excluded from that same hook because
DKIM signs those bytes; borrowing that reason here would claim a significance these files do not
have. Provenance stays checkable with trailing whitespace stripped from both sides.

This is LP-910's lesson applied *before* the mistake rather than after. That ticket's review had to
diff 56 hand-copied rows to find three slips; these are ~220 lines of column-aligned text where the
spec says *"keep the spacing exactly as below — the readers depend on columns"*. The UWM expiry table
is matched by **column position**, so one shifted space puts a date under the wrong header and
produces a plausible-looking wrong answer rather than an error. Copying mechanically removes the
failure mode instead of guarding it.

**`{SHY}` stays a placeholder in the committed file.** A real U+00AD is invisible in an editor and in
a diff, so a fixture containing them could be corrupted by any tool that strips them with nobody
seeing. The loader substitutes on read, which also makes the count assertable — and it is asserted:
6 / 3 / 6.

## ⚠️ Two invisible characters that got into the source, and how they are written now

`SOFT_HYPHEN` and `NON_BREAKING_SPACE` were typed as `"\u00ad"` / `"\u00a0"` escapes and **arrived on
disk as the literal characters** — `od -c` showed `302 240` where the escape had been. Ruff's RUF001
caught the non-breaking one; the soft hyphen is not in its ambiguous set and would have stayed
invisible indefinitely.

Both are now `chr(0x00AD)` and `chr(0x00A0)`. An escape is legible but can be normalised back into
the character by a tool that rewrites the file; `chr()` survives that, names its codepoint without
needing a comment, and cannot be mistaken for whitespace by a reader or a linter. A scan confirms
**zero literal soft hyphens or non-breaking spaces** remain anywhere in the new source.

## Tests — 20, all passing, no database

`tests/conditions/test_reader_lines.py`.

| Group | What it pins |
|---|---|
| normalisation | the soft hyphen becomes a real hyphen (the substitution the whole ticket depends on); trailing space goes and **leading space stays**; nothing else changes |
| lines from text | `x0` is the character offset; blank lines survive; `y` is the line index |
| lines from a PDF | words at one height become one line **across blocks**; tokens come back left-to-right whatever order the PDF emitted them; page numbers carried |
| `detect_format` | all three fixtures detect as UWM; **the UWM test is a `contains` and the Champions test an `equals`** — so a pasted sentence merely mentioning a certificate is GENERIC, not Champions; empty input is GENERIC rather than an error |
| the fixtures | `{SHY}` substituted with the count pinned at 6/3/6; the expiry table still lines up column-wise |

**One test earned its keep immediately.** `test_token_offsets_are_character_positions` asserts the
exact token tuple. The first draft said `Token("Invoice", 17.0)` from a head-count; the real offset is
**15.0**, and the test caught it. That is the argument for asserting the exact tuple rather than
something looser like "two tokens, in order" — which would have passed.

## Verified

| Check | Result |
|---|---|
| `tests/conditions/` | **57 passed** (10 LP-910 loader + 22 section 1 + 25 section 2) |
| `ruff` / `ruff format` | clean |
| `mypy app/` (strict) | clean, **533** source files |
| fixtures vs spec | `diff` clean on all three ranges, trailing whitespace stripped both sides |
| invisible characters in new source | **zero** |

## NOT verified

Section 3 is not built: **no Champions sheet and no generic paste has been read**, and `lines_from_pdf`
still has no real PDF to prove itself against. §7.4's synthetic PDF is the fixture that changes that,
and it must be *authored* rather than extracted — which is where the reviewer's row-by-row
transcription check genuinely earns its keep.

`lines_from_pdf` is exercised against **synthetic PDFs built in the test**, not against a real
condition sheet — the repo holds none by design (ADR-405), and the local smoke test over real sheets
is §7.5's, gated on `CONDITION_SHEETS_DIR`.

## Review fixes (section 1)

The review accepted `detect_format`, `normalise` and the trailing-whitespace decision, and **rejected
both** of my proposed answers to the `indent` gap. Two code changes came out of it.

**`indent` now returns `int | None`, and `Line` carries `from_pdf`.** The gap I flagged at handover:
`indent` counts leading spaces, but `lines_from_pdf` builds `text` as `" ".join(tokens)`, so it was
**always 0 for PDF input** while every fixture here is text. The spec's heading test is `<= 3 spaces`,
so a reader would have matched *every* line of *every* uploaded sheet.

Both of my suggested fixes were wrong, and the reasons are worth keeping. Reconstructing spacing from
`x0` gaps **invents characters the document does not contain**, and `text` is what `verbatim_text` is
built from — a direct collision with §9.1, and lossy under proportional fonts besides. Calibrating a
points threshold cannot be done yet, because **no PDF fixture exists until section 3 authors one**;
picking a number now means discovering it is wrong with section 2's reader already built on it. So
the absence is made visible in the type: a reader must handle `None` explicitly, and section 3 adds
the real points-based branch once there is something to measure.

**Word clustering is now sorted by vertical centre before grouping.** My chaining worry was wrong —
the dict key stays the opening word's centre and never drifts toward a mean, so drift is bounded at
one tolerance. The real defect was worse, and was found by tracing rather than reasoning: the result
**depended on word arrival order**, so identical geometry could group into one line or two depending
only on what `words_for` returned first. A layout reader whose output depends on word order is
untestable in the way that matters, and no fixture would have caught it.

⚠️ **This does not make the tolerance correct.** Two real lines 4pt apart can still merge, because a
centre within 3.0 of an *opening* word joins its cluster. Merging is the direction my own comment
wrongly called the safe one — it puts two rows' text on one line, which no later rule recovers.
Section 3 calibrates against a real PDF. Deterministic now; not yet proven correct.

Two tests pin both — `test_pdf_lines_report_no_indentation_rather_than_zero` and
`test_clustering_does_not_depend_on_word_arrival_order`. The conditions suite went 30 → **32**.

**Fixture provenance was independently re-verified** against all three spec ranges with trailing
whitespace normalised: clean. Two fixture *names* in my handover message were wrong
(`uwm_round2_2026-09-04`, and a `uwm_round3_2026-09-11` that does not exist). The files are
`uwm_round2_2026-09-10.txt` and `uwm_master_pagebreak.txt` — what the code and this ticket reference.
A wrong name in a message is exactly how a wrong name gets into a ticket.

**Not a section-1 change, but surfaced by the same run:** six of LP-904's guards were failing at setup
and had never executed. See [`LP-904.md`](LP-904.md).

## Section 2 — the UWM reader

`app/conditions/readers/uwm.py`, spec §6 rules 1–8, checked against §7.1–7.3. **25 tests, all
passing.** Still a pure function: no database, no network, no AI.

**The tests were written from the spec's expected tables before the reader ran once.** §7.1–7.3 state
the row counts, the code order, the note dates and texts, the owner hints, the expiry dates and the
duplicate count, so there was no excuse for asserting whatever the code happened to produce. 24 of
the 25 passed on first execution.

### ⚠️ The one that failed, and why it is the most useful test in the file

`test_round1_mortgagee_clause_is_captured_and_not_a_row`. My loop scanned only between `CONDITIONS`
and `EXPIRATION DATES` — so it found the clause on the **page-break** fixture, where a page boundary
breaks it into the middle of the list, and **missed it on the ordinary one-page letter**, where it
sits in the footer *below* the expiry table.

That is exactly backwards from what anyone would guess: the hard fixture passed and the easy one
failed. The clause is now captured from the block when it appears there, with a **footer fallback**
afterwards, first occurrence winning — a two-page letter prints it twice and both copies are
identical. A test written after the reader would have asserted `None` and called it correct.

### Decisions worth arguing with

**Dedup is on `(code, note-stripped text, notes)` — never on code alone.** UWM lists `0571` once per
change of circumstance, and the page-break fixture carries three of them with three different amounts
(815000 → 805000). Deduplicating by code would silently delete two genuine conditions, which is the
class of bug nobody finds because the output still looks plausible. Notes are stripped before
comparison so a page-overlap copy is still caught when the underwriter annotated one of them.

**The note regex is "asterisks THEN a date", not "asterisks".** `***NOTE***` (three asterisks, no
date) is *lender* text and appears twice inside `0132`. Matching on asterisks alone would attribute
the lender's own aside to the underwriter. The same rule is what will stop Champions' `**AM to pull
SSN Verification.` (§7.4) being read as a note in section 3.

**Expiry dates are assigned by nearest column, never by order.** Six of the twelve columns are blank
on every real sheet — 6 dates, 12 headers — so the Nth date is not the Nth header. Order-matching
would file insurance under `other` and nothing would look wrong. Over `8` from any header, the date
is reported in a warning rather than assigned.

**A missing header is a warning, not a failure.** The page-break fixture has no letterhead at all.
Refusing the sheet would discard all 16 conditions on it, so the reader warns `header not found` and
reads the conditions block anyway — which is what §7.3 expects.

**`coerce_date` is reused from `app/ai/extraction/parsing.py`** rather than writing a third
`MM/DD/YYYY` parser. It already accepts ISO and six US formats and never raises.

**The category regex is non-greedy so `TC:` is not swallowed.** Row `6378  TC  TC: Title company…`
has the category `TC` and a text *beginning* `TC:`. It appears in all three fixtures.

**A malformed shipped code map does not take the reader down.** `load_seed` raising
`LenderCodeSeedError` costs every row its code-map owner hint and nothing else; the seed step is
where that error is fatal.

### What section 2 does not do

`processor_assist` — the `(PA)` prefix from rule 3 and rule 7 — is implemented but **untested**: no
`(PA)` row appears in any of the three fixtures, so that branch has never executed. Rule 5's stored
fingerprint is also not computed here; `_fingerprint_text` exists only for deduplication, and the
persisted `text_fingerprint` belongs to LP-909's import.

## Related

LP-904 (`BucketKind`, `OwnerHint`, `OwnerHintSource`, `ConditionSheetFormat`), LP-910 (the code map
owner hints resolve through in section 2), `page_ocr.words_for` (LP-620-era, reused rather than
reimplemented), LP-905 (calls these readers from the parse task), LP-908 (runs only when a reader
sets `needs_ai`).
