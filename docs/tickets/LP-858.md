# LP-858 — The draft screen does not behave like the decision document says

- **Ticket:** LP-858 · Communication v1 follow-up · **UX defects, reported from use**
- **Status:** IN PROGRESS — built in four commits, each reviewed before the next starts.

## Progress

| # | Scope | Status | SHA |
|---|---|---|---|
| 1 | §1 — the mail-client picker fires on the button, not on open | REVIEWED — clean, 3 items folded into commit 2 | `1bf2ce3f` |
| 2 | §5 — hide ✦ polish while `email_draft_enabled` is false | REVIEWED — fix in `c134ae13` | `734c553e` |
| 3 | §2 + §2.1 — two-pane layout, compose folded in | REVIEWED — subject gap recorded, no code fix | `85f2fda2` |
| 4 | §3 + §7 + §8 — delete | REVIEWED — fixes in `238ffab0` | `d3a24c13` |

**All four are built and reviewed. What remains is listed in "Still open" at the end of this file.**

**§4 (HTML tags in a message) is deliberately NOT built.** No reproduction exists, and the two
candidate causes need different fixes. Per the ticket's own instruction to the reviewer: code
written for §4 without a row id and a screen name IS the finding.
- **Date:** 2026-09-13
- **Reported by:** the user, 2026-09-13, from the Communication tab on a real file.
- **UI contract — BUILD AGAINST THIS:** [`../design/lp858-draft-panel.md`](../design/lp858-draft-panel.md).
  Screens rendered beside it as `lp858-draft-panel-screens.html`.
- **Spec:** `comm-v1-draft-only-spec.md` (project doc) §6 buttons, §8 opening Gmail/Outlook, §9 rich text.
  ⚠️ its §2 is stale — `uq_communications_open_draft` was dropped by LP-832.
- **Scope:** Frontend — `communication/message-dialog.tsx`, `compose-draft-dialog.tsx`,
  `mail-client-dialog.tsx`; possibly `services/email_draft.py` for §3.
- **Does not touch:** sending, receiving, the transport seam, any template's words.
- **Migration:** none expected. §3 may need one; decide after the repro.

---

## §1 — The mail-client picker preempts the draft. CONFIRMED.

> *"on clicking draft, it suddenly asked me to choose what email client you want to open. Basically
> it should display the email first and later on clicking action button I should get that pop up."*

**Reproduced in code.** `message-dialog.tsx:376`:

```tsx
<Dialog open={open && !needsClient} onOpenChange={(next) => !next && close()}>
```

and `:191`:

```tsx
const needsClient =
  data?.is_editable === true &&
  preferences.data !== undefined &&
  preferences.data.mail_client === null &&
  !askedThisSession;
```

So on the **first editable draft of a session**, before any preference is saved, `needsClient` is
true and the message dialog is **suppressed entirely** — `open && !needsClient` is false — while
`MailClientDialog` renders. The processor asked to see a draft is asked instead which mail client
they use, about a message they have not read yet.

**Why it was built this way, and why that reason no longer holds.** The comment at `:755-762` is
about *z-order*: two modals were open at once, Radix marks everything outside the topmost one
`aria-hidden`, and the picker rendered first was unreachable to a screen reader. The fix chosen was
to render the picker last **and suppress the dialog underneath it**. That solves the accessibility
bug and creates this one. The right shape keeps both fixes: **do not open the picker on mount at
all.**

**What it should do.** Per spec §6, the draft's buttons are
`[Copy & open <client>]` · `[Copy message]` · `[Mark as sent]` · `[Delete]`. The client question
belongs to the **first button only**, and §8 says it is *"asked once, on the first draft"* — which
should be read as *the first time the button is pressed*, not the first time a draft is opened.
`Copy message`, `Mark as sent` and simply reading the draft need no client and must not trigger it.

**Done when:** opening a draft with no saved preference shows the draft. Pressing
**Copy & open mail app** with no saved preference shows the picker, and on answering it proceeds to
do what the button said in the same gesture — it does not ask and then make the processor press
again. `Copy message` and `Mark as sent` never raise it.

---

## §2 — Two panes: list on the left, draft on the right. DECIDED 2026-09-13.

> *"draft should open in right side panel and not a pop up… new compose should behave same by
> adding new draft in left panel and compose in right panel. with close button. if user do not add
> any detail it just delete the draft."*

This supersedes the open question the first version of this ticket left for the reviewer. **One
surface, two panes, with a create mode** — not two components.

**Confirmed by construction:** every surface on this tab is a Radix `Dialog` today —
`message-dialog.tsx:376`, `compose-draft-dialog.tsx:60-61`, `mail-client-dialog.tsx`.
**`frontend/components/ui/sheet.tsx` already exists**, so this is a swap of primitive, not a new
dependency.

### The layout

**Do not build from this section — build from [`../design/lp858-draft-panel.md`](../design/lp858-draft-panel.md).**
That file is the contract: exact layout, the 300px rail, the three landing states, the literal
strings and the acceptance commands. It is deliberately the only place the layout is described, so
there is no second version of it to drift.

In one line: **list left, selected draft right, no modal**, compose is the right pane in create
mode, and `compose-draft-dialog.tsx` is deleted rather than adapted.

### Two things to preserve while swapping

1. **Do §1 first.** With the picker no longer opening on mount, the two-modal `aria-hidden`
   collision that `message-dialog.tsx:755-762` exists to work around is gone rather than re-solved.
2. **The editor loads once.** `compose-draft-dialog.tsx:96-99` deliberately uses a plain
   `<textarea>` so ProseMirror's ~55 kB is not paid on a three-field form. Folding compose into the
   right pane means the rich editor is now the *only* editor — that is correct and simpler, but it
   must not end up mounted twice.

### The empty-draft rule — needs one decision before building

*"if user do not add any detail it just delete the draft"* is right, and there are two ways to get
there. **They differ in what happens when the browser closes rather than the button being pressed**,
and that case is not rare.

| | **A — create on click, delete on close-if-empty** | **B — create on first keystroke** |
|---|---|---|
| Row appears | immediately, as asked | when they start typing |
| Close with nothing typed | row deleted | nothing was ever created |
| **Browser closed / tab navigated** | **an empty draft is left in the list** | nothing is created |
| Extra machinery | a sweep, or accept the litter | none |

**Recommendation: B.** It delivers the same thing a processor sees — a row for the draft they are
writing — and the "delete it if empty" problem stops existing rather than being handled. A is only
better if a row must appear the instant Compose is pressed, before any typing, and that has no
stated reason behind it.

**Whichever is chosen, "no detail" must be defined.** Recommendation: delete only when recipient,
subject **and** body are all empty. A processor who typed a recipient and stopped has done work;
destroying it silently is worse than leaving a row they can delete in one click.

### A correction to the decision document

`comm-v1-draft-only-spec.md` §2 says *"one open draft per party, enforced server-side"* and *"Create
a second draft does not exist."* **That is no longer true.** LP-832 dropped
`uq_communications_open_draft` — its migration is
`20260909_1200_b3f8d61a4c92_lp832_many_open_drafts.py`, and `models/communication.py:111-123`
records why: a request now creates a new draft each time, and *"several unsent drafts on one file is
the ordinary state."* The concurrency guarantee moved to a row lock in `add_needs_to_draft`.

That is convenient here — a composed draft cannot collide with an existing one — but the spec doc is
stale on the point and should be corrected, or the next person will design against a rule the
database stopped enforcing.

## §3 — Every draft deletable. NOT BUILT AT ALL. CONFIRMED.

> *"Each draft should be deleteable."*

**There is no delete route.** `api/communications.py` has exactly five:

```
GET   /draft
POST  /compose
PUT   /draft/{draft_id}/body
POST  /draft/{draft_id}/polish
POST  /draft/{draft_id}/send
```

No `DELETE`, and no `discard_draft` / `delete_draft` anywhere in `backend/app/`.

**And it was specified.** `comm-v1-draft-only-spec.md` §6 lists `[Delete]` as the quiet fourth
button, and `message-dialog.tsx:629` carries a comment describing that exact button row —
``Copy & open <client>` · `Copy message` · `Mark as sent` — spacer — `Delete``. The comment shipped;
the button never did. So this is not a regression, it is a gap between a comment and the code, and
§2's flow depends on it: without delete, an unwanted draft is permanent.

### What delete has to decide — this is the real question, not the endpoint

A generated draft is not just text. LP-809 links it to needs items through
`communication_needs_items`, and LP-801 writes `details.docs_requested` on the originating finding,
which `finding-card.tsx` reads to render the request button as **"Requested" and disable it**.

**So: does deleting a draft un-request the documents it was asking for?**

- **If no** — the finding still shows "Requested", greyed out, for a request that no longer exists
  anywhere. That is the exact defect LP-819's review found for bounced mail and fixed with
  `_clear_finding_markers`.
- **If yes** — deleting a draft reopens the needs and re-enables the buttons, and the processor can
  ask again. This is almost certainly right, and **`_clear_finding_markers` already exists and can
  be reused** — it was written for the bounce path and matches via `requested_needs_item_id`.

**Recommendation: yes, and reuse `_clear_finding_markers`.** A draft is unsent by definition, so
nothing was ever asked of anybody; leaving the file claiming otherwise is the worse failure.

Soft delete, per house convention — `deleted_at`, not a row removal. No confirmation dialog on an
unsent draft with no edits; **confirm only when `body_edited` is true**, because that is the one
case where a processor's own words are lost.

## §4 — HTML tags visible in a message. NOT REPRODUCED — needs one example.

> *"Also in message it shows html tag."*

**I could not pin this from the code, and the ticket should not pretend otherwise.** The rendering
path that most obviously could do it is, in fact, correct. `message-dialog.tsx:565-573`:

```tsx
dangerouslySetInnerHTML={{
  __html: data.body_format === "html" ? data.body : emailBodyToHtml(data.body),
}}
```

That is the right branch, and `emailBodyToHtml` escapes every tag-opening character before emitting
its own, so a plain body cannot leak markup through it.

**Two candidate mechanisms, both worth checking against a real row before writing any code:**

1. **A body whose stored `body_format` disagrees with its content.** `body_format` is written in
   exactly two places — `email_draft.py:1111` forces `PLAIN` on regenerate, and `:1746` sets `HTML`
   on save. A body containing real HTML but stamped `PLAIN` would be escaped by `emailBodyToHtml`
   and the tags would appear on screen, which is precisely the reported symptom. The regenerate path
   at `:1111` is the one that rewrites a body and stamps the column in the same statement, so it is
   the first place to look.
2. **A surface that renders a body as text rather than through the branch above.** The detail
   surface is correct; the timeline row, the polish proposal block (`:510`) and any preview are the
   ones to re-check.

**Before building:** get from the reporter **which screen, which draft, and one line of the text as
it appeared**. Then query that row's `body_format` and the first 200 characters of `body` on
staging. That single row answers which of the two it is, and they need different fixes.

**Done when:** the mechanism is named with a row id, and no surface on this tab renders a stored
body as anything other than the branch at `message-dialog.tsx:565`.

---

## §5 — "Polish is not available on this environment" is NOT a bug.

> *"After clicking polish it says Polish is not available on this environment — your message is
> unchanged."*

**Working as designed and as documented.** `core/config.py:275`:

```python
email_draft_enabled: bool = False
```

`ai/polish.py:149`:

```python
if not settings.email_draft_enabled:
    return PolishOutcome(text=None, refusal="unavailable")
```

and the module's own docstring, verbatim:

> *"IT FAILS VISIBLY OR NOT AT ALL. `email_draft_enabled` is off in every environment today, so the
> ordinary outcome is a refusal a processor can read. It never returns something subtly different
> and calls it polish: a silent degradation is worse than an error here."*

This follows the house convention — `need_prose_enabled` and `finding_prose_enabled` also default
to `False`, because a bad prompt degrades a whole queue rather than one row.

**So the question is not "fix the bug", it is "turn the flag on, deliberately, somewhere."** That
needs a decision this ticket does not make:

- Turning it on spends Bedrock tokens on every polish press and puts model-written prose in front of
  a borrower — the reason the flag exists.
- The guards are already built: `refusal_for` blocks an invented date, amount or document, and the
  regulatory floor is imported from `email_draft` rather than restated.
- **Recommendation:** enable it in **staging only**, leave production off, and have Priya press it on
  ten real drafts before it goes further. That is what the flag is for.

**A genuine defect adjacent to it, worth fixing in this ticket:** the refusal reads *"not available
on this environment"*, which tells a processor nothing they can act on and sounds like breakage. If
the answer is "this is switched off deliberately", say that — and if a processor cannot switch it
on, do not show them a button that can only refuse. **Either hide ✦ polish when
`email_draft_enabled` is false, or word the refusal as a state rather than a failure.** Hiding is
the more honest of the two and matches the page's own stated principle at
`communication/page.tsx:20-24` — *"a processor cannot tell a feature that is broken from one that
was never wired."* That principle was applied to the receiving panels and not to this button.

---

## Suggested order

1. **§1** — the picker. Smallest, highest irritation, and it unblocks §2 by removing the two-modal
   collision rather than re-solving it.
2. **§5's visible half** — hide or reword ✦ polish. One condition, no design work.
3. **§2 + §3** — the two-pane layout and delete, together. They are one change: §2's flow is
   incoherent without §3, and §3's button has nowhere to live without §2.
4. **§4** — after the reporter supplies a row.

Separately, and not a code change: **decide whether `email_draft_enabled` goes on in staging.**

## Decided 2026-09-13 — all three answered

1. **The row is created on pressing Compose**, not on the first keystroke. The abandoned-empty-draft
   case is accepted knowingly; no sweep is built for it.
2. **"No detail" means no modification at all** — To, Subject and body all as the pane opened. A
   typed recipient survives.
3. **Delete does NOT un-request.** *"Keep it simple."* Needs items and `details.docs_requested` are
   left exactly as they are, and `_clear_finding_markers` is **not** called. Recorded consequence:
   the originating finding keeps its request button greyed as "Requested". The processor is not
   blocked — the **Request documents** catalog dialog still works — and because `requested_at` is
   still NULL on an unsent draft, no clock is running and there is nothing else to unwind.

**Two deletes, not one:** a processor deleting any draft is a **soft** delete (`deleted_at`, row
kept). A composed draft closed without a single modification is a **hard** delete — the row is
removed, because it never held anything.

Full detail in [`../design/lp858-draft-panel.md`](../design/lp858-draft-panel.md) §7 and §8.

## Decided, and not to be reopened by the implementer

Compose and detail are **one surface with a create mode** (§2). ✦ polish is **hidden** when
`email_draft_enabled` is false rather than reworded (§5) — the page's own principle. Whether the
polish flag is switched on in staging is a separate call, not this ticket's.

## For the reviewer

§1, §2, §3 and §5 are verifiable by reading the files cited. **§4 is not** — if the implementer has
written code for §4 without a row id and a screen name in the ticket, that is the finding: they
guessed at a mechanism, and the two candidates need different fixes.

---

## Verification log

Recorded so "it works now" can be compared against something rather than asserted. Re-run §9 of
[`../design/lp858-draft-panel.md`](../design/lp858-draft-panel.md) at each commit and add a row.

### Baseline — 2026-09-13 16:53, before any LP-858 work

| # | Check | Baseline | Target |
|---|---|---|---|
| 1 | `Dialog\|DialogContent` in `message-dialog.tsx` | **18 hits** | mail-client + delete confirm only |
| 2 | `compose-draft-dialog.tsx` exists | **EXISTS** | gone |
| 3 | `"not available on this environment"` | **2 files** — `lib/communication/polish-messages.ts` and its test | 0 |
| 4 | `open={open && !needsClient}` | **present, line 376** | gone |
| 5 | `@router.delete` in `api/communications.py` | **0** | 1 |
| 6 | `_clear_finding_markers` in `services/email_draft.py` | **0** | **must stay 0** — delete must not un-request |

**File fingerprints at baseline** — any change to these is LP-858 work:

```
message-dialog.tsx         776L   13:51
compose-draft-dialog.tsx   213L   13:21
communication/page.tsx      70L   13:43
api/communications.py      636L   13:51
```

**Found while taking the baseline, and it makes §5 smaller than the ticket implies:** the refusal
copy is not inline — it lives in `frontend/lib/communication/polish-messages.ts`. So §5 is a render
guard plus deleting that file's unavailable branch, not a hunt through components.

### Commits

| date | sub-ticket | SHA | checks 1–6 | notes |
|---|---|---|---|---|
| 09-13 17:21 | §1 picker | *not yet in progress file* | **1–6 pass** | Verified by reading, not by grep — see note below |
| 09-13 17:53 | §5 hide polish | *not in progress file* | **check 3: 2 → 0** | PASS, and improved on the spec |
| 09-13 18:12 | §2 two panes | *not in progress file* | **1: 18 \u2192 4 · 2: gone** | PASS \u2014 read, not counted |
| 09-13 18:14 | §3 delete | *not in progress file* | **5: 0 \u2192 1 · 6: 0** | PASS with one undeclared gap \u2014 see Check 4 |

---

# Commit 1 — §1, the mail-client picker

## What was wrong

`message-dialog.tsx:376` read `open={open && !needsClient}`. On the first editable draft of a
session, before any preference was saved, `needsClient` was true — so the picker opened **and the
draft was suppressed entirely**, not merely covered. A processor who clicked a draft was asked which
mail client they use about a message they had not read.

## What changed

- **`needsClient` is a capability question now, not a trigger.** It is read by `copyAndOpen` and by
  nothing else. The `data?.is_editable === true` condition came off it: the only caller is the
  handler behind a button that renders inside the editable branch, so a condition for it there is
  one no mutation could find — the same decoration LP-857 removed from `needsAnAddress`.
- **`askedThisSession` is gone**, replaced by `pickerOpen`. The old flag existed to stop a
  mount-driven picker reappearing; a button-driven one has nothing to suppress. A processor who
  reaches the question and answers it has answered it (the dialog cannot be dismissed — "Not now"
  stores `mailto`), and one who never presses the button is never asked.
- **Answering completes the original action in the same gesture.** `onChoose` calls
  `copyAndOpen(client)` with the answer **passed in**, not read back from state — `setChosenClient`
  has not re-rendered when the callback runs, so reading state there would open `mailto:` for a
  processor who just said Gmail. That is the same defect the LP-855 review fixed one layer up.
- **§10's literal strings** applied to the picker: *"Which mail app should this open?"* and
  *"Asked once. You can change it in preferences."*

## Tests

Both names §9 requires, plus one the contract implies and nothing covered:

- `opening a draft does not ask which mail client` — asserts the question is absent **and** the
  draft is on screen. The absence alone also passes on a dialog that rendered nothing.
- `Copy & open asks once, then completes the action` — no window before the press; the question on
  the press; the window on the answer, at `mail.google.com` rather than `mailto:`.
- `Copy message and Mark as sent never raise it` — §5 names four things that must not trigger it.
  Mount and open are covered above; these are the two buttons sharing the row with the one that
  does, which is where a broad trigger would hide.

## Mutation results

| Mutant | Caught by |
|---|---|
| picker reopened on mount (`open={pickerOpen \|\| needsClient}`) | 6 tests |
| `void copyAndOpen(client)` removed from `onChoose` | 2 tests |
| `chosen ?? activeClient` → `activeClient` (stale read) | 2 tests |
| draft suppressed behind the picker again | **initially survived** |

The last one is the finding. §5 says *"the draft stays visible behind the dialog"*, and nothing
asserted it — every test passed with the message unmounted. It is asserted by **text** rather than
by role, because Radix marks the pane behind the topmost modal `aria-hidden`.

**And the assertion is "still rendered", not "visible"** — a correction from review. jsdom loads no
CSS and jest-dom is not installed, so a Tailwind `display: none` is invisible to every matcher
available here, `toBeVisible` included. Text-presence is the strongest signal this environment has,
and it catches exactly the regression it exists for, because the shape being replaced **unmounted**
the draft. A real visibility guarantee needs a browser-level test, which this does not earn. The
comment claimed more than the assertion could support and now says what it actually proves.

## Noted, then measured away — there is no accessibility gap

I flagged that two Radix modals are open at once while the picker is up, and deferred it to
commit 3. **The review measured the tree rather than reasoning about it, and there is nothing to
defer:** 16 reachable buttons before the click, exactly 3 during — "Not now", "Use Gmail", "Close",
all the picker's own — one dialog by role, and the draft's text still in the DOM.

That is Radix nesting modals correctly, not LP-855's "zero reachable buttons" state, which was two
modals hiding *each other*. The picker is fully reachable and the draft being hidden from assistive
tech while a modal sits over it is the right behaviour. Commit 3 removes the second modal for the
reasons commit 3 already has, and carries no debt from here.

**The correction is worth more than the finding.** I wrote a caveat about a defect I had not
measured, and it would have travelled into commit 3 as a promise to fix something that was never
broken.

## Acceptance commands

```
$ rg -n "needsClient" frontend/components/file/communication/message-dialog.tsx
195:  // (`open={open && !needsClient}`), so the first editable draft of a session opened onto a
205:  const needsClient = preferences.data !== undefined && preferences.data.mail_client === null;
290:    if (client === null && needsClient) {
784:          suppressed the draft underneath (`open={open && !needsClient}`), which solved the
```

Line 290 is the `Copy & open` handler; 195 and 784 are comments quoting the shape being removed.
No `open=` prop references it — `open={pickerOpen}` at 789, `open={open}` at 399.

## CI

Frontend **1764 passed** across 146 files · Biome clean · `tsc --noEmit` clean · `next build`
clean, communication route 202 kB (unchanged). Backend untouched by this commit.

### Check 1 — 2026-09-13 17:21 · §1 picker · **PASS**

Verified in the code, because the grep lies here. `rg "open=\{open && !needsClient\}"` returns **two**
hits and both are **comments quoting the old line** (`message-dialog.tsx:195` and `:784`). A reviewer
running check 4 as originally written would read that as a regression and could "fix" working code.
**Check 4 in the design file has been rewritten** to assert the live wiring instead.

What is actually there:

| | |
|---|---|
| `needsClient` | `:205` — now a pure capability question. `is_editable` and `askedThisSession` are gone from it |
| read by | `:290`, inside the copy-and-open handler, and **nowhere else** |
| picker's `open` | `:784` → `open={pickerOpen}`, its own state |
| the draft's `open` | `:399` → `open={open}`. **No longer suppressed** |
| answering completes the action | `chosen` is passed through `onChoose` rather than read from state, because `setChosenClient` has not re-rendered when the callback fires. That is what makes it one gesture instead of two |

The z-order fix was kept rather than re-solved, and the reasoning is right: a button-driven picker
never collides on mount, so both the accessibility fix and this one hold.

**Worth noting from their own write-up:** they mutation-tested it, and the mutation *"draft
suppressed behind the picker again"* **initially survived** — the existing tests did not catch the
regression this ticket exists to prevent. They added one. That is the failure mode this whole ticket
was written about, caught by the builder rather than by me.

**Protocol deviation, minor:** `docs/phases/phase4-progress.md` was not updated — last touched
09-10. §2 of the execution protocol asks for the row in the same commit. Flagging, not blocking.

---

# Commit 2 — §5, ✦ polish is absent rather than refusing

## What was wrong

Nothing, in the sense the report meant. *"After clicking polish it says Polish is not available on
this environment"* is `email_draft_enabled: bool = False` doing exactly its job, and `ai/polish.py`'s
docstring argues the case: *"IT FAILS VISIBLY OR NOT AT ALL… a silent degradation is worse than an
error here."*

The defect is one layer out. That flag is off in **every** environment, so the refusal was not an
edge case — it was the only thing the button could ever do. A processor cannot switch it on, so a
control that can only disappoint them was on screen, wearing a sentence that named the environment
as the reason and therefore read as breakage. The page's own principle, three sections above it:
*"a processor cannot tell a feature that is broken from one that was never wired."* LP-857 applied
that to the receiving panels and did not reach this button.

## What changed

- **`Capabilities` gains `polish`**, from `settings.email_draft_enabled`. Same endpoint as
  `receiving`, same reason: the flag is decided server-side, and a `NEXT_PUBLIC_` twin would be a
  second definition that can diverge from the one the refusal actually comes from.
- **`polishAvailable` guards the whole control** — the button, its explanatory sentence, and the
  refusal line. Not a `disabled`, not a tooltip: absent.
- **`false` while the capability is loading.** The restrictive default, and here also the common
  one: with the flag off everywhere, a button that flashed in and vanished would be the ordinary
  case rather than the edge.
- **The string is gone from the repo entirely** — including from the comments explaining its
  removal. §9 greps the whole frontend for it, and prose quoting it would hold that check red
  forever. That is the trap the design file's own §9 note names about `needsClient`, and it caught
  me here first: my initial comments quoted the sentence verbatim.
- `polishMessage("unavailable")` still exists, because the capability can change under a page that
  is already open and the request can fail. It no longer names the environment — it says the polish
  did not run, the message is unchanged, and to try again, which is true of the case that survives.

## Tests

- `is not on screen when polish is not wired` — waits for the **real** ProseMirror editor before
  asserting, so the absence is measured against a tree that actually mounted.
- `is on screen when polish is wired` — the positive control.
- `is absent while the answer has not arrived`.
- `test_polish_is_reported_as_wired_only_when_the_flag_is_on` — backend. Flips
  `email_draft_enabled` alone and asserts the **whole** capabilities object, so a capability wired
  to the wrong setting, or added silently later, fails here.
- `message-dialog-polish.test.tsx` declares `polish: true` for its whole suite, the shape LP-857
  used for LP-834: a flagged-out feature keeps its tests rather than rotting.

## Mutation results

| Mutant | Caught |
|---|---|
| guard ignored — button always rendered | 2 tests |
| `?? false` → `?? true` (optimistic while loading) | 1 test |
| guard reads `receiving` instead of `polish` | 1 test |

## Folded in from commit 1's review

- **The `aria-hidden` caveat was wrong and is deleted**, above. Measured, not reasoned.
- **The "visible behind the dialog" comment overstated what its assertion proves**, and now says
  "still rendered rather than unmounted".
- **Two more copies of the old picker title**, outside the files commit 1 touched:
  `docs/domain/communication/v1-screens.md:131` and `draft-only-screens.html`'s Screen 10. Both now
  carry the new strings **and** the new trigger, which was the part worth more than the wording —
  the screens doc is what somebody reaches for to find out what the screen does.
  `rg "Where do you write your email"` over the repo returns nothing.

## Acceptance commands

```
$ rg -n "email_draft_enabled|polishAvailable" frontend/
  … 12 hits. The two LIVE lines are:
  components/file/communication/message-dialog.tsx:126:  const polishAvailable = capabilities?.polish ?? false;
  components/file/communication/message-dialog.tsx:518:                        {polishAvailable ? (
  The other 10 are prose — comments in `communications.ts`, `capabilities.ts`,
  `compose-request-dialog.tsx` and three test files, all describing the flag rather than reading it.

$ rg -rn "not available on this environment" frontend/
(nothing)
```

A render guard, not a refusal string. **The hit count is not the check** — the design file's own §9
note makes this point about `needsClient`, and it applies to every pattern here: comment bodies
match any filter you write, so the two live lines are named by shape and the rest is read rather
than counted.

## CI

Frontend **1767 passed** across 146 files · Biome clean · `tsc --noEmit` clean · `next build` clean.
Backend: ruff, `mypy app/` and the full suite green.

### Check 2 — 2026-09-13 17:53 · §5 hide polish · **PASS**

| | |
|---|---|
| capability | `api/capabilities.py:47` → `polish=settings.email_draft_enabled` — **wired end to end**, not hidden by accident |
| frontend type | `lib/api/capabilities.ts:33` `polish: boolean` |
| read | `message-dialog.tsx:119` → `capabilities?.polish ?? false` — safe default |
| render guard | `:515` → `{polishAvailable ? (` — the button does not render |
| old string | gone; `rg -l "not available on this environment" frontend/` → **0** |

The button will reappear on its own the day `email_draft_enabled` is switched on. That is the
difference between hiding a control and deleting it, and they got it right.

**Better than the contract asked for.** §5 said "hide it, or word the refusal as a state". They did
both, and the reasoning in `polish-messages.ts` is sharper than mine: the refusal string has **three
producers** — the flag being off (permanent), a transport failure (transient) and a failed request
(transient) — so *"try again in a moment"* would have been false for the first. They shipped only
what all three agree on: *"Polish didn't run — your message is unchanged."* No retry is promised
because no retry can be promised. I would not have caught that.

### §2 is in flight, not judged

At 17:53 `communication/page.tsx` was **70L → 201L, modified that minute**, and `Dialog` in
`message-dialog.tsx` has fallen **18 → 2**. `compose-draft-dialog.tsx` still exists, so the
conversion is part-done. Deliberately not assessed — a half-written file is not a finding.

---

# Commit 3 — §2 + §2.1, the two panes

## What changed

**`message-dialog.tsx` is no longer a `Dialog`.** Its export is `DraftPane`; it renders a `<section>`
with its own header and ✕. The file keeps its name because §9's commands address it by path, and
renaming it would break the reviewer's own check — noted rather than hidden.

**`compose-draft-dialog.tsx` is deleted**, both it and its test. Compose is the right pane in create
mode: pressing **+ Compose** creates the row and selects it.

**`TimelinePanel` is the left rail.** It takes `selectedId` / `onSelect` instead of owning the
modal; the selected row gets a petrol left bar and `aria-current`. Header reads
*"Drafts & messages"*. 300px, stacking below `md`.

**`page.tsx` owns the split and the selection ladder** — §2.1 rules 1 through 5, in order.

## The server had to change, and that was not obvious from the contract

§8 says the row is created when Compose is **pressed**, before a word is typed. `create_compose_draft`
refused exactly that on three counts: no recipient, no subject, no body. So the shape the contract
describes was impossible without relaxing it.

**Nothing was deleted — the guarantee moved to where it belongs.** The argument for refusing an
empty subject was *"a subject a system invented is one a borrower cannot recognise"*, which is an
argument against **defaulting** one; nothing here defaults anything. *"An empty message cannot be
sent"* is enforced by `send_draft`, and a draft nobody has finished is not a message anybody sent.
A recipient that is **present** must still parse — absent and malformed are different answers, and
one of them is a typo worth reporting.

`test_a_composed_message_needs_a_subject` is replaced by three: the empty draft is creatable, the
empty draft cannot be **sent**, and a malformed address is still refused. The middle one exists so
the move is visible from the test that used to hold the other end.

## What the tests and the existing guards caught

- **The deep link lost on the one render it exists for.** `chosen` started at `null` and only
  reacted to a *change* in the URL parameter — but on mount `seededFromUrl` already equals it, so
  the comparison never fires and the newest draft won. A `?draft=` link would have opened the wrong
  draft every time. Caught by giving the fixture a newest draft that is a **different** row, so the
  case cannot pass by agreeing with the default.
- **`window.matchMedia` is absent in jsdom** — and during a server render, and in some webviews. An
  unguarded call threw before anything rendered: the whole tab gone, because of a *default*. Guarded;
  absent now means "not narrow", which is the desktop behaviour and the safe way round.
- **`lib/unwired-components.test.ts` caught `ComposeDraftButton` orphaned** — rendered only by its
  own test once the page stopped using it. That guard is not mine and it found the thing §3 asks for
  independently of §9's `test ! -f`.
- **Closing had to stick.** Rule 2 auto-selects the newest open draft, so ✕ set the selection to
  null and the next render put the same draft straight back. A pane that cannot be dismissed.

## Mutation results — the whole selection ladder

| Mutant | Caught |
|---|---|
| sent messages become auto-selectable (rule 3) | 2 |
| newest → oldest (rule 2) | 2 |
| URL loses to the default (rule 1) | 4 |
| closing does not stick | 1 |
| narrow still auto-selects (rule 5) | 2 |
| empty state keyed on drafts rather than entries (rule 4) | 1 |

The last one is the distinction §2.1 draws and it would be easy to get wrong: **sent messages but no
open drafts still renders the split** — history is worth seeing — while an **empty file** is one
full-width empty state, because a 300px column of nothing beside a larger area of nothing is two
pieces of furniture for a file with no work on it.

## Acceptance commands

```
$ rg -n "Dialog|DialogContent" frontend/components/file/communication/message-dialog.tsx
3:import { MailClientDialog } from "@/components/file/communication/mail-client-dialog";
808:      <MailClientDialog

$ test ! -f frontend/components/file/communication/compose-draft-dialog.tsx
OK — does not exist
```

The mail-client dialog only. The delete confirm arrives in commit 4 and will be the second.

**§4's `rg -in "\bsend\b"` command is not readable as pass/fail, and saying so is the honest
answer.** It returns 20 hits, none of them a Send button: the `Send to` field label, `useSendDraft`,
`canSend`, `send.mutate`, and prose in `party-address-form` and `open-draft-dialog` explaining why
there is no send. The question it is asking — *is there a button labelled Send* — is answered by
`rg ">\s*Send\b|\"Send\""`, which returns only the `Send to` **label** at message-dialog.tsx:458.
The rendered row is `Copy & open <client>` · `Copy message` · `Mark as sent`, and `Delete` lands in
commit 4.

## Not done in this commit, by design

**Delete is commit 4**, so an empty compose draft created by + Compose currently persists rather
than being hard-deleted on close. §8's hard delete and §7's soft delete arrive together with the
route, which is the next commit. This is a consequence of the commit order, not of the design.

## CI

Backend **7775 passed**, 6 skipped, 1 xfailed · ruff, ruff format, `mypy app/` clean.
Frontend **1772 passed** across 146 files · Biome clean · `tsc --noEmit` clean · `next build` clean,
communication route **202 kB** (18.7 kB route, down from 19.1 — the compose dialog came off).

---

### Check 3 — 2026-09-13 18:12 · §2 + §2.1 two panes · **PASS**

Read rather than counted, because the counts here would pass on a layout that renders nothing.

| §2.1 rule | Where it is | Verdict |
|---|---|---|
| split is the landing state | `page.tsx:166` — `flex-col gap-6 md:flex-row`, rail `md:w-[300px]` at `:167` | correct, and 300px is the specified width |
| rule 1 — `?draft=` wins | `:59` — `useState<string \| null>(linkedDraft)`, **seeded** rather than watched | correct, and the comment names the exact bug the other shape has: on mount `seededFromUrl` already equals the parameter, so a change-watcher never fires on the one render a deep link exists for |
| rule 2 — newest open draft | `:105` — `openDrafts.find(…)` over `entries` | correct **because** `services/timeline.py:528` sorts `reverse=True`. This is the check that mattered: over an oldest-first list the same line silently selects the wrong draft |
| rule 3 — never a sent message | `:43-46` filters `status === "draft"` before any selection | correct |
| rule 4 — zero entries, no rail | `:145` returns a full-width empty state; the split's own empty state at `:199` is inside the right pane | correct, and it tests `entries` not `openDrafts`, which is the distinction that makes sent-history-with-no-drafts still render the rail |
| rule 5 — stacked selects nothing | `:86-91` — `matchMedia` read once, guarded for SSR and jsdom | correct; absent means "not narrow", which is the safe way round |
| rule 6 — never show a deleted row | `:68` `deletedIds`, consumed at `:103-105` | correct — holds the id for the frame between the 204 and the refetch, which is exactly where "select the next newest" would land back on the deleted row |

**No second editor.** `useEditor` / `EditorContent` appear once in the tree, `message-editor.tsx:142`
and `:305`. Folding compose into the pane introduced no second TipTap mount — §3's named trap.

**§10 strings present**, with one correction to the contract rather than to the code: `"Drafts &
messages"` is at `timeline-panel.tsx:407` as `Drafts &amp; messages`, which is the correct JSX
spelling. A literal grep for the string returns **0** and means nothing. Noted here so the next
reviewer does not read a zero as a defect — the same trap check 4 set in commit 1.

**`closed` is not reset by selecting another row, and that is right**: `chosen` is non-null after a
click and wins at `:106`, so the ✕ stays dismissible without making auto-selection reappear.

---

# Commit 4 — §3 + §7 + §8, Delete

## The route that was specified and never built

`comm-v1-draft-only-spec.md` §6 listed `[Delete]` as the quiet fourth button, and `message-dialog.tsx`
carried a **comment** describing that exact row. The comment shipped; the button did not, and neither
did an endpoint. `DELETE /outbound/draft/{draft_id}` now exists, 204, no body.

## Two deletes, and the destructive one is the guarded one

| Case | What happens |
|---|---|
| a draft the processor deletes | **soft** — `deleted_at`, row kept |
| a composed draft closed with no modification | **hard** — row removed |

`?discard=true` is a **request, not an instruction**. `_is_untouched_compose_draft` re-derives from
the row whether it may go: no template key, no needs link, nothing in To, Subject or body. So a
client that miscomputes "no modification" can only ever cause a **soft** delete, never data loss.
The client decides whether to ask; the server decides whether the row is one that may be removed.

## It does not un-request

Decided, and asserted: `test_deleting_a_draft_leaves_its_needs_and_finding_markers_untouched`
snapshots every need's status and `requested_at` before and after, and checks the
`communication_needs_items` rows survive. `_clear_finding_markers` is not called — it exists for the
bounce path and matching on `requested_needs_item_id` would reopen needs a processor did not reopen.

**The accepted consequence:** the originating finding keeps its request button greyed as "Requested".
The processor is not blocked — the catalog dialog still adds the document — and because
`request_needs_item` has no caller, `requested_at` is NULL and the needs are still PENDING. No clock
is running. **The only stale thing is that one button.**

## Confirms

- **Unedited → no confirm.** Template output; deleting it destroys nothing a person wrote, and a
  confirm on every delete is the dialog people learn to dismiss without reading.
- **Edited → confirm, quoting their own first line.** `body_format === "html"` *is* `body_edited`
  (LP-853 made that one fact in one place). Plex Serif italic, which is the Ledger's mark for
  verbatim quotation — words we are repeating back rather than words we wrote.
- **Hard delete → silent.** Nothing to undo.

## Mutation results

| Mutant | Caught |
|---|---|
| hard delete accepts anything | 2 backend |
| soft delete becomes a hard delete | 2 backend |
| a sent message is deletable | 1 backend |
| delete clears the needs links (un-requests) | 1 backend |
| a template key no longer disqualifies a discard | **initially survived** |
| confirm skipped on an edited draft | 3 frontend |
| confirm shown on every draft | 1 frontend |
| delete asks for a hard delete | 2 frontend |
| `onDeleted` never fires | 2 frontend |
| close discards whenever the body is empty | 1 frontend |
| auto-select does not skip deleted rows | 3 frontend |
| delete also marks the pane closed | 1 frontend |
| deleted ids are not remembered | **initially survived** |

**THE TABLE ABOVE WAS INCOMPLETE, AND REVIEW FOUND OUT HOW.** I mutated the conditions I had
thought about. The review mutated **all six on the delete path, one at a time**, and three survived
— against 2,141 tests across `tests/api` and `tests/services`, not just the delete file:

| Survivor | Why it mattered |
|---|---|
| the **body** check in `_is_untouched_compose_draft` | **the guard this entire asymmetry exists for.** My own docstring is written about it — *"a hard delete acting on a claim is how a processor's half-written message disappears because a comparison somewhere was wrong about whitespace"* — and the one shape nothing covered was a client asking to discard a draft somebody had **typed into**. The recipient test proved the asymmetry works; nothing proved it works for the field holding the words. |
| the route's **direction** check | Reachable: the route takes an id and `_scoped` resolves any communication on the file, so a caller passing an **inbound** id reaches it. |
| the **needs-link** check | Cannot fire. Verified independently: all three sites that write a `CommunicationNeedsItem` attach it to a draft whose creation sets a `template_key` — `party_requests.py:282`, `email_draft.py:1484`, and the append path at `:1433`, whose `open_draft` comes from `get_open_draft` → `_open_drafts_stmt`, which filters on `template_key`. The template check always fires first. |

The first two are tested now (`238ffab0`), including that the body refusal does not fall through to
a soft delete. The third was **kept and documented rather than tested**: a test would have to build
a row no code path can produce, which asserts the fixture rather than the product. It is the second
lock on the only operation here that destroys a row, and the day something links a need to a
template-less draft it is the check that notices.

**The lesson is about the method, not the guards.** Mutating the conditions I had in mind confirmed
what I already believed. Mutating *every* condition on the path found the one the design argument
was actually about.

**Two guards were decoration until the case that separates them was written**, which is the same
lesson twice in one commit:

1. `template_key is not None` agreed with the needs-link check on every fixture. They part company
   on a draft **rendered from a template with no needs links** — not a compose draft however empty
   it looks, because hard-deleting it removes the record that a request was prepared.
2. `deletedIds.has(chosen)` was unreachable, because `onDeleted` already clears the selection. It
   separates when the URL puts a deleted id **back** — the browser Back button after a delete, one
   keystroke away. Without it the pane says *"This message could not be loaded"* for a row the
   processor deleted a moment ago on purpose.

## §2.1 rule 6

Deleting the selected draft selects the next newest open draft, or falls to the empty state.
`deletedIds` is held locally because the refetch is not instant: for the frame between the 204 and
the new list, the deleted row is still in `entries` and "select the next newest" would land straight
back on it.

## Folded in from commit 3's review

**My docstring claimed the subject guarantee moved to `send_draft`. It did not — it was dropped.**
The body check is real (`email_send.py` refuses an empty one); nothing checks the subject anywhere,
so a draft with none can now be marked sent, which was impossible before commit 3.

The consequence is small and specific: **LP-821's evidence row can record a message with no subject
for a message that almost certainly had one**, because the processor typed it into Gmail after
`Copy & open` handed them a blank subject line. The audit record and what the borrower saw diverge
quietly.

**I have not added the refusal, and that is a scope call rather than a judgement that it is fine.**
Neither §4 nor §8 decides it, and `send_draft` is outside what this ticket names. **Recommended:
`send_draft` should refuse an empty subject the way it refuses an empty body.** The docstring now
says the guarantee was dropped instead of claiming it moved.

## Also found

`useDeleteDraft` inlined `["timeline", fileId]` and `verification-draft-invalidation.test.ts` —
not my guard — rejected it: one module owns timeline invalidation, because every mutation touching a
draft used to keep its own list of keys and the lists drifted, one ending up on a query nothing
reads. Routed through `invalidateDraftViews`, which also gets LP-845's cross-tab broadcast — worth
more for a delete than for a request, since a second tab would otherwise keep offering a row that
is gone.

## Not done — and it is a gap, not an omission

**§7's "Toast with an Undo" has no Undo.** The toast names the consequence (*"the documents it asked
for are still on the needs list"*); there is no undo button, because undoing a soft delete needs a
route that clears `deleted_at`, and §7 authorises exactly one route: `DELETE /draft/{draft_id}`.
Deferring the request until the toast expires was the alternative and it is worse — the row would
linger on screen, and a navigation inside the window would silently cancel a delete the processor
asked for.

Mitigation, such as it is: the row is soft-deleted, so nothing is lost in the database, and the
confirm covers the only case where a processor's own words are at stake. **Recommended: a restore
route, as its own ticket.**

## Two more things review settled

**The "Delete" disambiguation is a precaution, not a guard.** Replacing `within(confirm).getByRole`
with an unscoped `getAllByRole(...)[0]` leaves all ten tests passing — there is only one reachable
"Delete" at that moment, so the scoping decides nothing today. Kept, because unlike an index it
cannot silently drift if a second one appears. **Not counted as a guard.**

**Over-invalidating on delete is the cheaper error.** `invalidateDraftViews` refreshes needs and
activity, neither of which a delete changes — two wasted refetches. The alternative is a second
definition of "what a draft change invalidates", which is exactly what the guard that rejected the
inlined key exists to prevent. Wasteful beats divergent, and the waste is bounded.

## Acceptance commands

```
$ rg -n '@router.delete' backend/app/api/communications.py
183:@router.delete("/draft/{draft_id}", status_code=status.HTTP_204_NO_CONTENT)

$ rg -n "_clear_finding_markers" backend/app/services/email_draft.py
(nothing)

$ rg -n "Dialog|DialogContent" frontend/components/file/communication/message-dialog.tsx
3:import { DeleteDraftDialog } from "…/delete-draft-dialog";
4:import { MailClientDialog } from "…/mail-client-dialog";
910:      <DeleteDraftDialog
917:      <MailClientDialog
```

The mail-client question and the delete confirm, which is exactly what §3 permits.

## CI

Backend **7783 passed**, 6 skipped, 1 xfailed · ruff, ruff format, `mypy app/` clean.
Frontend **1786 passed** across 147 files · Biome clean · `tsc --noEmit` clean · `next build` clean,
communication route **203 kB** (16.9 kB route, down from 18.7).


---

### Check 4 — 2026-09-13 18:14 · §3 + §7 + §8 Delete · **PASS**, one undeclared gap

| Check | Value | Target |
|---|---|---|
| 1 `Dialog\|DialogContent` in `message-dialog.tsx` | **4** — two imports, two JSX tags at `:910` `:917` | mail-client + delete confirm only ✅ |
| 2 `compose-draft-dialog.tsx` | gone | ✅ |
| 3 `"not available on this environment"` | 0 | ✅ |
| 4 `open={open && !needsClient}` | 2, **both comments** · 4a `const needsClient = ` = 1 · 4b `if (client === null && needsClient)` = 1 | ✅ |
| 5 `@router.delete` | 1 — `communications.py:183` | ✅ |
| 6 `_clear_finding_markers` | 0 | ✅ **must stay 0** |

**The soft delete actually leaves the list**, which counts cannot show: `delete_draft` sets
`deleted_at` at `email_reply.py:265`, and `timeline.py:461` wraps the `Communication` select in
`only_active(…)`, which is `deleted_at IS NULL` (`models/helpers.py:31`). This is the check worth
running here — the systemic pattern across the 25 earlier tickets was a column written and never
read, and a `deleted_at` nothing filters is exactly that shape.

**The hard delete is a request, not an instruction, and that is better than the contract asked for.**
`_is_untouched_compose_draft` (`email_reply.py:270`) re-derives removability from the row — template
key, recipient, subject, body, linked needs — so a client that miscomputes "no modification" can
only ever cause a soft delete. §8 left that to the client; the server taking it back is right.

**Declared gaps — accepted, both need a decision from Geet:**

1. **§7's "Toast with an Undo" has no Undo** (`LP-858.md:801`, self-reported). Undoing a soft delete
   needs a route clearing `deleted_at`, and §7 authorises exactly one route. The rejected
   alternative — defer the DELETE until the toast expires — **is** worse: a navigation inside the
   window silently cancels a delete the processor asked for. Data is not lost; recovery is a
   database query. **Needs a restore-route ticket.**
2. **`send_draft` does not refuse an empty subject** (`LP-858.md:785`, self-reported). LP-821's
   evidence row can record no subject for a message that had one. **Needs a ticket.**

**Undeclared gap — the finding of this check:**

**§6 says `Copy & open` is disabled when the party has no address. It is not.**
`message-dialog.tsx:178` — `const canSend = bodyPlain.trim().length > 0 && !send.isPending;` — and
that gates `Copy & open` at `:768` and `Mark as sent` at `:800`. `needsAnAddress` (`:273`) gates only
whether `PartyAddressForm` renders at `:531`. So on a draft with no address on file the processor can
press `Copy & open` and get a compose window addressed to nobody, with the field that fixes it
sitting directly above. `Copy message` is correctly enabled.

No test asserts it either way — `message-dialog-address.test.tsx` covers the form's presence and not
the button's state. Not a regression: it was never wired, and §6 is a state table rather than one of
the four numbered commits, so nothing in the commit plan owned it. Small — a `|| needsAnAddress` on
one condition and a test. Recorded rather than fixed, because it is a product call whether a
processor may open a blank-To compose window deliberately.

**Contract corrected, not the code:** §10's `"This cannot be sent yet. Nobody has recorded an address
for the {party} on this file."` exists nowhere. `party-address-form.tsx:69` says **"No address on
file for the {party}"** with **"Who are they, and where do we write?"** under it — LP-857 copy that
predates this ticket, paired with the inline Save block §6 requires, and better: it names the state
and asks the answerable question. §10 has been updated to the shipped strings so the next reviewer
does not "fix" working copy.

**Protocol deviation, now four for four:** `docs/phases/phase4-progress.md` has not been touched
since 09-10. §2 of the execution protocol asks for the row in the same commit. The record exists —
it is in this file, in more detail than the progress table would hold — but it is in the wrong
place, and the progress file is what the next session reads first.

---

# Peer review, the other direction

I received review on all four commits and did not review the reviewer's until asked. Both of their
commits are now read rather than assumed, and their claims re-run:

**`c134ae13` — the polish refusal's retry promise.** Verified: `unavailable` has three producers
(`polish.py:150` flag-off, permanent; `polish.py:166` transport failure, transient;
`message-dialog.tsx:248` failed request, transient — plus `:243`'s fallback for a response with no
refusal, same class). Their mutation claim is exact: restoring *"Try again in a moment"* fails one
test and only one. The new suite's control — asserting the matcher DOES fire on a sentence that
promises a retry — is what stops the not-match being about a pattern that never fires.

**`238ffab0` — two guards nothing asserted.** Both mutants die now; I re-ran them. The body test
isolates its guard cleanly: a compose draft with only a body has no template key, no needs link and
nothing in To or Subject, so the body check is the only one that can fire. The inbound test
distinguishes by MESSAGE rather than by status code, which matters because the status guard would
also 409 — the assertion on the word "arrived" is what makes it about the direction check.

Their unreachability finding on the needs-link check I verified independently rather than accepting:
`party_requests.py:282` sets `template_key=key`, `email_draft.py:1484` sets
`template_key=draft_template_key(party)`, and `:1433`'s `open_draft` comes from `get_open_draft` →
`_open_drafts_stmt`, which filters on `template_key`. Keeping it documented rather than tested is
right.

## And one claim of mine that neither of us had run

*"The processor is not blocked — the Request documents catalog dialog still adds the document."*
That sentence is what the whole **do not un-request** decision rests on: delete leaves the finding's
button greyed, and the reason that is acceptable is that asking again works. It was asserted in
prose through four commits and two reviews and never executed.

It holds, and now there is a test. It composes because `get_open_draft` reads through
`only_active`: the deleted draft is invisible, so its needs are not counted as "already in a draft"
and the new request carries them. A mutant that makes the soft delete a no-op fails it.

Had it been false, deleting a draft would strand the document — asked for, no message, no way to
ask again — and *"keep it simple"* would have been the wrong call rather than the simple one.

# Still open

Nothing in LP-858's build scope. These are the things this ticket deliberately did not do.

| # | What | Why it was not done here | Needs |
|---|---|---|---|
| 1 | **§4 — HTML tags visible in a message** | Not reproduced. Two candidate mechanisms — a body whose `body_format` disagrees with its content, or a surface rendering a body outside the one correct branch — and they need **different fixes**. | From the reporter: which screen, which draft, one line of the text as it appeared. Then query that row's `body_format` and first 200 chars on staging. |
| 2 | **§7's Undo** | Undoing a soft delete needs a route clearing `deleted_at`; §7 authorises exactly one route. Deferring the DELETE until the toast expires was the alternative and is worse — the row lingers and a navigation silently cancels a delete the processor asked for. | Its own ticket: a restore route. The screen is honest in the meantime — the toast never says "Undo". |
| 3 | **`send_draft` should refuse an empty subject** | The guarantee was *dropped*, not moved, when §8 made an empty compose draft creatable. `send_draft` is outside what this ticket names. | One guard, symmetrical with the empty-body refusal. Until then LP-821's evidence row can record a message with no subject for one that almost certainly had one. |
| 4 | **§9's `rg -in "\bsend\b"` line** | It cannot tell a button from a field label, a hook name or prose — it returns 20 hits, none a violation. A command that cannot be read as pass/fail is an opinion with extra steps. | A line in the design contract matching a **button**. Not touched here: the contract was being edited concurrently. |
| 5 | **Whether `email_draft_enabled` goes on in staging** | Not a code change, and explicitly not this ticket's call. | A decision. The guards are built; the recommendation on file is staging only, with ten real drafts read before it goes further. |

**Not open, recorded so nobody re-opens it:** the needs-link check in `_is_untouched_compose_draft`
survives every mutant because it **cannot fire** — every writer of a `CommunicationNeedsItem`
attaches it to a draft with a `template_key`, so the template check always fires first. It is a
second lock on the only operation here that destroys a row, and it stays.

---

### Full contract audit — 2026-09-13, after all four commits

Every numbered requirement in `../design/lp858-draft-panel.md` checked against the code, not against
the commit messages.

| § | Requirement | Where | Verdict |
|---|---|---|---|
| 2 | split is the landing state | `page.tsx:166` | ✅ |
| 2 | 300px rail | `page.tsx:167` `md:w-[300px]` | ✅ |
| 2 | zero drafts → no split | `page.tsx:145` | ✅ |
| 2 | nothing modal but picker + delete confirm | `message-dialog.tsx:910,917` — the only two | ✅ |
| 2 | stacks below ~720px | `page.tsx:166` `flex-col md:flex-row`, `:90` `max-width: 720px` | ✅ |
| 2 | `[✕]` on the right pane | `message-dialog.tsx:502` `aria-label="Close"` | ✅ |
| 2 | header "Drafts & messages" | `timeline-panel.tsx:407` | ✅ |
| 2.1 | rules 1–6 | `page.tsx:53-106`; 11 tests in `page.test.tsx` | ✅ all six, plus 5 cases the contract did not ask for |
| 3 | right pane is not `dialog.tsx` | inline layout in `page.tsx` | ✅ |
| 3 | `compose-draft-dialog.tsx` deleted | absent | ✅ |
| 3 | no second TipTap mount | `useEditor` once, `message-editor.tsx:142` | ✅ |
| 4 | row order + spacer | `:765` `:777` `:796` → `:853` `ml-auto` → `:854` Delete | ✅ |
| 4 | label falls back to "mail app" | `compose-routes.ts:77` | ✅ |
| 4 | no Send button, not even disabled | no Send label renders anywhere | ✅ |
| 4 | ✦ polish absent, not refusing | `:126` `polishAvailable`, guard at `:594` | ✅ |
| 5 | picker fires on the button only | `:290`, and nowhere else | ✅ |
| 5 | draft stays visible behind it | `:399` `open={open}` | ✅ |
| 5 | suggestion pre-selected and says why | `mail-client-dialog.tsx:64,117` | ✅ |
| 5 | "Whatever this computer opens" is the skip | `:44`, `:129` "Not now" stores `mailto` | ✅ |
| 5 | answering completes the action | `chosen` passed through `onChoose` | ✅ |
| 6 | state table | rows and pane states | ⚠️ **one row wrong — see below** |
| 7 | soft delete, hard delete, no un-request | `email_reply.py:220-290` | ✅ |
| 7 | confirm only when `body_edited`, quoting them | `delete-draft-dialog.tsx` | ✅ |
| 7 | toast with an **Undo** | toast yes, Undo no | ⚠️ **declared gap, item 2 above** |
| 8 | row created on Compose press | `page.tsx:109` | ✅ |
| 8 | untouched close → hard delete | `:455` `discard: true`, server re-derives | ✅ |
| 9 | 8 of 9 acceptance commands | run verbatim | ✅ |
| 9 | the `\bsend\b` command | unusable as written | ⚠️ **self-reported, item 4 above** |
| 9 | 11 named tests | all 11 present | ✅ |
| 10 | literal strings | all present | ✅ (one contract string corrected — see Check 4) |

**A fifth button sits in the §4 row and is not a violation:** the secure-link button
(`message-dialog.tsx:826`) is gated on `receiving`, which is false in every environment, so the row
a processor sees is exactly §4's four.

**The §6 divergence, restated because it is the only undeclared one:** `Copy & open` is not disabled
when the party has no address. `message-dialog.tsx:178` gates it on a non-empty body alone.

**What this audit could NOT do:** re-run the suites. `node_modules` on this checkout holds macOS
native bindings and the shell available here is Linux (`rolldown` fails to load), so the CI lines in
this file — frontend 1786 / backend 7783 — remain the builder's claim, checked only for internal
consistency across the four commits. The tests' existence, names and assertions were read directly.
Nobody has yet looked at the rendered screen.
