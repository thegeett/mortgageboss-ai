# LP-393-4b — Re-score `same_line_of_work` against Priya's fresh labels

## What this ticket is

The last of the four scenario tags to be validly measured. LP-393-4a's `same_line_of_work` 31% was INVALID —
the fixed AI was scored against goldens Priya made on the occupation-LESS sheet (the stale-golden trap; its
prompt was confirmed correct, only the labels were stale). LP-393-2a regenerated the sheet with `occupation`
visible and blanked only the 7 data-gap cells; **Priya has now re-labeled them blind.** This scores
`income.same_line_of_work` against those fresh goldens so **IN-7** can join **IN-10 / IN-11 / AS-11** at the
bar step (LP-393-5).

## Acceptance criteria (met)

- Fresh labels joined by `(tag_id, subject_id)`; **0 unmatched**, none guessed.
- `same_line_of_work` scored against the fresh goldens with the REAL reasoner; clear-cut and ambiguous split.
- The preserved goldens (the other three tags + the same_line rows she left alone) confirmed unchanged; the
  two cells she *revised* are reported, not reverted.
- No tag/rule/producer/prompt/fixture change; `ACTIVE_RULE_IDS` unchanged (20); LF-6T3N untouched; suite green.

## Phase 0 — the join report

- **Join:** 39 filled income goldens (13 × has_2yr_history + 13 × is_declining + 13 × same_line_of_work);
  materialized `income_stability` predictions joined **0 unmatched** by `(tag_id, subject_id)`.
- **What she filled vs left `unknown`:** every `same_line_of_work` cell is now a **concrete** call — **12 `yes`
  / 1 `no` (B8), zero `unknown`, zero blank.** The data gap is fully resolved: a remaining `unknown` on an
  occupation-present row would have been a real finding (the tag unanswerable even with occupation); **there is
  none.**
- **The 7 re-labeled rows** (B3, B4, B6, B9, B10, B12, B13) all came back **`yes`** — matching her stated rule
  "one employer + unchanged occupation throughout → yes." Each keeps its original "No occupation given." note +
  the LP-393-2a `RE-LABEL` marker as the evidence chain.
- **Two PRESERVED cells were REVISED by her** in the same sitting (reported, NOT reverted — her fresh labels
  are the reference):
  - **B8 `yes` → `no`.** Warehouse Picker → Office Administrator. With occupation shown she read it as a
    *different* line of work — which is the clear-cut expected answer. The occupation fix didn't just fill the
    7 gaps; it let her correct a cell she'd mislabeled `yes` when occupation was hidden.
  - **B5 `no` → `yes`.** Single employer / single document (Bright Retail, Sales Associate). Her "one employer
    throughout = yes" rule applies; her earlier `no` was a can't-assess-one-year read.
- **The other three tags' goldens are unchanged** by the re-label round (verified against committed `f4d6b59`):
  has_2yr_history (13), is_declining (13), asset.liquidation_terms (6) all byte-identical; the untouched
  same_line rows B7/B11/B14/B15 unchanged.
- **Real reasoner, point-in-time:** scored via `materialize_tags(only_groups={income_stability})` with
  `reasoner=None` (real `claude-sonnet-4-5`), then `score_snapshot_against_golden`. Non-deterministic — the
  numbers are a point-in-time read, not a fixed constant (the keyless test pins the mechanism + the fresh-label
  state, never the live %).

## Phase 1 — the score

### `income.same_line_of_work` — **100% (13/13)** — FRESH (this pass)

| split | agreement | n |
|-------|-----------|---|
| clear-cut (B7 nurse→nurse=`yes`, B8 warehouse→office=`no`) | **2/2** | 2 |
| ambiguous (B3–B6, B9–B15 — her rule is the definition) | **11/11** | 11 |

Value distribution: Priya 12 `yes` / 1 `no`; AI 12 `yes` / 1 `no`. **Zero disagreements.** On every no-job-change
row the AI answered `yes` ("same employer + same occupation, no job change") — exactly her rule. Both clear-cut
checks pass. **The 31% was purely the stale-golden trap; with adequate data the tag measures correctly.**

### The two group-mates — refreshed naturally in the same pass (they share `income_stability`)

| tag | 4b (fresh) | LP-393-4a | disagreements |
|-----|-----------|-----------|---------------|
| `income.is_declining` | **100% (13/13)** | 100% (13/13) | none |
| `income.has_2yr_history` | **85% (11/13)** | 85% (11/13) | B12 (pay-stub-only → she wants a W-2/1099), B14 (the open framing item) |

Both reproduced LP-393-4a exactly — reported **fresh** (this materialization), not carried, since scoring
same_line naturally re-runs the whole group.

### The fourth tag — carried

`asset.liquidation_terms` = **100% (6/6)** — CARRIED from LP-393-4a. It lives in the separate `asset_facts`
group (document subject), NOT re-run by scoring same_line; its prompt + labels are settled. Its goldens are
confirmed unchanged.

## Phase 2 — the verdict + the consolidated bar-input picture

**`same_line_of_work` verdict: calibration-passed → IN-7 ready for a bar.** Not blocked on the prompt
(confirmed correct), not on data (the gap is resolved); the AI's implicit definition matches Priya's rule.

### THE CONSOLIDATED FOUR-TAG PICTURE (input to LP-393-5's bar proposals)

| tag | accuracy | n | fresh / carried | verdict | rule it gates |
|-----|----------|---|-----------------|---------|---------------|
| `income.same_line_of_work` | **100%** (13/13) | 13 | FRESH (4b) | calibration-passed | **IN-7** |
| `income.is_declining` | **100%** (13/13) | 13 | FRESH (4b, = 4a) | calibration-passed | **IN-10** |
| `income.has_2yr_history` | **85%** (11/13) | 13 | FRESH (4b, = 4a) | calibration-passed | **IN-11** (also IN-12) |
| `asset.liquidation_terms` | **100%** (6/6) | 6 | CARRIED (4a) | calibration-passed | **AS-11** |

**All four are calibration-passed → all four are ready for LP-393-5 bar proposals** (with the synthetic caveat).

> Rule mapping is from `activation_bars.yaml` (the gate of record): the load-bearing AI tag of IN-7 / IN-10 /
> IN-11 / AS-11. **Minor finding (out of scope):** the worksheet's own `consuming_rules` column shows the income
> tags as IN-1/IN-3 (`worksheet.py` `_TagMeta`), which is stale relative to the per-borrower income re-scope
> (LP-390-1). It's cosmetic (labeler context), does not affect the bar gate, and is worth a one-line worksheet
> fix in a later ticket — not touched here (no fixture/worksheet change in 4b).

## The open items (carried forward for LP-393-5)

1. **`has_2yr_history` B14 framing — STILL OPEN, NOT settled this sitting.** Priya left B14 = `no` (VOE contract
   ended 2026-06-30; two W-2s present; her note: "currently unemployed… need a new offer letter + a paystub").
   The tag asks HISTORY (which exists → AI `yes`); she answered CONTINUATION. She re-labeled same_line but did
   not revise B14 or its note, so the definitional question — *does a terminated job's two years still count as
   history?* — remains for her explicit ruling before IN-11's bar. (The B12 pay-stub-only divergence is the same
   flavor: her documentation standard, a rule the system does not yet encode — domain knowledge worth a rule,
   not an AI error on the core question.)
2. **The synthetic-data caveat.** These validate the AI's REASONING on clean scenario data — NOT robustness to
   real-document messiness. Any bar set in LP-393-5 must be paired with the LF-6T3N real-data result (n=2,
   indicative).

## Phase 3 — tests + equivalence

- `test_income_scenario_relabel_scored_lp393_4b.py` (keyless): the data gap is resolved (13 filled, 0 blank, 0
  `unknown`, 12 `yes`/1 `no`); the 7 re-labeled cells are filled and keep their evidence note; the clear-cut
  B7/B8 checks hold with occupation shown; the untouched rows are unchanged and the two revised cells (B5, B8)
  are pinned; the other three tags' goldens are untouched; `split_scored` keeps clear-cut apart and counts an
  `unknown` as a valid label, never a skip. The live 100% is non-deterministic → asserted nowhere, reported
  here.
- **EQUIVALENCE:** no tag / rule / producer / prompt / fixture change; `ACTIVE_RULE_IDS` unchanged (**20**);
  LF-6T3N untouched; full suite green (**2600 passed**, ruff + mypy clean). The CSV was normalized CRLF→LF
  (an Excel-save artifact from her re-label); its content is her fresh labels.

## Assumptions, decisions & gaps

- **Her fresh labels are the reference — none re-derived.** The two cells she revised (B5, B8) are her fresh
  judgment and are reported as a finding, not reverted to the LP-393-2a preserved values.
- **Fresh vs carried, stated plainly:** the three income tags are fresh from this pass (scoring same_line
  re-runs the group); liquidation is carried from LP-393-4a (separate group, not re-run).
- **No new ADR.** `same_line_of_work` did NOT diverge once the data was adequate — the outcome the ticket said
  would force an ADR did not occur. The stale-golden process lesson is already recorded in ADR-315 (revised in
  LP-393-4a).
- **Ready for LP-393-5 (Priya sets the bars):** IN-7, IN-10, IN-11, AS-11 — all calibration-passed, with the
  synthetic caveat and the two open items (B14 framing; B12 pay-stub standard) carried.
