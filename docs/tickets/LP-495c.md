# LP-495c — DT-7 activation, and the AI-enum abstain reconciliation it forced

**Branch:** `phase3_bucket_2_fast`. **Baseline verified this session** (not taken from the
ticket): `ACTIVE_RULE_IDS` 70 · `rule_kinds.csv` 135 rows · vocabulary 243 declared tags /
143 CSV rows · tree clean at `4b69316`.

## What this ticket does

Activates DT-7, which LP-495b built, measured and then held. The hold had one cause, and it
was upstream of the rule: `dti.atr_factors_documented` was declared with no abstain value, so
DT-7's legitimate "I cannot determine ATR" answer was indistinguishable from a broken pipeline.

Fixing that meant editing the vocabulary xlsx, and auditing the xlsx surfaced a **live**
instance of the same defect in shipped code.

---

## Part 1 — the defect, stated mechanically

Three facts compose into the bug.

1. `_build_tag` (`tag_materialization/ai.py:364`) coerces any model value outside the tag's
   declared `allowed_values` to `_unknown_tag(...)`, which sets **`confidence=None`**.
   A genuine in-vocabulary `"unknown"` instead keeps the model's own confidence.

2. `_scan_tag_degradations` (`services/verification_run.py`) matches the fail-closed marker
   **structurally**: `value == "unknown"` and `produced_by == AI` and `confidence is None`.

3. `VerificationRun.degraded` is `bool(self.degradations)`.

So for any AI tag whose **prompt sanctions `"unknown"` but whose declaration omits it**, every
run in which the model honestly abstains is reported as a degraded run. Degradation is meant to
signal a broken pipeline — a transport outage or a code defect, logged at ERROR. A legitimate
abstain is neither.

The fix is one-word and upstream: the declaration must permit the value the prompt already asks
for. Then leg 1 stops firing, the tag keeps its real confidence, and the abstain flows through
as the honest judgment it is.

## Part 2 — the audit: which tags actually have this defect

The declaration alone is not the test. A tag whose enum lacks `"unknown"` is only defective if
its **producer prompt offers `"unknown"`** and its group is actually **materialized**. I audited
all 143 vocabulary rows against `tag_production.yaml` on both conditions rather than reading the
declarations alone.

Of 59 AI-produced enum tags, 15 lack an abstain value. Only **three** meet both conditions:

| Tag | Declared | Prompt offers | Group | Read by | Status |
|---|---|---|---|---|---|
| `dti.atr_factors_documented` | `complete\|incomplete` | `complete\|incomplete\|unknown` | `atr_documentation` | DT-7 | inert (DT-7 not yet active) |
| `txn.is_nsf_or_overdraft` | `yes\|no` | `yes\|no\|unknown` | `txn_nsf` | AS-7 | inert (AS-7 not active) |
| `liab.in_application` | `yes\|no` | `yes\|no\|unknown` | `credit_profile` | **CR-1, CR-4** | **LIVE — both rules active** |

The other 12 are not defects and are deliberately left alone:

- **Seven are not materialized at all** — `liab.account_type`, `liab.is_disputed`,
  `liab.excluded_paid_off`, `liab.has_open_judgment_lien`, `property.appraisal_complete`,
  `mi.certificate_present`, `aus.data_matches_docs`, `loe.present_and_complete`,
  `contract.unusual_credits` have no AI group, so no prompt can contradict the declaration.
  Three of these (`property.appraisal_complete`, `aus.data_matches_docs`,
  `contract.unusual_credits`) additionally carry a malformed declaration —
  `enum: yes | no + missing[]` parses to the literal value `"no + missing[]"`. Recorded, not
  fixed here: unmaterialized, so nothing reads them, and inventing a shape for them is a
  separate decision.
- **Five have prompts that do not offer an abstain**, so declaration and prompt agree:
  `stmt.is_reserve_eligible`, `income.type`, `liab.derogatory_type`, `income.voe_present`,
  `income.offer_letter_present`.

`income.type` deserves a specific note, because widening it would be the tempting mistake.
Its stub abstains to `"unknown"` and is deliberately coerced — `stubs.py:141` records that
IN-12's self-employment gate *relies* on that honest-unknown being fail-closed. Its prompt
offers no `"unknown"`, so there is no prompt/declaration contradiction to fix, and adding one
would change a live rule's gating behaviour on no evidence. Left as-is.

### The live one

`liab.in_application` is the find that justifies the audit. Its prompt does not merely permit
`"unknown"` — it *mandates* it:

> "unknown" is a correct, expected answer — NEVER guess.
> TRUNCATION DISCIPLINE: … If the stated liabilities are truncated … you MUST answer "unknown"
> — NEVER a confident "no" over an unseen row.

CR-1 and CR-4 are both in `ACTIVE_RULE_IDS`. So on any real file where the stated-liability
list is truncated, or where there are no stated liabilities to compare against, the model
follows its instructions, the value is coerced to null confidence, and the run is flagged
degraded. This is shipped behaviour today, and it was not logged anywhere before this audit.

This is also why the AS-7 precedent could not be relied on. LP-495b took `txn.is_nsf_or_overdraft`
as a written-down precedent for the same pattern without checking it had ever been exercised —
AS-7 is inert, so it never ran through the orchestrator. `liab.in_application` is the case that
*has* been running, and it confirms the defect empirically rather than by analogy.

## Part 3 — the change

`docs/snapshot-fact-tags.xlsx`, `Fact-Tag Vocabulary` sheet, column C, three cells:

| Tag | Before | After |
|---|---|---|
| `txn.is_nsf_or_overdraft` | `enum: yes \| no` | `enum: yes \| no \| unknown` |
| `liab.in_application` | `enum: yes \| no` | `enum: yes \| no \| unknown` |
| `dti.atr_factors_documented` | `enum: complete \| incomplete` | `enum: complete \| incomplete \| unknown` |

then `uv run python -m app.scripts.generate_fact_tags`, committing the regenerated
`fact_tags.csv` with the xlsx.

The xlsx is a binary a reviewer cannot diff, which is the one reason to hesitate. Three things
mitigate it: the edit is a surgical rewrite of three cells' inline text with every other byte
of the sheet XML preserved (verified by diffing the extracted XML before and after); the exact
before/after strings are tabulated above; and the *reviewable* artifact — the regenerated
`fact_tags.csv` — carries the whole semantic change in three one-word diffs.

Not done by hand-editing `fact_tags.csv`: the xlsx is upstream and the next regeneration would
silently revert it, with no test to catch the loss. Not done via `vocabulary_extra.yaml`:
`projection.py` raises `ProjectionError` on a tag duplicating a CSV tag, by design.

## Part 4 — DT-7 activation

---

## Part 5 — executed

**DT-7 activated. Four declarations reconciled.** `ACTIVE_RULE_IDS` 73 → **74**. **Model spend: $0** —
DT-7 activated on the rate LP-495b already measured; nothing was re-derived.

### The audit was re-run, and it found a fourth tag

Part 2 above listed **three** defects. That audit matched **line by line**, and
`stmt.is_reserve_eligible`'s prompt puts its sanctioned `"unknown"` on a *different line* from the tag
name, so it was a false negative. LP-497 found it by reading the prompt directly. Re-running the audit
with a **window** match rather than a line match confirms the complete set is **four**:

| tag | declared before | now | producer group | read by |
|---|---|---|---|---|
| `dti.atr_factors_documented` | `complete\|incomplete` | `+ unknown` | `atr_documentation` | DT-7 |
| `txn.is_nsf_or_overdraft` | `yes\|no` | `+ unknown` | `txn_nsf` | AS-7 (held) |
| `liab.in_application` | `yes\|no` | `+ unknown` | `credit_profile` | **CR-1, CR-4 (live)** |
| `stmt.is_reserve_eligible` | `yes\|no\|partial` | `+ unknown` | `stmt_facts` | (the fourth) |

The correction matters beyond the count: the original method could have missed others, and re-running
it properly is what makes "four is the complete set" a claim rather than a hope.

### The fix, and the proof it works

Four cells in `docs/snapshot-fact-tags.xlsx`, `fact_tags.csv` regenerated. The xlsx edit is a surgical
rewrite of four inline strings with every other byte of the sheet preserved (verified: `+40` bytes,
exactly the four insertions; all 13 other zip entries byte-identical). The reviewable artifact is the
CSV — four one-word diffs.

Proven at the mechanism rather than asserted. `_build_tag` on a model `"unknown"` with confidence 0.9:

| tag | before | after |
|---|---|---|
| the four reconciled | `confidence=None` → **degrades** | `confidence=0.9` → **does not** |
| `income.voe_present` (control) | `confidence=None` → degrades | `confidence=None` → **still degrades** |

The control is the important half: `income.voe_present`'s prompt genuinely offers no abstain, so
declaration and prompt agree and a model returning `"unknown"` there really *is* off-vocabulary. It
must keep failing closed, and it does — the reconciliation is targeted, not a blanket disabling of the
fail-closed mechanism. A test pins that.

End to end: a full stubbed run now carries **no tag-level degradation from any of the four**. The 12
that remain are all `income.type`, which is the deliberate case — its prompt offers no abstain and
IN-12's self-employment gate relies on that honest-unknown being fail-closed.

### DT-7

Activated on LP-495b's measurement, used unchanged: **1.0000 over 4 constructed cases**, full
three-value spread (`complete` / `incomplete` / `unknown`), 0 disagreements,
`us.anthropic.claude-sonnet-4-5-20250929-v1:0`, 2026-08-13. `status: ratify-pending`,
`measured_accuracy: null` — the rate is self-consistency, not accuracy against labels, and the status
is what keeps those distinct. `ratification_pending=True` proven through a **real rule evaluation**.

### Two tests that had to change, and one that ran out of subject

- **`test_dt7_is_built_but_held_on_its_enum_gap`** was written by LP-495b to fail the moment the enum
  gained a value, with the note *"if it now has an abstain, DT-7 can activate on the rate already
  recorded on its bar"*. It did its job. **Inverted rather than deleted**, so the transition stays
  visible in history.
- **`test_owner_matches_and_reserve_are_semantically_unchanged`** (LP-400) asserted the enums were
  untouched. The widening is purely **additive** — no existing value's meaning changed — so every
  yes/no/partial golden remains exactly as valid. Updated with that reasoning at the site.
- **`test_a_blocked_JUDGMENT_rule_surfaces_through_the_stub`** lost its subject: the demo rule moved
  IN-7 → IN-13 → DT-7 as each went live, and **DT-7 was the last blocked judgment rule**. There is now
  none. The test asserts that fact (so the comment cannot go stale silently) and exercises the real
  path by simulating DT-7's prior blocked state — the machinery under test is entirely real, only the
  "which rules are blocked" input is varied.

**The dormant-probe interaction LP-495b could not diagnose does not reproduce.** Its registry note
worried that `test_dormant_probe`'s set "still reports them dormant" when the rules are active.
Measured directly: `atr_documentation` is now in `required_ai_groups()` and out of
`dormant_ai_groups()` — the complement holds exactly, and both sides of the test were updated.

## Phase E — regression

`git stash -u` on a clean tree, md5 comparison:

| artifact | before → after | |
|---|---|---|
| RAW LF-6T3N snapshot | `f105b89f…` → `f105b89f…` | identical |
| legacy attrs AS-1 / AS-2 / AS-12 / IN-12 ride | `ab800266…` → `ab800266…` | identical |
| materialized (subject, tag) pairs | 741 → 741 | 0 added, 0 removed |

**The tag SET is unchanged, but three stub VALUES did change**, and that is the change worth checking
rather than the count. `stubs.py` picks its abstain from `allowed_values`, so `txn.is_nsf_or_overdraft`,
`liab.in_application` and `stmt.is_reserve_eligible` flipped from stubbing a substantive `"no"` to
abstaining honestly as `"unknown"`. **No downstream verdict moved**: LF-6T3N's distribution went
844 → 845 with the single new row being DT-7, and `satisfied` / `fired` / `needs_review` unchanged at
**21 / 2 / 4**. CR-1 and CR-4 read `liab.in_application` and neither verdict changed.

`stubs.py`'s own docstring named `stmt.is_reserve_eligible` as a case needing the substantive `"no"`
workaround. That is now false, and the docstring was corrected: `income.voe_present` is the only
remaining case, and it is legitimate.

## Equivalence

| | value |
|---|---|
| `ACTIVE_RULE_IDS` | 73 → **74** |
| `SNAPSHOT_VERSION` | **4**, unchanged |
| `rule_kinds.csv` | **135**, unchanged |
| vocabulary | **244** tags, unchanged (values widened, no tag added) |
| suite | **4764 passed**, 5 skipped, 1 xfailed |
| ruff / mypy | clean |

Baseline measured on a clean stash of this tip: **4758**, + 6 new tests = 4764, fully accounted.

## What this leaves

- **`liab.in_application` is fixed**, which closes the live defect found at the start of this session:
  CR-1 and CR-4 no longer flag a run degraded when the model follows its prompt's instruction to
  answer `"unknown"` on a truncated stated-liability list.
- **AS-7's enum blocker is gone.** It remains held on its *other* reason — no real NSF event exists in
  2,557 raw PDFs (LP-497) — and `test_as7_is_built_but_held` still guards it.
- **There is no blocked judgment rule left.** Every judgmental rule is now live.
- Unchanged: `IN-9.yaml`'s stale B3-3.1-09 (five tickets), `validation_aid.py:54`'s `806500`,
  TI-6 tier U, LL-2026-03 tier S.
