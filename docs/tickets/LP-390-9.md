# LP-390-9 — Activate IN-3 (Priya signed off its bar)

## What this ticket is

Priya validated IN-3's bar (0.98 auto). IN-3 is an AI rule (LP-384 reclassification): its derived
YTD-annualized shortfall reads `income.documented_monthly` — the **same tag + evidence as IN-1**, measured 100%
(LP-379-D). This activates IN-3 through the eligibility gate. **IN-3 only, `ACTIVE_RULE_IDS` 19 → 20.**
  
## Acceptance criteria (met)

- IN-3 goes live **via the gate** (`validated:true` + `registry._LP390_9_ACTIVATED`), not a blind edit.
- No other rule newly-activates.
- The real run reports IN-3's ACTUAL verdict on LF-6T3N, with the couldnt_check root named.
- `ACTIVE_RULE_IDS` 19 → 20; the 19 previously-live rules unchanged; full suite green.

## Phase 0 — eligibility + the flag is clean

| check | result |
|-------|--------|
| IN-3 `status` | `calibratable-now` ✓ |
| IN-3 `threshold` | **0.98 — a REAL threshold**, not null (not the AS-5 stray-flag class) ✓ |
| IN-3 `validated` | `true` (Priya's sign-off) ✓ |
| IN-3 `measured_accuracy` | 1.0 ≥ 0.98 → `is_eligible` **True** ✓ |
| IN-3 `ships` | `auto` (read from the yaml) ✓ |
| Loader accepts it? | **Yes** — a validated bar on a calibratable-now rule WITH a real threshold is legal; the LP-390-7 reject-rule fires only on a *not-calibratable/null-threshold* stray flag (AS-5), not this ✓ |
| Only IN-3 newly-eligible? | **Yes** — `eligible_rule_ids()` gains exactly IN-3 (9 total). AS-2/AS-12 already live (LP-390-7); AS-5 correctly false; all others held ✓ |
| AI-dependency honored? | **Yes** — IN-3 declares `income.documented_monthly` load-bearing, so `income_amounts` folds into `_required_ai_groups` (already required via live IN-1). Verified: the tag is **produced** on the fixture (12 subjects, real values), not just declared ✓ |

## Phase 1 — activate IN-3

- **`registry.py`:** new `_LP390_9_ACTIVATED = ("IN-3",)`, folded into `ACTIVE_RULE_IDS`. IN-3 ships auto.
- **`activation_bars.yaml`:** IN-3's stale "HELD by validated:false" comment updated to the sign-off + ACTIVE;
  the header "honest state" updated (all 5 calibratable rules now signed off). No bar VALUE changed (Priya's).
- Touched no other rule.

## Phase 2 — the real run on LF-6T3N (reported, not predicted)

Materialized `income_amounts` (real AI): **`income.documented_monthly` produced on 12 subjects** with real
values (6500, 4550, 6375, …). Then evaluated IN-3 (and IN-1 as the reference — same tag):

- **IN-3 → couldnt_check** — *"the YTD-annualized income shortfall could not be read … present but unclear."*
- **IN-1 → couldnt_check** — the identical shape (*"documented-vs-stated income shortfall … present but
  unclear"*).

**The root (named, per LP-370's warning — not called "correct" without evidence):** this is the **HONEST
abstain**, NOT a tag-production bug. The distinguishing evidence:
- `income.documented_monthly` (IN-3's declared AI tag) **IS produced** (12 subjects) — the fold-in works, so
  it is *not* a couldnt_check-because-the-tag-didn't-produce failure.
- The couldnt_check is on the **derived** `ytd_annualized_shortfall_pct`, which resolves *unknown* because its
  comparison input (parsed YTD gross / stated income for the annualized comparison) is not resolvable on
  LF-6T3N.
- **Live IN-1 does exactly the same thing** on this file (no stated income to compare). IN-3 matching its twin
  IN-1 is the correct, expected outcome — a file carrying stated income + YTD gross would drive it to
  satisfied/open.

**Active count 19 → 20** (confirmed). No ADR needed — the abstain is the honest no-comparison-input case, not
the tag-production trap LP-384's reclassification guarded against (that trap would show `documented_monthly`
absent; here it is present).

## Phase 3 — tests + equivalence

- **The gate:** IN-3 passes; no other rule newly-activates; `eligible_rule_ids()` gains exactly IN-3.
- **The loader accepts IN-3's validated bar** (real threshold + calibratable-now) — asserted as the legal
  counter-case to the AS-5 stray-flag rejection.
- **IN-3 ships auto** (asserted — matches the yaml).
- **IN-3's AI tag is produced when IN-3 runs** (the fold-in) — the Phase-2 run + the LP-389
  materialization guard (`test_active_rules_upstream_ai_groups_are_actually_materialized`) cover it; IN-3 does
  not couldnt_check for a missing-tag reason (distinct from the honest no-stated-income abstain, pinned in
  `test_lp384_stuck_rules`).
- Added a curated reason label for `income.ytd_annualized_shortfall_pct` (IN-3's gated tag surfaces it in a
  couldnt_check reason — the LP-376-C human-reasons guard requires it).
- **EQUIVALENCE:** the 19 previously-live rules identical; only IN-3 added; the activation-set/count guards
  across the suite updated 19 → 20; full suite green (ruff + mypy clean).

## Assumptions, decisions & gaps

- **No ADR** — a single activation, and the couldnt_check is the honest no-input abstain (not the
  tag-production reason that would warrant a finding). Recorded here instead.
- **Gate of record:** IN-3's flag was already `validated: true` and clean (real threshold) — confirmed, not
  patched. LP-384's reclassification (IN-3 is AI, not no-ai) is exactly what let the fold-in work.
- **What the next activation needs:** the LP-390-8 producer fixes (AS-6's borrower-name context, the
  IN-12/AS-5 producers), the AS-4 reserve-eligible domain definition, and AS-5's ADR-302 design call. With
  IN-3 live, **all 5 calibratable-now rules are now activated** — the remaining candidates are blocked on
  calibration / a producer / a design question, not on a sign-off.
