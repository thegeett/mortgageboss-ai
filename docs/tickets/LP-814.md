# LP-814 — Reminder suggestions, and the clock each rule reads

- **Ticket:** LP-814 · Phase 4, M5, ticket 23 of 28
- **Status:** Built — awaiting review
- **Date:** 2026-09-09
- **Scope:** `services/reminders.py` (new), `models/reminder_snooze.py` (new),
  `api/reminders.py` (new), the Reminders page and suggestion cards
- **Migration:** `e2a9c4f18b63`

---

## The clock is `requested_at`, and the trap has two mouths

§1.4: *"A reminder keyed on `created_at` would nag about documents nobody has asked for yet."*
`request_needs_item` had existed since LP-19 with zero callers until LP-811a, so `requested_at` was
NULL on every row that had ever existed — every rule here would have been a query over an empty set.

And LP-819 **unwinds** `requested_at` on a hard bounce: the borrower never received the request, the
need goes back to `PENDING`, the stamp is cleared. That is correct, and it means a bounced request
correctly has **no clock**.

So the trap runs both ways. A rule reading `created_at` nags about a need nobody asked for; a rule
*falling back* to `created_at` when the stamp is NULL — which looks like tolerance — nags **hardest
about exactly the requests that bounced**, against a mailbox that does not exist, and every nudge
bounces too.

### The two tests named after the trap were passing on something else

`test_a_need_nobody_has_asked_for_is_not_suggested` and `test_a_bounced_request_produces_no_clock`
both build needs at status `PENDING` — which the status filter excludes anyway. Measured: swapping
`requested_at` for `created_at` in the query failed **one** test, and neither of those two.

`test_the_stamp_decides_and_not_the_creation_date` is the case only the clock can decide —
`REQUESTED`, created a month ago, requested yesterday. It was missing, and it is what actually pins
the claim the module is named after.

The `requested_at IS NOT NULL` clause turns out to be **redundant and is kept anyway**: SQL's
three-valued logic already makes `NULL < timestamp` unknown, so removing it changes no test. The
comment now says that rather than letting it read as load-bearing.

---

## Why there is no Celery beat

The plan says "Celery beat over `requested_at`". A beat that computes suggestions and writes them
changes nothing a read cannot do — it **suggests only**, so it has no side effect worth scheduling —
and it would be a table going stale the moment a document arrives, nudging about something that
turned up an hour ago.

What a beat *would* serve is reaching a processor who is not looking, and that is a notification
channel this product does not have. **Escalated** rather than built as a task nothing drains — which
is the shape this loop has now found five times.

The cross-file read is what makes rule 3 possible at all: *"this file has gone quiet"* is by
definition about a file nobody is opening, so a per-file endpoint could never surface it. A processor
would have to visit the file to be told they have not visited the file.

---

## What is stored, and what is not

**Suggestions are computed on every read.** The rules are a function of `requested_at`, the needs
list and the activity log; materialising them means a second copy that goes stale, and a processor
nudged about something already resolved is how a list teaches its reader to ignore it.

**What is stored is the decision** — the only fact a query cannot derive, because a person chose it.
Snooze and dismiss share one row: *"come back on Thursday"* and *"stop telling me"* are the same
decision at different distances, and two tables would make "is this suppressed?" two questions, the
second of which is the one somebody forgets.

The unique index carries **`NULLS NOT DISTINCT`**, because `subject_id` is NULL for a rule about the
file itself. Without it every "file untouched" snooze is a fresh row and none of them suppresses
anything — Postgres's default biting in the direction that makes a feature silently do nothing.
Tested by snoozing twice.

---

## The three rules, and the judgement in each

- **Pending > 3 days.** `status == REQUESTED` as well as the stamp: a need satisfied by a document
  arriving another way keeps its `requested_at` and is not waiting for anything.
- **No reply > 5 days.** *One suggestion per file, not per message* — a processor who sent three
  requests and heard nothing has one problem, and three cards for it is a list they stop reading.
  And "nothing since" means **nothing inbound after the send**, not "no inbound at all": a borrower
  who replied last month and has gone quiet since Tuesday is exactly what this is for.
- **File untouched > 7 days.** The **activity log** is the clock, not `loan_files.updated_at` —
  that column moves when a background job writes a status, so a file nobody has touched in a
  fortnight can look fresh. A file with no activity at all falls back to its creation, because it is
  genuinely untouched and skipping it would hide the newest files, which are the likeliest to have
  been forgotten.

Submitted and withdrawn files are never nagged about: a finished file is not a forgotten one, and
nudging about it teaches a processor to ignore the list.

**Days, not business days.** A borrower's weekend is not a working day for them either — the clock
is about how long somebody has been waiting — and a business-day count needs a per-jurisdiction
holiday calendar for a threshold nobody has calibrated.

---

## Tests

`tests/services/test_reminders_lp814.py` (25),
`components/reminders/suggestion-cards.test.tsx` (9).

Verified by mutation, each confirmed applied:

| Mutation | Result |
|---|---|
| the clock swapped to `created_at` | 2 fail |
| a satisfied need still nagged about | 1 fails |
| any inbound reply clears rule 2 | 1 fails |
| submitted files nagged about | 1 fails |
| a named file not re-scoped to the company | 1 fails |
| a snooze never expires | 1 fails |
| re-snoozing after an unsnooze does nothing | 1 fails |
| newest problem sorted first | 1 fails |
| dismiss sends a date instead of null | 1 fails |
| the card sends the wrong subject | 2 fail |
| one icon for all three rules | 1 fails |

**One mutant survived and the test it exposed was overclaiming.** Colliding React keys do not drop a
card — React warns and renders both — so `test_renders_one_card_per_suggestion` passes with a key of
just the file id. The comment claimed otherwise; it now says what a collision actually costs
(reconciliation reusing state between two cards) and states that this test does not reach it.

---

## Deliberately not done

- **No Celery beat.** See above. **Escalated.**
- **No cross-file snooze.** A snooze is per (file, kind, subject). "Stop telling me about quiet
  files" as a company-wide preference is a different feature and would hide rule 3 entirely, which
  is the rule most worth keeping.
- **Thresholds are not configurable.** 3, 5 and 7 come from spec 4.5. Named constants rather than
  settings, because a per-company threshold is a decision nobody has made and a settings row nobody
  reads is worse than a constant somebody can find.
- **A suggestion offers no "send" action.** Spec 4.5 says suggests only. The card's actions are
  "put it off" and "stop telling me"; a test asserts nothing on it reads as having sent.

## For the reviewer

- **Tenancy** — every rule is company-scoped, and a named file is re-checked against the caller's
  company even though a scoped route resolved it. Snoozes are written through `ScopedLoanFile`.
  Tested both directions, including same-company-two-files for the file-level path.
- **Message content in logs** — this module logs nothing. A suggestion's summary is built from the
  need's **title**, which comes from a template or a processor, never from a borrower's message.
- **AI in the decision path** — none. Nothing here proposes what to say.

Worth a second measurement:

1. **The "one suggestion per file" collapse in rule 2.** It picks the newest qualifying send and
   asks whether anything inbound followed it. Please check the case of a send that bounced —
   `FAILED` is excluded from the query, so a bounced send does not produce a "no reply" card, which
   I believe is right and is the kind of thing worth disagreeing with.
2. **`_untouched` falls back to `created_at` for a file with no activity.** That is the opposite
   fallback to the one rule 1 refuses, and for a different reason — worth checking I have not
   reasoned myself into an inconsistency.
3. **The absence of a beat.** If you think the plan's "Celery beat" is load-bearing rather than
   descriptive, that is the finding.

---

## Review (second session, protocol §3)

One finding. The three rules are right, and the builder's own catch — two tests named after the trap
passing on the status filter instead — is the more instructive half of the ticket.

### A — A dismissal silenced a request it was never about

`until = None` is a documented dismissal: *"stop telling me"*, and it never expires. Suppression was
keyed on `(loan_file_id, kind, subject_id)` alone, so the decision attached to the **subject** rather
than to the **event**. Measured:

```
PROBE before dismissal: 1
PROBE after dismissal:  0
PROBE after a NEW request goes unanswered: 0
```

The sequence is ordinary and this loop built every step of it. A processor dismisses "you asked ten
days ago". The send hard-bounces, so LP-819 unwinds the request — need back to `PENDING`,
`requested_at` cleared, *"a request that bounced was never made"*. The processor confirms the
address and sends again. Four days pass with no reply. **The engine says nothing**, because of a
decision about a request that no longer exists.

Fixed without a column: `Suggestion.since` is already what each rule fired from — `requested_at` for
rule 1, the send for rule 2, the last activity for rule 3 — so a decision is stale exactly when its
subject's event has moved past it. A snooze suppresses only while `created_at >= since`. A dismissal
still means "stop telling me about **this**", and a genuinely new request is not that.

Three tests, verified in both directions: suppressing on the key alone fails the staleness test, and
never suppressing fails both controls — the one where a dismissal must still hold for the request it
was about, and the one where a timed snooze must still expire on its own terms rather than being
replaced by the new comparison.

**My first version of that test failed against correct code**, and the reason is worth recording:
it created the dismissal at `now` and then backdated the "new" request to four days ago, so the
request predated the decision — a sequence that cannot happen. The fixture, not the fix, was wrong.
That is the same shape as the builder's own finding below, arriving from the other side: a test
whose scenario is not the scenario it names.

### The builder's own catch, which is the ticket's best moment

`test_a_need_nobody_has_asked_for_is_not_suggested` and `test_a_bounced_request_produces_no_clock`
were both named after the exact trap I flagged when handing over LP-812 — and both built their needs
at `PENDING`, which the status filter excludes before the clock is consulted. Swapping `requested_at`
for `created_at` failed **one** test and neither of those two.

So two tests named after a property were passing on a different one, and the case only the clock can
decide — `REQUESTED`, created a month ago, requested yesterday — did not exist until the mutation
asked for it. Finding that before review, and saying it in those terms, is the discipline working.

The `requested_at IS NOT NULL` clause being redundant under three-valued logic is right, and saying
so in the comment rather than letting it read as load-bearing is the right call: a reader who
removes it should find out from the comment that nothing changes, not from a test that does not fail.

### The builder's three, measured

1. **Rule 2 excluding `FAILED` sends — right, and I would not change it.** A bounced send producing
   a "no reply" card would tell a processor to chase a message that never arrived, and LP-819
   already surfaces the bounce with the address suppressed and an actionable reason. Two cards for
   one fact, one of them wrong about what happened.
2. **`_untouched` falling back to `created_at` — not an inconsistency, and the distinction is
   exact.** A file with no activity genuinely *has* been untouched since it was created, so
   `created_at` is the true answer. A need with no `requested_at` genuinely *was not asked for*, so
   `created_at` would be a fabricated one. Same fallback, opposite truth value — which is why one is
   correct and the other is the trap.
3. **`NULLS NOT DISTINCT` on the snooze index — necessary, and tested the right way.** `subject_id`
   is NULL for the file-level rule, and without it every "file untouched" snooze is a fresh row that
   suppresses nothing. That is LP-803's default biting in the direction where a feature silently
   does nothing, and snoozing twice is the test that catches it.

### The Celery beat — descriptive, not load-bearing

The builder asked directly, so: **not building it is right**, and this is a ruling rather than an
acceptance.

`phase4.md` line 833 says "Celery beat over `requested_at` / needs status" and, in the same entry,
"**Suggests only; never sends**". Those two together decide it. A beat exists to make something
happen when nobody is looking. A computation with no side effect, whose output is read on demand and
goes stale the moment a document arrives, has nothing for a schedule to add — and a materialised
suggestion list would be *worse* than the read, because it can be wrong between runs.

What a beat would genuinely serve is reaching a processor who is not looking at the screen, and that
needs a notification channel this product does not have. Building the beat without one produces a
task nothing drains, which is the fifth-form pattern this loop has now found five times and the
builder correctly declined to make it six.

### Done when, and the standing checks

The three rules fire on the right thresholds, suggest only, and never send. The React-key correction
is worth noting: colliding keys do not drop a card, React warns and renders both, so a test claiming
otherwise passes either way — the comment now says what a collision actually costs and that the test
does not reach it.

Standing checks: suggestions are built from `company_id` on the authenticated user's session and
snoozes are scoped through the file; nothing logs a subject, body or address; no AI.
