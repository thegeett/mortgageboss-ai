# LP-454 — Step D.3: the parsed-tag dividend (declare only what is populated + clean)

## What this ticket is

LP-451's largest cheap tier: ~16 rules whose fields "now EXIST" (the LP-446 dividend) needing only a tag
declaration. But the last two D-steps under-delivered (LP-452's `property.type` is null; LP-453's tradelines
were open-ended). **So this ticket MEASURED the real stored values first and declared only what is clean and
populated.** The honest result: **2 of ~16.** No model calls (read stored extractions only; ~$0.95 credit).

## Acceptance criteria (status)

| criterion | status |
|---|---|
| Phase A: per-candidate real values, clean-vs-open, populated-vs-null | ✅ |
| The four-way verdict | ✅ |
| Declare ONLY the populated + clean set | ✅ (2 tags) |
| Every declaration passes the LP-450 guard | ✅ |
| No rule written, no value normalised, no threshold, no existing tag touched | ✅ |
| Proof: each tag's actual value per file | ✅ |
| The honest count (how many actually unblocked) | ✅ **0 finished; 2 declared as scaffolding** |
| ADR on the third instance | ✅ ADR-354 |
| Equivalence: ACTIVE=37, suite green, ruff+mypy clean, no model calls | ✅ |

## PHASE A — the measurement (from STORED data, no model calls)

Two structural walls blocked verification of the ~16:

**Wall 1 — the candidate documents don't exist on the three files.** 10 of 16 rules need `appraisal`,
`title_commitment`, `aus_findings`, `condo_questionnaire`, or `master_insurance_policy` — NONE present on
LF-96SV / LF-6T3N / LF-XU26. So PR-2/4/5/6, TI-3/4/5, AU-3, CO-1, IH-7 have no data to measure.

**Wall 2 — the LP-446 fields are absent from the pre-LP-446 stored extractions** (re-extraction is
budget-forbidden). On documents that DO exist:
- `homeowners_insurance` (LF-96SV ×3): `mortgagee_name`, `wind_hail_deductible` → **ABSENT** (IH-2, IH-8).
- `purchase_agreement` (LF-6T3N): `seller_credit_amount`, `personal_property_*` → **ABSENT** (PC-4, PC-8).

### The only populated + clean fields in stored data — 2 of 16

| rule | field | stored value | type | clean? |
|---|---|---|---|---|
| **CR-13** | `credit_report.report_date` | **`2026-07-17`** (LF-96SV) | date | ✅ |
| **PC-5** | `purchase_agreement.earnest_money_amount` | **`5000.0`** (LF-6T3N) | Decimal | ✅ |

Both are **pre-LP-446 original fields** — which is why they survive in stored data.

### The four-way verdict

- **✅ DECLARE NOW (populated, clean, the rule's LAST blocker): 0.** Neither finishes its rule.
- **⏳ TAG ALONE NOT ENOUGH (populated, clean, declarable — rule still blocked): 2** — `credit.report_date`
  (CR-13 also needs a Priya validity window + a derived date-vs-today) and `contract.emd_amount` (PC-5 also
  needs `contract.emd_sourced`, an AI txn-match). **Declared** — real, guarded scaffolding.
- **⚠️ DECLARE-BUT-INERT (clean type, null on every file — the property.type case): 4** — `contract.seller_credit`,
  `contract.personal_property`, `property.appraised_value`, `property.appraisal_date`. **NOT declared** (Geet:
  "a legal tag null on every file looks like progress and isn't" — the property.type lesson, one ticket old).
- **❌ NOT SAFE TO DECLARE — str fields, values unmeasurable (the open-vocab trap): ~10** — `condition_rating`,
  `appraisal_completion_condition`, `repairs_required_indicator`, `fha_condition_deficiencies`,
  `open_liens_indicator`, `judgments_indicator`, `vesting_type`, `vesting_marital_recital`, `recommendation`,
  `mortgagee_name`, `wind_hail_*`, `personal_property_included`. All `str`, all null in stored data → the D3 test
  cannot be run. Declaring blind is the `is_disputed`-forbearance / earnings-four-bucket trap. **NOT declared.**

## PHASE B — what was declared (2 parsed document tags; both already in the vocabulary)

Both were vocabulary orphans in `fact_tags.csv` (declared, no producer) — this gives each a producer, so no
`vocabulary_extra` add and **no tag-count change**:

```yaml
contract.emd_amount: {mode: parsed, subject: document, data: earnest_money_amount}   # PC-5 / AS-2
credit.report_date:  {mode: parsed, subject: document, data: report_date}            # CR-13
```

No value normalised, no boolean derived, no threshold, no rule written, no existing tag touched.

## PHASE C — THE PROOF (reported, not predicted)

Materialised on the three real snapshots (parsed + derived, **no AI**):

| file | credit.report_date | contract.emd_amount |
|---|---|---|
| **LF-96SV** | **2026-07-17** (the credit report) | absent (no purchase agreement) |
| **LF-6T3N** | absent (no credit report) | **5000.0** (the purchase agreement) |
| LF-XU26 | absent | absent |

Both LP-450 guards pass (`report_date`, `earnest_money_amount` are real extraction fields). Both fail closed on
an absent field (asserted).

**Equivalence:** the only changes are two parsed producers for tags that were unproduced orphans read by no
live rule — so no existing tag's value changes, `ACTIVE_RULE_IDS` = 37, and the 37 live verdicts are unchanged
(verified via the full suite over the stored fixtures — no live run). Full suite green; ruff + `mypy app/`
clean; tag-count guards unchanged (both tags already in the vocabulary).

## THE HONEST COUNT

The audit projected ~16 parsed-tag unblocks. On the current corpus and budget the real number is **2 declared,
0 finished** — both leave their rule blocked on Priya/AI. The rest is a data/corpus problem (below), not a
coding one.

## What step D is now BLOCKED on (recorded here, not only in the conversation) — ADR-354

Two blockers, **neither fixable by another ticket**:
- **API credit.** Stored extractions predate LP-446, so its 232 new fields are not in stored data and can't be
  measured without RE-EXTRACTION (model calls the ~$0.95 budget forbids). Every rule whose input is an LP-446
  field is unverifiable until the corpus is re-extracted.
- **Documents.** No `appraisal`, `title_commitment`, `aus_findings`, `condo_questionnaire`, or
  `master_insurance_policy` document exists on any of the three files. ~10 rules wait on a corpus with them.

**Step D pauses here** until the corpus grows or re-extraction is funded.

## Assumptions / decisions

- Declared exactly the 2 populated + clean fields; recorded plainly that neither finishes its rule.
- Did NOT declare the 4 clean-typed-but-null fields (the property.type lesson) or the ~10 str fields (the
  open-vocab trap) — measuring their values needs re-extraction or files that carry those documents.
- Both tags were already in `fact_tags.csv`; only production declarations were added (no vocab/count change).

## NEW gaps / what remains (per LP-451)

- **CR-13** still needs a Priya validity window + a derived `closing − report_date` recipe.
- **PC-5** still needs `contract.emd_sourced` (an AI/derived txn-match, tying AS-2).
- **The other 14 candidates** are blocked on the corpus/budget (ADR-354), not on a declaration.

## ADR

**ADR-354** — the audit measured the field SURFACE, not the field DATA; three D-steps under-delivered for that
one reason; the standing rule is: verify a field is populated with clean values on real data before declaring a
tag on it. Cross-references LP-452 (null data), LP-453/ADR-353 (open vocabulary), LP-448 (the earnings lesson).
