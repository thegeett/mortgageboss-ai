# LP-811b — Send (frontend): the draft panel

- **Ticket:** LP-811b · Phase 4, M1, ticket 8b of 27 — **split from LP-811**
- **Status:** Built — awaiting review
- **Date:** 2026-09-07
- **Plan:** [`phase4-build-plan.md`](../phases/phase4-build-plan.md) LP-811 section
- **Scope:** `lib/types/communication.ts` (new), `lib/api/communications.ts` (new),
  `components/file/communication/outbound-draft-panel.tsx` (new),
  `app/(protected)/loan-files/[id]/communication/page.tsx`
- **Migration:** none. Frontend only.

**This completes M1.**

---

## The navigation question answered itself

I split this off LP-811 partly because the panel "needs a home in the file view, which is a
navigation decision". It already has one. `app/(protected)/loan-files/[id]/communication/` has
existed since LP-33 as a `TabPlaceholder` reading *"Coming in Phase 4"*, and `lib/navigation.ts:97`
already links to it.

So there was no decision to escalate — I checked before assuming, and the answer was in the repo.
The placeholder is replaced; the route, the nav entry and the icon are unchanged.

---

## What was done

The Communication tab now shows the accumulating document request: who it goes to, the subject, the
editable body, the Reply-To and Bcc addresses, and three actions — **copy**, **open in mail client**,
**mark as sent**.

Nothing transmits. The processor takes the text to their own client; "mark as sent" records that it
went out, which is what moves every requested document to `REQUESTED` and starts LP-814's clock.

### `mailto:` is withheld with a reason, not just disabled

The server decides whether a message fits (`mailto_available`), and `mailtoUrl` returns `null`
rather than a truncated link. A `mailto:` over the client's limit **does not fail** — it opens a
compose window containing part of the message, which a processor may send without noticing.

A disabled button with no explanation gets worked around. The panel says why, and names the limit,
because a processor who is not told will assume the feature is broken and paste the message anyway —
which is the same outcome by a different route.

### The textarea is seeded by draft identity, not by content

LP-809 **regenerates** the draft on every add and every remove, so the query returns a new object
often. Re-seeding the textarea from it would throw away a processor's edits mid-sentence.

Seeding is keyed on the draft's `id`, tracked in explicit state rather than through an effect
dependency list. The obvious version — a `useEffect` keyed on `[draftId]` that reads `draftBody` —
requires lying to the linter about its dependencies, and **a lint suppression is something somebody
later "fixes"**. Here the fix would be the data loss. Render-phase state comparison says the same
thing without a suppression to remove.

### The empty state is ordinary, not an error

A file with nothing outstanding has no draft. The API client turns that 404 into `null` rather than
throwing, and the panel says so — an empty textarea with a Send button under it would read as a
broken page on a file in good order.

---

## Tests

`components/file/communication/outbound-draft-panel.test.tsx`, 10 tests.

`mailtoUrl` is deliberately **not** mocked. It is the function under test in two cases, and mocking
it would leave them asserting that a mock returns what it was told to.

Two are about work being lost, which nothing reports:

- `does not overwrite an edit when the same draft re-renders` — the regeneration case.
- `does seed from a different draft` — its control. A component that never seeded would satisfy the
  first and would show an empty box for every draft.

Verified by mutation, each confirmed applied:

| Mutation | Result |
|---|---|
| the `mailto_available` gate removed | 1 fails |
| seeding keyed on presence rather than identity | 9 fail |
| the composed body sent instead of the edited one | 1 fails |
| the send button enabled without a recipient | 1 fails |

---

## Assumptions

- **The recipient is typed each time.** There is no borrower-email lookup: `Borrower` has the field,
  but choosing *which* borrower on a multi-borrower file is a real decision nobody has made, and
  pre-filling the wrong one is worse than typing it. Recorded rather than guessed.
- **`navigator.clipboard` is available.** It requires a secure context, which the app is. A failure
  rejects the promise and the button simply does not flip to "Copied".

---

## Deliberately not done

- **No timeline.** LP-812 merges communications with the activity log; this tab shows the outbound
  draft only, and the placeholder's own description promised the rest of it.
- **No draft editing of the document list.** Removing a document is a needs-list action (LP-809's
  `remove_need_from_draft`), and duplicating it here would be a second way to do one thing.
- **No optimistic update on send.** The mutation invalidates and refetches. A send that is refused —
  already sent, or rate-limited — must not have looked like it worked first.

---

## For the reviewer

The three standing reviewer checks:

- **Tenancy** — no `company_id` anywhere; the client calls file-scoped routes and the server gates
  them.
- **Message content in logs** — nothing is logged. The body is rendered and copied, never sent
  anywhere but the API.
- **AI in the decision path** — none.

The judgement calls worth a second opinion:

1. **Render-phase `setState` instead of a `useEffect` with a suppressed lint rule.** It is the
   pattern React documents for deriving state from props, but it is unusual in this codebase — check
   whether it is the right local convention, and if not, whether the alternative is genuinely safe
   from the "someone fixes the suppression" failure.
2. **No borrower-email lookup.** Typing an address every time is friction on the most common action
   in the tab. The counter is that pre-filling the wrong borrower on a multi-borrower file sends a
   real person somebody else's document list.
3. **The panel is the whole tab.** LP-812 will want the timeline above or beside it, and I have not
   left a layout seam for that. Cheaper to shape now if you can see what it should be.

---

## Review (second session, protocol §3)

One finding, and it is the kind a browser hides: nothing throws, and the record disagrees with what
the borrower received.

### A — "Open in mail client" carried the composed body, not the processor's edit

Three controls sit side by side and two of them used the edited text:

| Control | What it used |
|---|---|
| Copy message | `body` — the edit |
| Mark as sent | `body` — the edit |
| **Open in mail client** | **`draft.body` — the composed draft** |

`mailtoUrl(draft, recipient)` read the body off the draft. Measured before the fix, with an edit
typed into the textarea:

```
PROBE mailto contains COMPOSED BODY: true
PROBE mailto contains the EDIT      : false
```

So a processor edits the message, opens their mail client, and sends the version they just rewrote
away from. Then they click "Mark as sent", which records **their edit** — and the stored
communication now says something the borrower never read. LP-821 builds the evidence record on that
column. A lost edit is a nuisance; a record that disagrees with what was sent is the one failure an
evidence record cannot survive, and it would have looked correct from every side.

`mailtoUrl` now takes the body as a parameter, and the panel passes the textarea's contents.

**The length gate had the same root and is fixed with it.** `draft.mailto_available` is the server's
verdict on the *composed* body (LP-811a's `build_outbound`), so an edit that ran past the limit was
still offered a link that truncates silently. Both gates now apply: the server's verdict, which may
know things this side does not, and a re-measure of the text actually being sent. Either saying no
is a no — strictly more conservative than either alone, and it keeps the pre-existing
too-long test meaningful.

Three tests, verified in both directions: reverting the body source fails the edit test and the
control; removing the re-measure fails the length test. The control exists because a `mailtoUrl`
that always returned `null` would have satisfied the first two.

One note on my own error while writing those tests: the first version asserted on
`decodeURIComponent(link.href)` and failed against correct code, because `URLSearchParams` writes
spaces as `+` and `decodeURIComponent` does not undo that. A test failing for a reason unrelated to
the behaviour is the same trap as one passing for an unrelated reason, and it is easier to mistake
for a real finding.

### The builder's three, ruled on

1. **Render-phase state instead of `useEffect` — correct, and it matches local convention.** I
   checked rather than judged: `components/file/verification/verification-panel.tsx:150` already
   uses the same "value from a previous render" pattern with its own explanatory comment, and
   `rg` finds **zero** `biome-ignore useExhaustiveDependencies` suppressions anywhere in
   `components/`. So the codebase's existing answer to "derive state from props" is this one, and a
   lying dependency list would have been the departure. The reasoning is also right on its own
   terms: a suppression is something a later reader removes, and here removing it destroys a
   processor's typing.
   The page's `"use client"` + `useParams` shape matches every other interactive tab
   (`needs`, `verification`, `documents`), so that is convention too.
2. **No borrower-email lookup — right to record rather than guess.** "Which borrower on a
   multi-borrower file" is a real question, and pre-filling the wrong one sends a real person
   somebody else's document list. Worth saying what the answer probably looks like so the next
   ticket does not re-derive it: not a pre-filled value, but the file's borrowers offered as a
   choice — a `datalist` or select — which removes the typing without the app ever deciding who the
   recipient is. Left unbuilt; it is a product decision and outside this ticket.
3. **No layout seam for LP-812 — right to leave it.** Shaping a seam for a ticket nobody has
   designed is the same error as sizing a discriminator column for LP-818's reply drafts, which we
   agreed to avoid in LP-809 for the same reason. The panel is a self-contained component that a
   later layout can place; that is the seam. Recorded for LP-812.

### On the split, and the reason for it

The builder retracted its own justification: it told me the frontend half needed a navigation home
that someone else had to decide, and the tab route had existed since LP-33 with `lib/navigation.ts`
already linking to it. The retraction is the right call and the checking-before-escalating is the
habit that matters — but the split still stands on the reason now given, that two reviewable halves
beat one diff nobody reads. §4 says as much directly.

### Done when, and the standing checks

The LP-811 section has no "Done when" clause — the sixth, and the last of M1. Judged against the
frontend bullets: copy and send are both present, "open in mail client" is disabled above the length
threshold, and the Bcc offer is displayed with the reason. The `Reply-To` and footer tag are
LP-811a's and shown here for review.

Standing checks: no `company_id` handling in the frontend; nothing logged; no AI. The panel displays
what LP-810 composed and LP-811a scanned, and the one thing it adds — a processor's own edit — is
now the thing that actually travels.
