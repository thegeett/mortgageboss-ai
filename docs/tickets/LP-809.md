# LP-809 — Draft accumulation

- **Ticket:** LP-809 · Phase 4, M1, ticket 6 of 26
- **Status:** Built — awaiting review
- **Date:** 2026-09-06
- **Plan:** [`phase4-build-plan.md`](../phases/phase4-build-plan.md) LP-809 section
- **Design:** [`phase4.md`](../phases/phase4.md) §6 (the audit record)
- **Scope:** `models/communication_needs_item.py` (new), `models/communication.py`,
  `services/email_draft.py` (new), `services/finding_resolution.py` (the wire-up), one migration
- **Migration:** `b8d5e0a17c42` — the join table, two provenance columns, a partial unique index,
  two readonly views.

The plan is four sentences: `Communication(direction=OUTBOUND, status=DRAFT)` plus a
`communication_needs_items` join; wire `request_documents_in_bulk` to add to the open draft;
regenerate on add and remove.

---

## What was done

### One email, not four

A processor requests documents on Tuesday, three more findings land on Wednesday, and the borrower
should receive **one** message listing everything. So a draft holds a **set** of needs, and its body
is regenerated from that set on every add and every remove.

**Regenerated, never appended.** Appending cannot express a removal: the removed document would stay
in the email while the needs list said otherwise, and the email would look perfectly fine. That is
the failure mode `test_the_body_is_regenerated_not_appended` exists for.

Until LP-810's flag flips, the regeneration is LP-817's deterministic render — what that module
means by "the plain template". Nothing in this ticket calls a model.

### The join, and why the existing column was not enough

`Communication.needs_item_id` already exists and holds **one** need. That is the right shape for a
sent, single-purpose message and the wrong one for a draft still collecting. It is unchanged and
still answers "which need was this message about".

Both sides of the join **CASCADE**, deliberately differing from `needs_item_id`'s `SET NULL`. That
column preserves the historical fact that a *sent* message concerned a need that has since gone. A
draft has no history to preserve — it was never sent — and a need that no longer exists must not
still be listed in an email.

### What does not go into a borrower's email

LP-800 assigns every document type a responsible party, and this draft is addressed to the borrower.
A need for an appraisal, a title commitment or an employer's VOE is real work that is **not theirs
to do**, so it is not added.

It is **not lost**. The need stays on the needs list, which is where a processor tracks it, and
LP-820 owns the paths that chase the other parties. `DraftUpdate.skipped_not_borrower` returns which
ones, rather than swallowing them: a processor who requested five documents and sees three is owed
an answer, and the answer is not an error.

**An untyped need counts as borrower-facing**, which is the opposite of LP-800's default and
deliberate. LP-624 makes untyped needs routine — a request generated from a finding's own sentence
("One more source stating the date of birth") has no catalog type at all. Those needs were created
*by* a processor clicking "request documents", so the decision to ask the borrower was already made
by a person. LP-800's processor default protects against a document nobody classified; it should not
veto a request somebody made.

---

## Decisions

### Two columns the plan did not ask for

`communications.template_key` and `communications.template_version`.

`phase4.md` §6 requires the record to capture *template + version* beside the rendered body, because
that record is evidence. ADR-401 pins each version to a content hash so a version names exactly one
set of words — worth nothing if the version used is not stored. **No other M1 ticket has a migration
to add them**: LP-811 is explicitly migration-free and LP-810's is the prose cache. So they belong
with the ticket that first renders a template into a communication, which is this one.

The version is stamped **from the render itself**, not read from the registry separately. Read
separately, a template bumped between the render and the stamp files the new words under the old
version — the one thing the hash pin exists to make impossible. `render_draft_body` returns the
whole `RenderedTemplate` for that reason: subject, body, key and version travel together, so there
is no point at which they can come apart.

### A partial unique index, and the constraint LP-818 will have to revisit

`uq_communications_open_draft` on `(loan_file_id, template_key)` where `status = 'draft' AND
deleted_at IS NULL`.

The service looks before it creates, but look-then-create races: two near-simultaneous requests each
find no draft, each create one, and the file holds two half-populated drafts with no basis for
choosing between them — the same shape as the duplicate needs items LP-801's review found, reached
by another route. The index is what makes "the open draft" true under concurrency rather than merely
usual. It is declared on the model as well as in the migration, because the test database is built
by `create_all` and a constraint that exists only in the migration is one no test can violate.

**Scoped to `(file, template kind)` rather than to drafts in general**, because a file may
legitimately hold more than one kind of draft. LP-818 adds needs-less **reply** drafts, and one per
thread is the shape that will want. A reply draft has no template key to distinguish it by, and NULL
keys do not collide in a unique index — so LP-818 will pass *through* this constraint without being
governed by it. Recorded in the migration and here rather than discovered there.

### An emptied draft is kept

A processor removing the last line is editing, not abandoning. Deleting the row would discard
whatever else they had changed, and an empty draft is visibly empty where a missing one is not.

### The signer is not resolved

The body keeps the template's own `$processor_name` placeholder. A draft is not addressed to anyone
until it is sent, and LP-811 owns the send — resolving a signer now would bake in whoever happened
to click "request documents", who is not necessarily who sends it.

---

## Tests

`tests/services/test_email_draft_lp809.py`, 14 tests, plus one added to
`tests/test_readonly_query.py`.

Three of the behaviours here fail **silently** if wrong, which is what the tests are shaped around:

- **A second draft** is invisible until somebody opens the tab and finds two.
- **An appended body** reads perfectly while contradicting the needs list.
- **A stale `template_version`** is a correct-looking audit row pointing at the wrong words.

`test_two_files_get_two_drafts` is the control for every accumulation test: without it, "one open
draft" could be satisfied by one draft for the entire database and all of them would still pass.

### The views are executed, not only string-matched

`test_the_lp809_views_are_valid_sql` runs both views against the real test database — the gap LP-822's
review found, in the migration immediately after it. The **rebuild** of `readonly.communications` is
the more dangerous of the two: it drops a view that already exists and recreates it, so a mistake
does not merely fail to add a view, it leaves the deploy with none, and the text-matching checks pass
because the text is syntactically fine. The test also asserts `body`, `subject`, `sender` and
`recipient` are still absent — a rebuild is exactly where dropped content comes back by accident,
because the new view is written fresh rather than altered.

Verified by mutation, each confirmed applied before the run was believed:

| Mutation | Result |
|---|---|
| `template_version` stamped as a literal rather than from the render | 1 test fails |
| an untyped need treated as not borrower-facing | 2 tests fail |
| the borrower-facing filter removed | 1 test fails |
| draft ordered by title rather than by when each need was added | 1 test fails |
| `template_key` mistyped in the rebuilt view | 2 tests fail |

---

## Assumptions

- **One accumulating draft kind exists today.** Everything is scoped to
  `initial_documentation_request`. The reminder and condition-response templates render fine but
  nothing accumulates into them; LP-814 and LP-820 are where that question arrives.
- **`display_id` is the borrower-facing loan reference.** It is what the file is called everywhere
  else a person sees it.
- **A need's `title` is safe to put in front of a borrower.** It is written by
  `request_documents_in_bulk` from a document label or a finding's own request sentence, both of
  which are already borrower-facing prose. A hand-typed need title would also reach the draft, and
  that is the same trust the needs list already places in it.

---

## Deliberately not done

- **No API and no frontend.** The plan scopes this to the backend; LP-811 owns copy-and-send and
  LP-812 the timeline. There is currently no way for a processor to *see* the draft this ticket
  builds — which is fine for one ticket in a chain, and stated so nobody assumes otherwise.
- **No draft deletion or discard.** Nothing asks for it, and "discard this draft" interacts with the
  partial unique index in a way that wants a real decision rather than a guess.
- **No recipient.** `Communication.recipient` stays null. The borrower's email address is LP-811's
  to resolve, and putting a guess there now would look like an addressed message.
- **No activity-log entry for a draft change.** LP-812 merges communications with the activity log
  and is where "was this noisy?" gets answered with a screen in front of somebody.

---

## For the reviewer

The three standing reviewer checks:

- **Tenancy** — no `company_id` derivation. Every entry point takes an already-resolved `LoanFile`;
  the draft is a file-owned child (ADR-052) and scopes transitively. LP-805 remains the only
  resolver.
- **Message content in logs** — no logging added. Relevant here because this ticket is the first to
  put a rendered borrower-facing body into the database: it goes to `Communication.body` and
  nowhere else, and the readonly view still drops it.
- **AI in the decision path** — none. The body is a deterministic template render.

The judgement calls worth a second opinion, in the order I would attack them:

1. **The two provenance columns**, which the plan did not ask for. I argue §6 requires them and no
   later M1 ticket can add them. If you disagree, they are cheap to pull — but then §6's requirement
   has no home in M1 and that should be recorded as an escalation rather than left.
2. **An untyped need being treated as borrower-facing**, which inverts LP-800's default. My reason
   is that a person already decided; the counter-argument is that LP-800's default exists precisely
   because unclassified things should not reach a borrower, and consistency has value of its own.
3. **The partial unique index's scope.** I chose `(loan_file_id, template_key)` knowing LP-818 will
   slip through it. The alternative — a discriminator column now, sized for a draft kind LP-818 has
   not designed yet — seemed worse. It is a guess about a ticket I have not read.

**LP-809 does have a "Done when"?** No — it has none, the fourth after LP-802, LP-817 and LP-822.
Judged against the section's four sentences: the join exists, `request_documents_in_bulk` adds to the
open draft, and the body regenerates on add and on remove.

---

## Review (second session, protocol §3)

One finding, and three of the builder's four judgement calls endorsed. The design is right and the
migration is careful — the view-execution test carried forward from LP-822's review is exactly the
right response to a view REBUILD, which is a worse case than the new view that prompted it.

### A — A dollar amount in a need title breaks the send

`render_draft_body` deliberately leaves `$borrower_first_name` and `$processor_name` in the stored
body so the send decides who signs. That makes the stored body a **mixture** of template
placeholders and text a processor wrote, and the second substitution pass has to survive the human
half. It does not.

Measured on a title no one would think twice about — *"Proof of $10,000 gift deposit"*, an ordinary
mortgage ask:

```
PROBE stored body contains: True
PROBE send-time substitute RAISES: ValueError: Invalid placeholder in string: line 5, col 12
```

`$10` is not a valid placeholder, so a strict `Template.substitute` over the stored body raises and
the send dies on a perfectly reasonable request. LP-811 would have inherited this and met it as a
runtime error on real data, with nothing in LP-809 to say the body was not safe to substitute.

Fixed here rather than left for LP-811, because this ticket created the hazard: `finalise_draft_body`
is now the one supported way to resolve a stored body, using `safe_substitute` — which is right for
this input for the same reason `substitute` is right in LP-817's `render`. There the context is a
caller's and a missing variable is a bug; here the content is a person's, and a literal dollar amount
should reach the borrower exactly as typed. The docstring states the distinction so the two do not
look like an inconsistency.

Two tests, both verified by mutation: strict substitution fails the round-trip test, and a
`finalise` that resolves nothing fails both.

### What I got wrong, since it bears on how much to trust the rest

I also predicted a deadlock: a need typed to a lender-held document after being added to a draft
would make `_document_line` call `render_document_block`, which raises for non-borrower types, so
`_regenerate` would fail and the need could never be removed. **Measured, and it does not happen.**
`_document_line` guards on `get_guidance(...).borrower_label`, and LP-800's own test proves every
type carrying a label is BORROWER — so the raising branch is unreachable from here. The reasoning
was clean and the conclusion was wrong; the probe is the only reason it is not in this document as a
finding.

### The builder's four, ruled on

1. **`template_key` / `template_version` — KEEP THEM, and this is not scope creep.** `phase4.md` §6
   requires the record to capture template + version beside the body, and that record is evidence.
   ADR-401 pins a version to exactly one set of words, which buys nothing unless the version used is
   stored. Checked the claim that no later M1 ticket could host them: LP-811 is explicitly
   migration-free and LP-810's migration is the prose cache, so the columns would otherwise have no
   home in M1 and §6's requirement would be quietly dropped rather than deliberately deferred.
   Adding them with the ticket that first renders a template into a communication is where they
   belong. Stamping the version from the render rather than from a separate registry read is the
   detail that makes it honest.
2. **An untyped need counts as borrower-facing — endorsed, with the precondition written down.**
   The argument is right and my own LP-801 review is why: `canonical_need_type` returns None for
   every `requested_documents` sentence, so untyped needs are not an edge case, they are the normal
   output of the path that feeds this. And LP-800's processor default exists to stop a document
   *nobody classified* reaching a borrower — not to veto a request a person deliberately made. The
   one soft spot is that the reasoning is about the CALLER, not the need: `request_documents_in_bulk`
   passes needs a processor just created by clicking, and the function is public. Today it has one
   caller and the precondition holds; the docstring now says so, so a future caller passing
   AI-proposed needs meets the assumption rather than inheriting it.
3. **The partial unique index's scope — endorsed.** `(loan_file_id, template_key)` with NULL keys
   not colliding is a real limitation for LP-818's reply drafts, and the alternative is a
   discriminator column sized for a ticket nobody has designed. Guessing at that shape now is the
   more expensive mistake. It is recorded in the migration, the model and the progress Notes, which
   is the difference between a limitation and a trap.
   I checked the predicate actually matches, because a partial index whose `WHERE` never fires is a
   guarantee that silently does not exist: `str_enum` sets `values_callable`, so the column stores
   `draft` and not `DRAFT`. And the index is declared on the model as well as in the migration, so
   `create_all` builds it and a test can violate it — the comment there makes the point better than
   I would have.
4. **Rebuilding `readonly.communications` — right call, and the test is the right one.** A rebuild
   is where dropped content comes back by accident, because the new view is written fresh rather
   than altered, and the text-matching checks pass on syntactically fine SQL. Asserting
   body/subject/sender/recipient are still absent is the assertion that matters.

### Done when, and the standing checks

**LP-809 has no "Done when" clause** — the fourth, after LP-802, LP-817 and LP-822. Judged against
the section's four sentences: the join table exists, `request_documents_in_bulk` adds to the open
draft, and the body regenerates on add and remove. All met.

The three standing checks pass. Tenancy: no `company_id` derived anywhere; the draft is reached
through a `LoanFile` the caller already resolved under the authenticated user's company. Logging:
none added in the new service, and the wire-up's activity `detail` carries titles and ids, no body
or subject. AI: none — `render_draft_body` is LP-817's deterministic render, which is what that
module means by the plain template.

---

## Follow-up, 2026-09-07 — the singular route had no draft call

Reported from staging on **LF-JR4T**: a processor clicked "Request" on the ID-3 date-of-birth
finding, saw the activity line on the communication page, and found no draft.

**Measured two ways before changing anything.** In the code, `add_needs_to_draft` had exactly one
call site in the whole backend, inside `request_documents_in_bulk`. On staging, the file's activity
read `Requested docs from finding ID-3` — wording only `request_docs_for_finding` produces, where
the bulk route writes `Requested N document(s) from verification findings` — and the file had
**zero** rows in `communications`, so no draft had been created and then lost.

**Why the gap was invisible.** The section this ticket was built from states the chain as *"processor
clicks 'Request docs' on a finding -> the request is added to a pending draft -> multiple requests
accumulate into one email"*, and then names `request_documents_in_bulk` as "the accumulation
primitive". The build note was followed exactly. The singular click in the same sentence had no
implementation, and nothing looked wrong: the needs item appeared, the finding showed as requested,
the activity logged. Only an email that was never started was missing, and that is not a thing a
screen can show.

**The fix.** `request_docs_for_finding` takes the `loan_file` its one caller already holds — a
parameter rather than a lookup, so the endpoint's tenant-scoped read is not repeated unscoped — and
makes the same `add_needs_to_draft` call, in the same position the bulk route makes it: after the
needs item is flushed (the join row's foreign key needs it) and before the activity log (a line
claiming a request was made should not precede the draft that will carry it).

**The class, not the instance.** Two routes create a needs item from an explicit "request documents"
action; one had the draft call and one did not. `POST /needs` — "a need the AI missed" — is
deliberately **not** a member: it records a need, it is not the processor asking a borrower for
anything, and the section's trigger is the click on "Request docs". The derived creators
(`needs_engine`, `needs_ai`, `needs_from_findings`, the MISMO import) are not members either.

**The reader was broken too, and the backend fix alone would not have shown anything.**
`useResolveFinding` invalidated needs, activity, DTI and LTV, but not the draft the request now
writes. With the default 60s `staleTime`, a processor who had the communication page open, clicked
Request, and went back would still have seen no draft. Fixed in the same change.

**Historical rows are left alone, by decision.** LF-JR4T carries four earlier finding-origin needs
and no draft. They are not backfilled into one: a draft that silently fills with old requests is its
own surprise, and the next request creates a draft holding only what was just asked for.

**Tests, at the layer the bug was seen.** Two API tests — the row-level button starts the draft and
carries the need into its body, and both routes accumulate into ONE draft. The second exists because
giving the singular route its own draft would satisfy the first and mean a processor using both
buttons mails the borrower twice. Both fail with the call removed. The fixture matters: `ID-3`
declares an EMPTY `requires_documents`, so the bulk route resolves nothing for it — the accumulation
test uses `IN-1` for its bulk leg, or it would pass on one need without exercising the second route
at all. One frontend test asserts the invalidation and fails without it.

---

## Review — the follow-up commit (`4ee92d76`)

Six findings, all confirmed by running something rather than by reading. Five fixed here; the sixth
turned out to point at the wrong component and is fixed at the right one.

**1. The button asked the borrower for documents that are not theirs to send.** `create_needs_item`
was called with no `needs_type`, and `_is_borrower_facing` counts an untyped need as borrower-facing,
so every request from this route joined the borrower's draft regardless of whose document it was.
Measured over the 67 rule specs that declare documents:

| | rules |
|---|---|
| every declared document is the borrower's | 28 |
| every declared document is someone else's | 34 |
| a mix of both | 5 |

The 34 name their own credit report and the closing disclosure (the lender's), the appraisal
(lender), the title commitment (title company), the VOE (employer), the purchase agreement (agent),
the condo master policy (insurer). The **bulk** route types its needs from the document label and
skipped all 34 correctly; the row button emailed all 34. Same intent, two opposite routings — a
processor clicking "Request Credit Report" on CR-6 drafted an email asking the borrower to send us
their own credit report.

**2. The finding's internal prose became a line in the borrower's email.** The need was titled
`f"Documents for: {finding.message}"`, and `_document_line` falls back to the title for an untyped
need, so the borrower read `- Documents for: Stated monthly income is not supported by the pay
stubs`. `request_documents_in_bulk`'s own docstring rejects exactly this: "The title is the DOCUMENT,
not the finding's prose."

**3. A second click asked the borrower twice.** `requestable()` checks only the rule id, the endpoint
never read `details["docs_requested"]`, and the row cannot gate itself — `RuleFindingPublic` carries
no `docs_requested` field at all, which is why the button stays live. A second click created a second
needs item with a fresh id, and `add_needs_to_draft` dedupes only by id, so the email grew a second
identical line. The bulk route has guarded this since LP-801 (`_already_asked_key`).

**All three are one defect: a need built in a shape the draft machinery cannot reason about.** The
fix is one creation path, not three guards. `_create_document_needs` — the body of
`request_documents_in_bulk` — is now shared, the endpoint passes the documents the row offered
(`_documents_a_finding_wants`, the same expression `RuleFindingPublic.missing_documents` renders and
the bulk endpoint groups by), and the type, title, dedupe key and draft membership all follow from
them. The dedupe now spans both routes rather than one. `request_docs_for_finding` returns a list,
because a finding wanting two documents is two needs — the shape the bulk route has always had.

**The untyped fallback stays** for a finding that names no document, which is LP-624's channel
(`requested_documents` records a sentence: "One more source stating the date of birth"). That is the
case `_is_borrower_facing`'s borrower-by-default is actually about, and it is the reported LF-JR4T
path. The row offers no button in that state, so it is reachable only through the API.

**Found while fixing 1: an empty draft was minted for a request the borrower has no part in.**
`add_needs_to_draft` created the `Communication` row *before* consulting the filter, so requesting
the appraisal produced an outbound draft with no documents in it — occupying the file's one
open-draft slot, showing on the communication page as an email waiting to be sent, and sendable by
LP-816 as a real message listing nothing. Guarded; an already-open draft is untouched, and
`DraftUpdate.draft` is now `Communication | None`.

**4. `["needs", identifier]` matched no query, and the new test pinned it.** The needs list is
registered under `needsQueryKey` = `["loan-file-needs", id]` — needs-dashboard, needs-summary,
needs-completeness and verification-stats all read it — and TanStack prefix-matches element by
element, so `"needs"` never matched `"loan-file-needs"`. Pre-existing; what this commit added was
`verification-draft-invalidation.test.ts` asserting that dead key as its **positive control**, so it
shipped pinned by a green test that read as proof the invalidation worked. The test now registers
real queries and reads `isInvalidated` back, with an unrelated query as the negative control — a key
that matches nothing cannot pass that way.

**5. A live model call sat inside the button's HTTP request.** `add_needs_to_draft` → `_regenerate` →
`_composed_framing` called `compose()` whenever `email_draft_enabled`, and the prose cache is keyed
on `requested_labels`, so every add changed the key and missed. Against CLAUDE.md's "long work runs
on Celery, not in the request". Latent rather than live — the flag is `False` and appears in no
environment — but it becomes real the moment someone turns it on. Split: `_cached_framing` reads a
stored composition and never calls the model, `compose_open_draft_prose` does the composing, and
`tasks/email_draft.compose_draft_prose` runs it after the commit. The draft is a complete email in
between (the plain template carries v1's exact wording), so a worker that is slow or down costs the
file its composed framing and nothing else. Nothing is marked on exhaustion, deliberately: the
failure leaves a sendable draft, and an error nobody can act on is noise.

**6. The confirmation described only the needs item — and the row that matters confirmed nothing at
all.** The reported line (`findings-list.tsx`) renders the LEGACY AI-sweep findings. Enumerating
every `notify*` under `components/file/verification/` showed it is the only one: the §8 rule-finding
rows — including the row-level Request button this ticket wired — called `mutate(action)` with no
handlers, so a request that succeeded and one that failed looked identical. Both now say what
happened, in the same words, and the words are true for both outcomes: *"On the needs list, and
whatever the borrower can send is in the file's email draft."* "Whatever the borrower can send" is
load-bearing — for those 34 rules the correct outcome is a need and deliberately no email.

**Every fix carries a mutation check.** The pre-fix untyped shape (`documents = []`) fails all three
new endpoint tests; removing the empty-draft guard fails the routing test; removing
`_already_asked_key`'s `continue` fails the second-click test; composing in the request path again
fails three LP-810 tests including the new one; the dead query key fails the rewritten invalidation
test; and removing the panel's handlers fails the confirmation test.

Backend 7414 passed / 6 skipped / 1 xfailed; frontend 1440 passed; ruff, mypy strict, biome, tsc and
build all clean.
