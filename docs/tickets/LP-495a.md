# LP-495a — Structural: OC-1 · DT-6 · RE-1 · LO-1 · LO-2

⚠️ **Created in the first step** (standing policy 10). **Phase A only — nothing built, nothing committed
to the rule engine. Awaiting approval.**

## What this ticket is

Five structural rules. **Phase A was asked to decide whether three of them exist at all.** It decides
that two do not, one survives in reduced form, and one is the fifth instance of LP-487's question.

| rule | Phase A verdict |
|---|---|
| **DT-6** | ⚠️ **BUILD** — see the REVISION below. My drop was wrong. |
| **RE-1** | ⚠️ **BUILD** — see the REVISION below. My drop was wrong. |
| **OC-1** | ⚠️ **KEEP — it does NOT duplicate OC-2**, and it is probably deterministic (LP-487, 5th time) |
| **LO-1 / LO-2** | **KEEP** — real documents, real fields; LO-1 needs a trigger source decision |

---

## A1 — ⚠️ "Retained" is an INFERENCE. No document, field or MISMO fact states it.

**Four independent searches, all negative:**

1. **No extractor field.** `grep` across all 121 extraction specs for `retain|reo|for_sale|listed_for|
   disposition|intent_to_sell|pending_sale` returns **one** hit — `retained_earnings_or_capital` on a
   business tax return, unrelated.
2. **No REO schedule document.** ⚠️ Searched the **raw source tree**, not one classifier query (policy 11,
   LP-494's mistake). 15 filename matches for `reo|real.?estate.?owned|schedule|rental|lease` — **all of
   them lease agreements and mortgage notes.** No REO schedule, no 1003 property section.
3. **No MISMO fact.** `mismo_section.py` emits nothing for REO, owned properties, or dispositions.
4. ⚠️ **The tags themselves are VOCABULARY ORPHANS.** `property.is_retained_reo` and
   `property.retained_pitia` are in `fact_tags.csv` and `rule_tags.csv` with a producer named, and have
   **zero entries in `tag_production.yaml` and no producer in `app/`** — the exact ADR-289 shape, and
   `property.is_warrantable_condo`'s situation exactly.

**So DT-6 is dropped, and the reason is RE-2's, which is stronger than "no fields exist":** owning a
property is not retaining it. A borrower may be selling. **Inferring the trigger makes the rule wrong at
its root** — it would put a PITIA into the DTI for a property that is under contract, inflating the ratio
and failing a qualified borrower.

### ⚠️ RE-1 considered SEPARATELY, as the ticket asked — and it also fails, for a different reason

Reconciling *owned* properties across sources needs no retention inference, so it deserved its own look.
**It fails on the other side: there is nothing to reconcile AGAINST.**

The *signals* exist and are strong — **71 mortgage statements, 21 property tax bills, 22 lease agreements,
16 payoff statements**, many for non-subject properties. But the **stated** side does not: no REO schedule
is extracted, and MISMO emits no owned-property list. **"Undisclosed REO" is a claim about a disclosure
that the system cannot see**, so the rule could only ever say *"documents suggest another property"* —
which is the AI-inference half the catalog names, with no stated list to test it against.

**Recommend: drop both.** If Priya says a 1003 REO section reaches processors, RE-1 becomes buildable and
worth reopening — that is question one for her.

---

## A2 — ⚠️ OC-1 does NOT duplicate OC-2. They ask different questions from different evidence.

**I read both live specs rather than assuming from the shared tag** (CO-3's mistake in LP-494):

| | OC-1 | OC-2 (live) |
|---|---|---|
| question | do the borrower's **own 1003 declarations agree with each other**? | is the stated occupancy **plausible against address signals**? |
| evidence | intent-to-occupy, FHA second-residence indicator, financed-unit count | address consistency, current-address type, subject-address agreement |
| nature | a **comparison** | a **judgment** |

OC-1's spec says so explicitly: *"it does NOT assess address signals (the produced tag has no residence
address to reason over — LP-371 D3)"*. **ADR-375 does not apply — this is not one comparison judged
twice.** Keeping OC-1.

⚠️ **And a data finding that cuts the other way from LP-494's.** `property_type` is null on all 28 files,
but **`occupancy_type` fills 22 of 24 stored properties** (all `primary_residence`). **OC-1's scoping input
RESOLVES.** The condo lane's gap is not universal, and I should not have generalised it.

---

## A3 — ⚠️ LP-487's question, and the catalog table in this ticket is WRONG

**All five rows read `ai_fuzzy_match`, not "Deterministic + AI".** The ticket's table describes intent;
`rule_kinds.csv` is the artifact. Reported, not silently reconciled.

**OC-1 is the strongest candidate for the 5th instance.** Its tag `occupancy.consistent_with_signals` is
declared `ai`, but OC-1's own spec calls it *"the loan-level occupancy.consistent_with_signals **structural
fact**, which encodes that **DECLARATION agreement**"*. Comparing three 1003 declarations against each
other is a deterministic comparison — there is no perception left. ⚠️ **But the tag is SHARED with live
OC-2**, so re-kinding it is not a local edit and needs Phase B care.

**LO-2's catalog rationale already splits itself** — *"Explanation+date+amount (DET); signature (AI for
scans)"*. The deterministic three-quarters needs no model.

**DT-6 / RE-1: moot** (dropped).

---

## A4 — the standard checks

- **Distrust overlap: none**, and `txn.amount` is moot since DT-6 is dropped.
- **Occupancy scoping** will be an applicability predicate, not an outcome (LP-487).
- ⚠️ **LOX corpus, measured:** `letter_of_explanation` 9 · `_misc` 9 · `_asset` 7 · `_property` 7 ·
  `_income` 2 · `credit_explanation_letter` 4 = **38 documents across six types.** ⚠️ Only
  `letter_of_explanation_asset` (7) produces `explanation_items` list rows — **the other 31 produce none**,
  so LO-2's per-item completeness read resolves on one type today. And **7 LOX emails route to
  `general_correspondence`, which has no extractor.**
- ⚠️ **LO-1's real blocker is not a tag, it is a SOURCE for the trigger.** `loe.required_conditions` is
  "the list of conditions requiring an LOE" — that list is **lender- and AUS-driven and nothing in the file
  enumerates it.** Deriving it from other rules' findings is possible and is a design decision, not a
  lookup. **This is the one thing I need Priya's answer on to build LO-1 honestly.**

---

## ⛔ Phase A stops here

**Recommendation: drop DT-6 and RE-1 with the evidence above; build OC-1, LO-1 and LO-2.**

**Open for approval:**
1. **DT-6 + RE-1 dropped?** (Both fail, for two different reasons.)
2. **OC-1 re-kinded to deterministic?** It shares its tag with live OC-2, so this is not a local edit.
3. **LO-1's trigger source** — derive from the run's own findings, or hold for Priya's condition list?

**Network:** not yet used this ticket — no threshold is needed until a rule survives to Phase B.
**Model spend: $0.**

---

# ⚠️ REVISION — the A1 drop was WRONG, and the mistake was the same one as LP-494's

Geet asked "are you sure?" — the same challenge that un-dropped CO-3 — and the answer is no.

**What I did wrong:** I searched for a source for *"retained"*, found none, and stopped. **I never checked
whether the RECONCILIATION these rules actually describe has both sides available.** I asserted "there is
nothing to reconcile against" without querying the stated side even once. That is LP-494's error exactly:
concluding absence from a search I did not run.

## Both sides resolve on real data

**The stated side — which I claimed did not exist:**

```
StatedLiability: 135 rows across 14 loan files
  MortgageLoan 61 · Revolving 42 · Installment 29 · HELOC 2 · Open30Day 1
```

⚠️ **61 disclosed mortgage obligations.** MISMO emits `liability.{k}.type / .monthly_payment /
.unpaid_balance / .holder_name`. I looked for an REO *schedule*, did not find one, and declared the
reconciliation target missing — while the borrower's disclosed mortgages sat in MISMO, populated, on half
the files.

**The signal side — the strongest corpus in this lane:**

```
mortgage_statement: 71 unique docs, 71 extracted
  54/71 lender_name · 53/71 property_address · 52/71 monthly_payment
  52/71 unpaid_balance · 52/71 escrow_amount · 52/71 loan_number_masked
```

## How both rules survive WITHOUT the retention inference

⚠️ **The trigger only breaks if a rule ASSERTS retention. Neither needs to.** Both SURFACE a discrepancy
and hand the retention question to the processor — CO-3's absent-fidelity pattern and CO-5's litigation
pattern, both already accepted.

**And they are split so ONE MATCHER serves both (ADR-375, the CR-1/CR-4 precedent):**

| rule | question | outcome |
|---|---|---|
| **RE-1** | is each mortgage statement's obligation **DISCLOSED** among the stated liabilities? | unmatched → `needs_review` — *possible undisclosed obligation* |
| **DT-6** | for a MATCHED obligation, does the stated monthly payment equal the statement's **PITIA** (payment + escrow)? | short → `needs_review` — *confirm retained vs. selling; the DTI may understate* |

**Neither ever fires.** An unmatched statement can be a paid-off loan, a duplicate, or a co-signed debt;
an understated payment can be a property under contract. Both are questions, not defects.

**RE-2 stays dropped and that is still right** — it asserted a *consequence* from the inference. These
surface a *discrepancy*. Different acts.

## ⚠️ What stays unwired, stated so it cannot be mistaken for coverage

- **`property.is_retained_reo` and `property.retained_pitia` remain vocabulary orphans** — no
  `tag_production.yaml` entry, no producer, no source. **Neither rebuilt rule reads them.** They stay
  exactly as inert as `property.is_warrantable_condo`.
- ⚠️ **Schedule E is genuinely empty** — the snapshot models `ScheduleEPropertyRecord` (address, rents,
  expenses, net) but **0 property rows across 15 real tax returns**. The rental-income path DT-6's catalog
  mentions ("or net rental") is **not available**, and the 75% factor from B3-3.8-01 is therefore not used.
  Recorded so the next reader does not go looking for it.
- ⚠️ `lender_name` at **54/71** means the tolerant match abstains on ~24% of statements. Abstain is the
  safe direction here, and the bar will say so.

## LO-2 — measured, and one leg is dead

```
LOX (6 types): 38 unique docs, 34 extracted
   9/34  explanation_summary      7/34  borrower_signature_present
   6/34  referenced_date          5/34  borrower_signature_date
   0/34  referenced_amount   ← never populates
```

⚠️ **The AMOUNT leg fills 0/34 — the TI-3/4/5 block.** LO-2 must be built on explanation + date +
signature and **must not assert on amount**. ⚠️ And `borrower_signature_present` is a **typed field**, so
even the signature half needs no AI — the catalog's *"signature (AI for scans)"* is stale. That is LP-487's
question answering yes a **sixth** time.

---

## ⛔ Where this session stopped

**Phase A is complete and revised. No rule code was written.** The remaining context in this session is not
enough to build five rules, their tags, recipes, specs, bars, tests and the ~20-site census cascade without
risking a half-applied change on a shared branch.

**The build plan, ready to execute:**

1. **One matcher recipe** — `reo.statement_disclosure` (doc-scoped on `mortgage_statement`) matching
   `lender_name` + `monthly_payment` / `unpaid_balance` against MISMO `liability.*` where
   `type == MortgageLoan`. Tolerant on the name; abstain when it cannot be read. **RE-1** and **DT-6** both
   read it — one matcher, two questions (ADR-375).
2. **LO-2** on `explanation_summary` + `referenced_date` + `borrower_signature_present`, doc-scoped across
   **all six** LOX types, with the amount leg deliberately absent.
3. **OC-1** — spec already exists and is held only because its tag is AI. Either derive a self-consistency
   rate (~$0.05) and activate on `ratify-pending`, or re-kind the tag deterministic — ⚠️ **it is SHARED
   with live OC-2**, so that is not a local edit.
4. **LO-1** — still blocked on the one thing research cannot supply: **what conditions require an LOE.**
   That list is lender- and AUS-driven and no document enumerates it. Deriving it from the run's own
   findings is a design decision for Geet, not a lookup.

**Model spend this ticket: $0.**

---

# ⚠️ THE LESSON, and it belongs to the review as much as to the build

**A search that finds no source for ONE side of a comparison has not established the comparison is
impossible. Check BOTH sides before dropping a cross-source rule.**

**Second occurrence in two tickets:**

| | what was checked | what was not |
|---|---|---|
| **LP-494 / CO-3** | IH-7 covers master insurance → "duplicate" | IH-7's own header **excludes fidelity** — the leg CO-3 needed |
| **LP-495a / DT-6 + RE-1** | no source for "retained" | **the stated side** — 135 `StatedLiability` rows, 61 `MortgageLoan`, never queried |

⚠️ **The failure mode is RHETORICAL, not technical, and that is why it survived review.** A1 opened with
*"Four independent searches, all negative"* — and **not one of the four queried the stated side.** All four
probed extractor schemas and document filenames; none touched the database. Enumerating searches reads as
thoroughness and conceals that they all examined the same half of the comparison.

⚠️ **THE APPROVER MADE THE SAME ERROR, and asked for it to be recorded.** Geet approved the drop, then:
*"I read 'four independent searches' as thorough without noticing none of them queried the stated side."*
**A confident count of negative searches passed review precisely because it looked like diligence.** Both
author and reviewer were reading the count instead of the coverage. Recorded so the next reader distrusts
the shape of the argument, not just its conclusion.

**The checks, before any future drop:**
1. Name **both** sides of the comparison, then show a query result for **each**.
2. Query the **database / MISMO** side, not only extractor schemas — stated facts live in
   `StatedLiability` and `mismo_section.py`, not `app/ai/extraction/`.
3. When a trigger is an inference, ask whether the rule can **surface** rather than **assert**. A
   `needs_review` that hands the question to the processor needs no inference (CO-3 fidelity, CO-5
   litigation).
4. One matcher can serve two rules asking different questions (ADR-375) — overlap is not automatically a
   drop.

---

# ⛔ BUILD DIRECTIVES — approved, for the fresh session

Approved by Geet. **Everything else in the ticket stands.** ⚠️ **Print the context before trusting any
rate — LO-2 reads LOX documents, exactly the surface that silently failed in LP-493a.**

### 1. `reo.statement_disclosure` — ONE matcher, RE-1 and DT-6 both read it (ADR-375)

Match `mortgage_statement` (`lender_name` + `monthly_payment` / `unpaid_balance`) against MISMO
`liability.*` where `type == MortgageLoan`. Tolerant on the name; abstain when unreadable.

- ⚠️ **Neither rule may assert retention.** Both surface a discrepancy as **`needs_review`**.
- ⚠️ **Neither reads `property.is_retained_reo` or `property.retained_pitia`** — those stay vocabulary
  orphans, and **a test must pin that**, exactly as CO-1's test pins `property.is_warrantable_condo`.
- ⚠️ **`lender_name` fills 54/71 → ~24% abstain. Say so on the bar.**

### 2. LO-2 — `explanation_summary` + `referenced_date` + `borrower_signature_present`, all six LOX types

- ⚠️ **The amount leg is deliberately absent** — `referenced_amount` fills **0/34**, the TI-3/4/5 block.
  Note it on the spec, and note that only `letter_of_explanation_asset` produces `explanation_items` rows.
- ⚠️ **"No LOE exists" and "an LOE exists but cannot be read" are DIFFERENT verdicts.** They must not
  collapse.
- ⚠️ **`borrower_signature_present` is a TYPED field**, so the catalog's *"signature (AI for scans)"* is
  stale. **Report it; do NOT re-kind without its own Phase A.**

### 3. OC-1 — derive a self-consistency rate, activate on `ratify-pending`

⚠️ **Do NOT re-kind the tag.** `occupancy.consistent_with_signals` is **shared with live OC-2**;
re-kinding is a behaviour change on shipped code and needs its own regression evidence.

### 4. LO-1 — HELD for Priya

The question is *what conditions require an LOE*. ⚠️ **Deriving it from the run's own findings would make
LO-1 a META-RULE over other rules' output, which nothing in the architecture does.** That is an ADR-sized
decision, not a build step.

---

# ⛔ BUILD (Phase B–E) — RE-1 · DT-6 · LO-2 built and ACTIVATED; OC-1 next; LO-1 held

Phase A was **not** re-run; every measured number below is either carried from it or newly measured
where Phase A did not look. **ACTIVE_RULE_IDS 63 → 66.** `SNAPSHOT_VERSION` 4 · `rule_kinds.csv` 135 ·
nothing added to `_BASE_ACTIVE`.

## Acceptance criteria (status)

| # | criterion | status |
|---|---|---|
| 1 | Phase A not re-run; numbers reused | ✅ |
| 2 | ONE matcher serving RE-1 and DT-6 (ADR-375) | ✅ `_reo_match_statement`, pinned by test |
| 3 | Neither asserts retention; **neither can produce `fired`** | ✅ pinned as a spec property |
| 4 | A test pins `property.is_retained_reo` / `retained_pitia` unwired | ✅ |
| 5 | The ~24% `lender_name` abstain stated on the bar | ✅ |
| 6 | LO-2's amount leg absent, `referenced_amount` 0/34 recorded | ✅ |
| 7 | LO-2: "no LOE" and "LOE unreadable" different verdicts, both proven | ✅ + a test asserting they differ |
| 8 | `borrower_signature_present` reported stale, **not re-kinded** | ✅ pinned both ways |
| 9 | OC-1 activated on a self-consistency rate; tag NOT re-kinded | ⏳ next |
| 10 | The context printed before any rate is trusted | ⏳ with OC-1 |
| 11 | LO-1 held, meta-rule reasoning recorded | ✅ |
| 12 | No threshold invented | ✅ — all three carry none, deliberately |
| 13 | AS-1, AS-2, AS-12, IN-12 byte-identical | ✅ md5 diff over `git stash -u` |
| 14 | `SNAPSHOT_VERSION` 4 · `rule_kinds.csv` 135 · suite green · ruff + mypy | ✅ |
| 15 | Census run before pinned counts touched | ✅ |
| 16 | Cost reported; committed locally; not pushed | ✅ **$0 so far** |

## ⚠️ THE ONE PLACE THE BUILD DIVERGES FROM THE APPROVED DIRECTIVE — and why

**Directive 2 said LO-2 should read `explanation_summary` + `referenced_date` +
`borrower_signature_present`, "document_type-scoped across all six LOX types". Those three fields exist
on EXACTLY ONE of them.** Verified against every extractor in `app/ai/extraction/` and against the bench
corpus — not one query (policy 11):

| document type | docs | carries the three legs? |
|---|---|---|
| `letter_of_explanation` | 9 | **ALL THREE** — summary 9/9, referenced_date 6/9, signature 7/9 |
| `letter_of_explanation_misc` | 9 | no — `issue_orquestion` / `reason_or_cause` / `signature_date` |
| `letter_of_explanation_asset` | 7 | no — `source_or_origin_of_funds` / `letter_date` |
| `letter_of_explanation_property` | 7 | no — `reason_or_cause` / `letter_date` / `signature_date` |
| `letter_of_explanation_income` | 2 | no — `reason_or_cause` / `letter_date` / `signature_date` |
| `letter_of_explanation_child_care` | 0 | no — `no_expense_reason` / `letter_date` |
| `credit_explanation_letter` | 4 | ⚠️ **NO EXTRACTOR AT ALL** (bench status: `no_extractor`) |
| `application_loe` | 0 | signature only — no summary, no `referenced_date` |

**Phase A's `9/34 · 6/34 · 7/34 · 0/34` are all correct.** What they conceal is that their DENOMINATOR is
the whole family while their NUMERATOR can only ever come from the 9 base letters — they are really
**9/9, 6/9 and 7/9 on the only type that has the fields.** Read as a sparse fill across 34 letters they
invite exactly the rule the directive describes, which would have reported **25 of 34 letters
incomplete** on real data.

⚠️ **This is the ticket's own lesson recurring in a third form.** LP-494/CO-3 trusted "IH-7 covers it";
LP-495a Phase A trusted "four independent searches"; this trusted a fill rate without asking what its
denominator ranged over. In all three the number was true and the inference from it was not.

**What was built instead:** the three legs are read on `letter_of_explanation`; every other LOE type is
**still in scope** and resolves to `couldnt_check` — *"a letter is present and its completeness cannot be
read"*. The other types' fields are **not aliased** onto the legs: `letter_date` is when the letter was
WRITTEN and `referenced_date` is the date of the EVENT; `borrower_certification` is a prose attestation
and `borrower_signature_present` is whether a signature is on the page. Different facts, and mapping one
onto the other would answer LO-2's question with a fact that is not its answer.

## Per rule

### `reo.statement_disclosure` + `reo.statement_payment_coverage` — ONE matcher, two rules (ADR-375)

`_reo_match_statement` matches each `mortgage_statement` against the MISMO `liability.{k}.*` rows where
`type == MortgageLoan`, tolerant on the holder name (**reusing IH-2's `_normalise_lender_name` /
`_lender_names_agree`**, not a second matcher). A test pins that both recipes call it.

**The match is on NAME because there is no other key** — MISMO emits no property address on a liability,
so the statement's `property_address` (51/67) has nothing to compare against.

**It abstains in four distinct ways, each fail-closed:**
- the statement names no lender (**54/71 → ~24%**, stated on the bar);
- nothing identifying survives normalisation;
- ⚠️ **the application states NO mortgage liabilities** → `unknown`, **never `undisclosed`**. A file whose
  1003 was never imported must not read as a file full of undisclosed debts;
- ⚠️ **more than one stated liability matches** → `unknown`. A first and a second with one servicer is
  ordinary, and picking one would attach DT-6's payment comparison to a liability chosen by list order.

| rule | verdict shape | `input_resolves` |
|---|---|---|
| **RE-1** | `disclosed`→satisfied · `undisclosed`→**needs_review** · else couldnt_check | **true** — 6 statements across 2 active loan files, **all six resolve** (4 undisclosed, 2 disclosed) |
| **DT-6** | `covered`→satisfied · `short`→**needs_review** · else couldnt_check | **true** — same 6 statements; the 2 MATCHED resolve (both `short`), the 4 unmatched abstain by design |

⚠️ **Neither declares a `fired` outcome at all** — pinned as a spec property, not left to the fixtures.

### ⚠️ THE PITIA DOUBLE-COUNT — the directive's wording would have broken DT-6 on every file

The directive said *"does the stated payment cover the statement's **PITIA** (payment + escrow)"*. **The
statement's `monthly_payment` IS ALREADY THE PITIA.** `app/ai/prompts/extraction/mortgage_statement.txt`:

```
monthly_payment  (number)  the total monthly payment (principal+interest+escrow)
escrow_amount    (number)  the escrow portion of the payment, if shown
```

Escrow is a **COMPONENT of the total, not an addend**. Summing them would double-count escrow on all
**50 of the 67** corpus statements that fill both fields and report a shortfall on nearly every file —
**the CO-5 mistake** (a 30-day delinquency figure compared against a 60-day cap) in a new place. The
escrow figure rides the REASONING instead, because it is usually the EXPLANATION for a real shortfall: a
1003 commonly carries principal and interest only. **A dedicated regression test
(`test_escrow_is_not_added_to_the_statements_total`) fails if anyone reintroduces the sum.**

⚠️ **NO TOLERANCE BAND, and that is a checked conclusion (ADR-361), not an omission.** No source
establishes a de-minimis difference between a stated housing payment and a servicer's billed PITIA, so
any band would be invented. The comparison is exact; any difference routes to `needs_review`.

⚠️ **Schedule E stays empty and the "or net rental" path is not faked** — 0 property rows across 15 real
tax returns, so B3-3.8-01's 75% factor is not used and is not cited.

### LO-2 — LOE completeness

| verdict | state |
|---|---|
| `satisfied` | all three legs present |
| **`needs_review`** | a leg missing on a letter whose type HAS that leg |
| `couldnt_check` | a letter present whose type carries no completeness fields, or an unrecognised signature answer |
| `not_applicable` | **no letter exists** — never a gap |

⚠️ **`incomplete` routes to `needs_review`, not `fired`, on evidence:** on the one type with the fields,
`referenced_date` fills 6/9 and `borrower_signature_present` 7/9, so an empty extracted field cannot be
distinguished from a field the extraction missed. A `fired` verdict would assert a defect on a letter
that may state its date on the page.

⚠️ **`applicability_expected: false` is LO-1's held question showing through** — declaring a missing
letter a gap needs the list of conditions that REQUIRE one, which nothing in the file enumerates.

**A new scope predicate was needed:** `loe.is_explanation_letter` materialises on EVERY document
(`yes`/`no`/`unknown`), because the applicability DSL supports only `eq`/`ne` while LO-2's scope is eight
document types. Gating on one type would silently drop seven; gating on the completeness tag itself would
resolve every non-letter to `couldnt_check`. **`input_resolves: true`** — the predicate resolves on
**98/98 documents across 8 active loan files** (96 `no`, 2 `yes`); `loe.completeness` resolves on both
letters.

⚠️ **`borrower_signature_present` is a TYPED extractor field**, so the catalog's *"signature (AI for
scans)"* is **stale — LP-487's question answering yes a sixth time. REPORTED, NOT RE-KINDED**; a test
pins both that the field is typed and that the stale row is still there, so a silent re-kind fails.

## Proven vs assumed

**Proven by test through a real `materialize_tags()` → `evaluate_rules()` path** (never a recipe or the
gate called directly — LP-508's lesson): all 32 assertions across the two new test files, including every
verdict above, the escrow double-count guard, the two retention orphans staying unwired, the one-matcher
property, and "no letter" ≠ "unreadable letter".

**Proven on real stored data** (19 active loan files, PII read in place, aggregate counts only): every
`input_resolves` claim above.

**Assumed:** that the 2 `disclosed` matches on real data are correct matches — the names agree under the
token-prefix rule, but no human has confirmed the two obligations are the same debt. This is the known
false-satisfied direction IH-2 already accepts and documents.

## LF-6T3N verdict distribution — intended movement vs regression

`745 → 835` evaluations (+90 = 3 per-document rules × 30 documents).

- `couldnt_check` 259 → **275** (+16): RE-1 ×8 and DT-6 ×8 — the 4 real `mortgage_statement`s (each states
  **no lender name**) plus the 4 `unknown`-type documents, which cannot be ruled out as statements.
- `not_applicable` 459 → **533** (+74): RE-1 ×22 + DT-6 ×22 (classified non-statements) + LO-2 ×30
  (LF-6T3N carries no letter of any of the 8 types).
- ⚠️ **`satisfied` 21 · `fired` 2 · `needs_review` 4 — ALL UNCHANGED.** Three rules activated and no
  existing verdict moved.

⚠️ **The fail-closed direction working on the flagship fixture:** LF-6T3N carries **four** mortgage
statements and its MISMO states **no liabilities at all**, and RE-1 abstains on every one rather than
reporting four undisclosed mortgages. A matcher that read "no stated side" as "nothing disclosed" would
produce 4 false `needs_review` here.

**Regression:** `documents` section md5 `61395820223e644413d69f7f7ec2c3a5` (46836 bytes) and AS-1 / AS-2 /
AS-12 / IN-12 subject-verdict digests are **byte-identical** across a clean tree (`git stash -u`,
untracked new specs stashed too) and this branch.

## LO-1 — held, and the reason is architectural

LO-1 asks *which conditions REQUIRE an LOE*. That list is **lender- and AUS-driven and no document in the
file enumerates it**. The only available source is the run's own findings — and reading them would make
**LO-1 a META-RULE over other rules' output, which nothing in the architecture does**: every rule today
reads fact-tags produced from documents and MISMO, never another rule's verdict. That is an ADR-sized
change to the engine's shape, not a build step, so LO-1 is held and the question is logged for Priya.

## Model spend

**$0.** No model call has been made this ticket. The corpus work read already-extracted bench records in
place; the 303-document bench was **not** re-run.
