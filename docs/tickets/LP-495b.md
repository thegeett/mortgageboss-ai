# LP-495b — Judgmental: IN-13 · IN-14 · OC-3 · DT-7

Created in the first step (standing policy). Phase A only — nothing built, nothing changed in the rule
engine. Branch `phase3_bucket_2_fast` (LP-496 established `phase3_bucket_2` is a stale ancestor).

**Model spend: $0.** No model call. Network used to verify published guidance; results below.

---

# PHASE A — stop and report

Two of the ticket's premises are wrong, in opposite directions: the tag believed to have no producer has
one, and the rule believed buildable has no producer at all. The empirical blockers are real and are the
same for three of the four rules.

## Headline

| rule | spec | tags produce | trigger present in corpus | Phase A verdict |
|---|---|---|---|---|
| **IN-13** | exists | yes | **no** — 0 other-income documents | build/keep, hold on corpus |
| **IN-14** | exists | **yes** (bar is stale) | **no** — 0 rental docs on any file | keep, correct the bar, hold |
| **OC-3** | none | **yes** — producer exists | **no** — 0 investment properties | build, hold. **Not a missing-producer drop** |
| **DT-7** | none | **no producer at all** | n/a | **hold** — and the scope question below |

## A1 — `occupancy.rental_support` HAS a producer. The ticket's premise is wrong, and so is IN-14's bar.

```yaml
# tag_production.yaml:253
occupancy.rental_support: {mode: ai, subject: loan, data: occupancy_rental}
```

Added by **LP-418** (`2a12b83`, "the producer batch — build producers that unblock inert rules"), whose
own comment says it *"Unblocks IN-14 … and OC-3"*. The `occupancy_rental` AI group exists at
`tag_production.yaml:1196` with a full prompt naming leases, Schedule E and Form 1007/1025.

**Where the ticket's belief came from:** IN-14's activation bar still says

> `occupancy.rental_support has NO declared producer (not in tag_production.yaml) — it does not materialize.`

That was true when written and **LP-418 fixed the tag without updating the bar.** This is the fifth
instance of one pattern now recorded across four tickets — a claim that reads as established because it
was written down once and never re-derived:

| | the claim | what nobody re-checked |
|---|---|---|
| LP-494 / CO-3 | "IH-7 covers it" | IH-7's header excludes fidelity |
| LP-495a / DT-6+RE-1 | "four independent searches" | none queried the stated side |
| LP-495a / LO-2 | "9/34 fill" | the denominator spans types that cannot produce it |
| LP-496 / PR-1 | "MI-1 and PR-1, both live" | PR-1 has no spec |
| **LP-495b / OC-3** | **"rental_support has no producer"** | **LP-418 added one; the bar was never updated** |

**So OC-3 is NOT in PC-1's position.** Nothing needs wiring to an invented source.

**The harder question, and it is the real blocker.** Measured on stored data:

```
Property rows: 24
  occupancy_type: primary_residence 22 · null 2 · INVESTMENT 0
  property_type:  single_family 5 · null 19
```

**There is not one investment-occupancy property in the corpus.** OC-3 triggers on investment occupancy,
so its trigger is unexercised — the rule can be built and would resolve `n/a` on every file. That is a
different finding from a missing tag and is recorded as such (ADR-330's vacuity shape, not ADR-289's
orphan shape).

## A2 — `dti.atr_factors_documented` is a two-value enum, confirmed. But that is not what blocks DT-7.

`fact_tags.csv:66`:

```
dti.atr_factors_documented,loan,enum,"[""complete"", ""incomplete""]",Are all ATR factors documented.,AI,DT-7,AI-at-rule-time
```

**No abstain value, as the ticket suspected.** Two corrections to what follows from that:

**1. The runtime CAN abstain, and there is a working precedent.** `ai.py::_build_tag` coerces any
off-vocabulary answer — including a literal `"unknown"` — to an unknown-valued tag that keeps the model's
reasoning, and the gate routes it to `couldnt_check`. `txn.is_nsf_or_overdraft` is declared
`["yes","no"]` and its prompt explicitly offers `unknown` with instructions on when to use it; LP-418
recorded the gap as **value-space drift** to reconcile at the next `docs/snapshot-fact-tags.xlsx` pass,
because the generated `fact_tags.csv` cannot be hand-edited. DT-7's enum is the same shape.

The honest caveat: this makes the abstain path work **by coercion rather than by declaration**. The
prompt must sanction `unknown` explicitly, or the model is offered only two answers and will pick one.
That is a prompt-shape requirement, not a blocker — and it must not be met by mapping a low-confidence
answer onto `incomplete`.

**2. The actual blocker is that DT-7 has NO PRODUCER.** `dti.atr_factors_documented` appears in
`fact_tags.csv` and `rule_tags.csv` and has **no `tag_production.yaml` entry and no AI group** — the
ADR-289 vocabulary-orphan shape. LP-405 already recorded it as *"needs-producer → needs-extractor (spans
the credit report)"*. There is also no DT-7 spec and no activation bar.

**Is ATR completeness a processor's judgment at all?** The ticket asks the right question and the answer
shapes the rule:

- ATR is a **creditor obligation** under Reg Z, assessed at underwriting. A processor assembling a file
  is not the party making the determination, and a rule that asserted it would put a compliance
  conclusion in a processor's queue.
- The buildable rule is the **presence** question: for each of the eight factors, does the file carry a
  document that would support it? That is checkable from the document set, needs no ATR determination,
  and is genuinely a processor's job.
- **Recommendation: if DT-7 is built at all, build it as document-presence per factor, and rename the
  intent in the spec so it cannot be read as the determination.** The catalog rationale ("All ATR
  factors documented — completeness judgment") already reads as presence; the tag name
  (`atr_factors_documented`) does too. It is the *rule name* ("ATR documentation completeness") that
  invites the stronger reading.

## A3 — rental data: leases exist in the bench, and reach no loan file. The forms are unmodellable.

Both sides queried, per the standing rule.

**Stored side (what rules run against) — zero:**

```
lease_agreement 0 · rental_agreement 0 · form_1007 0 · form_1025 0 · rent_roll 0
files carrying ANY rental document: 0 of 19
Schedule E PROPERTY rows across all files: 0
```

**Bench side (the raw corpus) — leases only:**

```
lease_agreement 21
form_1007 0 · form_1025 0 · rent_roll 0 · single_family_comparable_rent_schedule 0
```

**And Form 1007 / 1025 are not merely absent — they have no catalog type and no extractor**, so they
cannot be classified, extracted or attached. B3-3.8-01 names them as the market-rent source; the system
cannot represent one. Recorded as a data-model gap, not a corpus gap.

The lease extractor **does** carry `monthly_rent`, `security_deposit`, `lease_start_date`,
`lease_end_date` and both party names — so the 75% calculation is representable **from a lease**, on a
file that carries one. None does today.

## A4 — the corpus carries no "other" income. Two of the specific types cannot even be classified.

IN-13 triggers on non-employment income. Measured across the bench:

| B3-3.4 income type | catalog type | extractor | documents in bench |
|---|---|---|---|
| Social Security award | yes | yes | **0** |
| Disability award | yes | yes | **0** |
| Retirement / pension award | yes | yes | **0** |
| **Alimony** | **none** | **none** | — |
| **Child support** | **none** | **none** | — |
| Capital gains | — | — | 1099: 3 |
| Commission | yes | yes | 6 |
| Note income (cancelled checks) | yes | yes | 2 |

Two distinct findings:

- **ADR-354 shape** — three award-letter extractors exist and **zero documents of any of them** are in
  the corpus. Schema present, data absent.
- **Worse for the two types with the most specific continuance rules:** alimony and child support have
  **no catalog document type at all**, so a divorce decree or support order cannot be classified. IN-13's
  sharpest cases are unrepresentable.
- **Commission (6 docs) does not rescue IN-13** — commission is B3-3.1 employment income with a
  history requirement, not one of B3-3.4's "other sources" with a continuance test. Counting it would be
  the LO-2 denominator error again: a number that looks like coverage but ranges over the wrong set.

## A5 — the standard checks

- **Catalog rows confirmed against `rule_kinds.csv`** — all four `judgmental` / `ai_judgment`,
  `priya_validated` false, `threshold_needs_signoff` false. All four therefore ship **ratify**
  unconditionally (`judgment.py`), so ratify is the destination, not a waypoint. Row count 135, untouched.
- **Tag rows confirmed against `fact_tags.csv` + `tag_production.yaml`.** The ticket's tag table is
  accurate except for `occupancy.rental_support`'s producer (A1) and `dti.atr_factors_documented`'s
  missing producer (A2).
- **Shared tags — flagged as the ticket asks.** `income.type` and `income.has_2yr_history` are read by
  **IN-11 (ACTIVE)** and **IN-12 (ACTIVE)**; DT-1 is inert. `income.type` is produced at the **document**
  subject by the `income_amounts` group; `income.has_2yr_history` and `income.continuance_3yr` at the
  **borrower** subject by `income_stability`. Any change to either is a change to two live rules, and
  IN-12 is additionally on the byte-identical regression list.
- **Stale guide citations found, reported not fixed:** `IN-13.yaml` cites **B3-3.1-09**, `IN-14.yaml`
  cites **B3-3.1-08**, and `IN-9.yaml` cites **B3-3.1-09**. The current numbering is **B3-3.4** (other
  income) and **B3-3.8-01** (rental).
- **Vacuity (ADR-330) / CR-3 trigger test:** IN-13's and OC-3's triggers are *inferences over documents
  the corpus lacks*, not statements a document makes — the trigger exists in principle but is unobserved.
  DT-7's trigger is a determination no document states.

---

# The researched values — verified here, with the network result per item

Network available. `robots.txt` respected; one bot-block encountered and **not** circumvented.

| value | source | verified | note |
|---|---|---|---|
| Rental income **× 75%**, remaining **25%** absorbed by vacancy/maintenance | **B3-3.8-01, page dated 10/08/2025** | **verified verbatim here**, tier P | quoted below |
| **Form 1007** (one-unit) / **Form 1025** (two-to-four) | same page | **verified verbatim here** | neither exists in the data model |
| Lease supported by **two months' consecutive bank statements** or **security deposit + first full month's rent with proof of deposit** | same page | **verified verbatim here** | |
| ATR structure: eight factors, (c)(2)(i)–(viii) | 12 CFR 1026.43 | **verified**, tier P | |
| The eight factors, individually | CFPB + Cornell LII (eCFR text) | **verified verbatim here** — see below | eCFR direct **blocked**, see note |
| Rental income ≤ **30%** of qualifying income under 12 months' management experience | — | **NOT verified** — tier S, taken from this ticket | not used in any build |
| IN-13 per-type continuance table (alimony/child support 3yr, capital gains 3yr, SSA family-member 3yr, housing allowance 3yr) | B3-3.4 family | **NOT re-verified** — taken from this ticket | moot for now; no corpus instances |
| TI-6 rapid-transfer window | — | **still unobtained**, tier U | unchanged |
| LL-2026-03 condo reserve step-up | — | **still tier S** (403 on primary) | unchanged |

**Verbatim, B3-3.8-01 (page dated 10/08/2025):**

> "When current lease agreements or market rents reported on Form 1007 or Form 1025 are used, the lender
> must calculate the rental income by multiplying the gross monthly rent(s) by 75%."
> "The remaining 25% of the gross rent will be absorbed by vacancy losses and ongoing maintenance expenses."
> Lease support: "two months consecutive bank statements or electronic transfers of rental payments for
> existing lease agreements, or copies of the security deposit and first full month's rent check with
> proof of deposit for newly executed agreements."

**The eight ATR factors, verbatim.** eCFR's own URL **302-redirects to `unblock.federalregister.gov`**, a
bot-block interstitial; following it would circumvent bot protection, so it was **declined** (LP-494's
precedent). Obtained instead from the **publisher, consumerfinance.gov** (items i–v) and **Cornell LII's
eCFR text** (items vi–viii), the latter showing edition **January 10, 2025 [90 FR 2503]**:

1. "The consumer's current or reasonably expected income or assets, other than the value of the dwelling"
2. "If the creditor relies on income from the consumer's employment in determining repayment ability, the consumer's current employment status"
3. "The consumer's monthly payment on the covered transaction, calculated in accordance with paragraph (c)(5)"
4. "The consumer's monthly payment on any simultaneous loan that the creditor knows or has reason to know will be made"
5. "The consumer's monthly payment for mortgage-related obligations"
6. "The consumer's current debt obligations, alimony, and child support;"
7. "The consumer's monthly debt-to-income ratio or residual income in accordance with paragraph (c)(7) of this section; and"
8. "The consumer's credit history."

This matches the ticket's list on all eight. The direct-eCFR revision date could not be read because of
the block; the Cornell edition date is cited in its place and the substitution is recorded rather than
presented as an eCFR fetch.

---

# What survives, and the recommendation

**No rule is dropped.** Three are buildable and blocked on data; one is blocked on a producer and a
scope decision.

- **IN-13 — keep, and the per-type table is the real work.** Its spec exists, both tags produce, and its
  verdict tag already scored 5/6 against Priya's labels (LP-427). Two things hold it: `income.type` is
  unscored, and the corpus contains **no** other-income document to exercise the trigger. Building the
  per-type continuance table (rather than one blanket three-year test) is correct and is what the ticket
  asks for, but it would be built against zero instances, and its two sharpest types cannot be classified
  at all. Recommend building the table with per-type abstain, and recording `input_resolves: false`.
- **IN-14 — keep, and correct the stale bar.** `needs-producer` is factually wrong; the tag has produced
  since LP-418. The honest status is `not-calibratable-yet` with `input_resolves: false`: zero rental
  documents reach any loan file.
- **OC-3 — build, hold.** Not a missing-producer drop. Its blocker is that no investment-occupancy file
  exists, so the tag resolves `n/a` everywhere.
- **DT-7 — hold, and the scope question needs answering first.** No producer, no spec, no bar, and the
  enum needs `unknown` sanctioned in the prompt (the `txn.is_nsf_or_overdraft` pattern) even though the
  runtime can coerce one. Before any of that: **is it the presence question or the determination?** Only
  the presence reading belongs to a processor, and I recommend that reading explicitly.

**The honest summary: all four rules are limited by the same thing — the corpus contains no instance of
any of their triggers.** That is a corpus finding, not four rule findings, and it is why every one of
them would ship `input_resolves: false` on today's data.

---

# CORRECTION to Phase A — "blocked on data" was wrong, and it was mine

Geet pushed back on the framing and was right twice over.

**1. I searched the wrong tree.** Phase A's rental and other-income counts came from the bench OUTPUT
JSON (~1,300 classified documents). The raw source trees hold **2,562 files** across four directories —
`mortgageboss-batch` (944), `batch-process` (1,014), `old_batch` (304), `mortgageboss-batch-part3` (300).
I never opened three of them. That is the "never assert absence from one query" lesson, committed by the
person who wrote it down two tickets ago.

**Re-run against the raw filenames — 71 rental hits, not zero:**

```
1928_Lease_Signed · Lease_agreement_1401 · Lease_agreement_12114 · Lease_agreement_7277
Lease_renewal_9413 · Lease_renewal_12114 · Raj_Lease_Barrymore · Arizona_Lease_Agreement
Cascade_Lease_doc · Sawyer_Lease_doc · Rental_or_Lease_Agreement_Residential_Rental_Contract
1928_Lease_checks              <- B3-3.8-01's "rent check with proof of deposit"
Verification_of_Rent (x2)      <- a direct rental-support document
RentalUnitMortgageStatement    <- a rental unit's own mortgage statement
SMARTRENTS LLC tax returns     <- a rental-holding entity's returns
LOX_from_landload-_Rent_free · current-occupancy-payhoa-unit-detail
```

Bench document CONTENT also mentions `investment` (36 documents), `rental property` (7) and
`second home` (1). So the rental and investment triggers are **present in the corpus**; they are absent
only from the 19 loan files currently loaded into the database.

**What survives from Phase A's measurement:** the other-income search. Re-run across all four raw trees
for social security / disability / pension / annuity / alimony / child support / divorce / award /
retirement, the only hits are **family trust certificates**. IN-13's award-letter trigger really is
absent — that finding holds, and it held because it was the one I checked on the raw tree.

**2. "Blocked on data" is a conclusion I am not permitted to reach.** Standing instruction, recorded
2026-08-12: *build every remaining rule from own research and self-set thresholds; never defer on missing
corpus or expert validation.* Phase A recommended holding three rules on `input_resolves: false`, which
is that deferral wearing a status field. Withdrawn.

**All four rules are built, self-calibrated against a researched default threshold, and activated.** The
corpus limits what can be OBSERVED, not what can be BUILT — and it is stated per rule rather than used as
a reason to stop.

---

# BUILD — all four built and researched; two calibrated; activation deferred one step

## What is done

| rule | built | research corrected | rate measured | activated |
|---|---|---|---|---|
| **IN-13** | spec rewritten | per-type continuance table replaces one blanket 3-year test; stale B3-3.1-09 corrected | no | no |
| **IN-14** | spec + bar corrected | 75%/25% calibrated from the verified primary (was "pending Priya"); stale B3-3.1-08 corrected | no | no |
| **OC-3** | new spec | — | **1.0000, 4 cases, full 3-value spread** | deferred |
| **DT-7** | new producer + new spec | eight ATR factors verbatim | **1.0000, 4 cases, full 3-value spread** | deferred |

`ACTIVE_RULE_IDS` 67 (unchanged) · `SNAPSHOT_VERSION` 4 · `rule_kinds.csv` 135 · suite **4690 passed**
(baseline) · ruff + mypy clean · AS-1/AS-2/AS-12/IN-12 and the documents-section md5 byte-identical.

## Self-calibration: scenario fixtures, not the corpus

The corpus derivation was started and **killed before any call** once Geet challenged it. It would have
run 114 calls over 19 loan files that carry none of these triggers, returned the same abstain on every
case, and then been refused by pre-rate check 3 — $0.67 for a number already committed to the bin. It
also made the calibration depend on loan files, against standing policy.

Replaced with **constructed scenario cases that exercise each trigger**. Cost **$0.12** for the cohort,
and unlike the corpus run it produces real spreads:

**`dti.atr_factors_documented` (DT-7) — 1.0000 over 4 cases, spread {complete, incomplete, unknown}**

```
atr_complete        (6 docs covering all 8 factors)  -> complete
atr_missing_credit  (no credit report)               -> incomplete, naming factors (vi) and (viii)
atr_paystub_only                                     -> incomplete
atr_empty_file                                       -> unknown      <- never "complete"
```

**`occupancy.rental_support` (OC-3, IN-14) — 1.0000 over 4 cases, spread {adequate, inadequate, n/a}**

```
rental_lease_and_proof      (lease + receipt evidence, investment) -> adequate
rental_lease_no_proof       (lease alone, investment)              -> adequate
rental_investment_no_docs   (investment, no rental document)       -> inadequate
rental_primary_residence    (a lease on a PRIMARY residence)       -> n/a
```

Every branch reachable, and every answer correct on a case whose right answer is known by construction —
which is more than a rate alone can show.

## Three real defects the derivation found

**1. The loan context carried no documents at all.** Printing the context (pre-rate check 1) showed
`documents=0` for both loan-subject groups. `_loan_context` returned MISMO facts only, so a prompt
telling the model to consider "a lease agreement, Schedule E, an appraisal rent schedule" was handed an
empty document set and answered from 1003 facts alone. **This is PC-5's failure shape and it has been
latent since LP-418 declared `occupancy_rental`.** Fixed with an `include_documents` opt-in defaulting
off, so live OC-2's and OC-1's `occupancy` group is byte-identical. LF-6T3N now shows 30 documents where
it showed 0.

**2. DT-7 would have returned `incomplete` on every file in existence.** The first derivation returned
`incomplete` on the deliberately-complete fixture, and the model's own reasoning named why: factors
**(iv) simultaneous loan** and **(vii) DTI or residual income** have no supporting document by nature — a
simultaneous loan that does not exist needs no document, and DTI is a calculation the lender performs.
Both derivations agreed at 1.0000 on the wrong answer, so **the rate could not have caught this; reading
the reasoning did.** The prompt now handles both factors explicitly.

**3. `occupancy_rental` mis-scoped a lease on a primary residence.** The first run disagreed on that case
(`adequate` vs `n/a`). A lease on a primary-residence file is the borrower's own housing, not rental
income they are claiming; calling it "adequate" invents a claim the file never made. The prompt now
scopes on occupancy before reading any document, and the disagreement is gone.

## Activation: OC-3 live at 68; DT-7 held on the enum gap, which turned out to be real

`ACTIVE_RULE_IDS` **67 -> 68** (OC-3). `SNAPSHOT_VERSION` 4 · `rule_kinds.csv` 135 · suite **4701
passed** · ruff + mypy clean · AS-1/AS-2/AS-12/IN-12 and the documents-section md5 byte-identical.

**DT-7 is built, measured, and held — and the ticket's Critical section was right.** Activating it turned
`test_full_run_end_to_end` red, and the mechanism is exactly the enum gap:

`dti.atr_factors_documented` is declared `["complete","incomplete"]` with no abstain. The prompt
sanctions `unknown` and `ai.py` coerces it to an unknown tag — but **a coerced tag carries
`confidence=None`, which is precisely the fail-closed marker the orchestrator's degradation scan matches
on.** So every run in which ATR cannot be determined is flagged `degraded`. Degradation is meant to mean
a broken pipeline, not a legitimate abstain.

**I had talked myself past this on a precedent I never checked was exercised.** `txn.is_nsf_or_overdraft`
has the same shape, and I treated it as validation — but **AS-7, the rule that reads it, is INERT**, so
that pattern has never run through the orchestrator. Same failure as everything else this session: a
written-down claim taken as established without re-deriving it.

**It unblocks when the tag's declared vocabulary gains `unknown`** at the next
`docs/snapshot-fact-tags.xlsx` reconciliation. There is no in-repo fix: the generated `fact_tags.csv`
cannot be hand-edited, and `vocabulary_extra.yaml` refuses to shadow an xlsx tag. The measured rate is
recorded on DT-7's bar and a test pins both halves — the rule stays held, and the enum stays two-valued —
so whoever fixes the vocabulary sees why.

**IN-13 and IN-14 remain built and held on a named reason:** their shared `income.continuance_3yr` is
underived because its group is borrower-subject and gathers only documents **attributed** to the
borrower, so its scenario fixtures need `belongs_to` set. Caught by pre-rate check 1 showing `docs=0` —
a fixture gap, not a corpus gap.

## LF-6T3N

`836 -> 837`, entirely OC-3 (loan-scoped, abstaining under the keyless stub). A **per-rule diff of the
whole distribution** before and after confirms only that one row appears and every other rule's counts
are identical: `satisfied 21 · fired 2 · needs_review 4 · not_applicable 529` unchanged. The single
movement is intended.

## Proven

- OC-3's `ratification_pending=True` proven through a real evaluation on both the yes and no paths.
- **OC-3 has no `satisfied` path at all** — across every combination of tag value and judgment value it
  routes to `needs_review`. Asserted rather than assumed, because the obvious expectation (a "yes"
  clears the rule) is wrong here by design: LP-376-B's armor means ratify is the destination.
- Never satisfied on a missing document, by code path.
- The `include_documents` fix pinned, so removing it fails loudly rather than silently blinding the model.

## Model spend

**$0.12** — two scenario derivations. The corpus derivation was killed before a single call; the
303-document bench was not re-run.

---

# IN-13 and IN-14 ACTIVATED — 68 -> 70. The blocker was not the fixtures.

`ACTIVE_RULE_IDS` **70** · `SNAPSHOT_VERSION` 4 · `rule_kinds.csv` 135 · suite **4714 passed** ·
ruff + mypy clean · AS-1/AS-2/AS-12/IN-12 and the documents-section md5 byte-identical.

## The real blocker: a producer that could not see its own documents

I had recorded IN-13/IN-14 as blocked on a *fixture* gap (borrower-attributed documents). Fixing the
fixtures exposed the actual cause:

```
income_stability.applies_to = [w2, pay_stub, voe, uniform_residential_loan_application]
```

**All four are EMPLOYMENT documents.** `income.continuance_3yr` — the verdict tag of IN-13 *"Other income
continuance"* and IN-14 *"Rental income support"* — was produced by a group that could **never** see a
Social Security award letter, a pension letter, a disability letter or a lease. The prompt asked about
continuance; the gathering delivered pay stubs.

This is the same defect class as the loan-context one found earlier in this ticket: **a prompt asking a
question about documents the gathering never delivers.** Two instances in one ticket, in two different
context builders.

**Widened** to add `social_security_award_letter`, `disability_award_letter`,
`retirement_pension_award_letter`, `lease_agreement`, `1099`. This is a **shared group** — IN-7, IN-10,
IN-11 and IN-12 are LIVE on its other three tags — so it is not a local edit. It is a **no-op on real
data, verified rather than asserted**: the corpus carries zero award letters of any kind and zero leases
on any loan file, and the LF-6T3N distribution plus the byte-identical md5 both confirm no live verdict
moved.

(`1099` had to be quoted in the YAML — unquoted it parses as an integer and the loader rejects the list.)

## The rates

All four groups derived over scenario fixtures, fresh-context with the keys reversed. **$0.12.**

| tag | rate | cases | spread |
|---|---|---|---|
| `dti.atr_factors_documented` | 1.0000 | 4 | complete · incomplete · unknown |
| `occupancy.rental_support` | 1.0000 | 4 | adequate · inadequate · n/a |
| `income.continuance_3yr` | 1.0000 | 5 | **yes · unknown only** |
| `income.type` | 1.0000 | 3 | base · other · unknown |

**What `income.continuance_3yr`'s rate does not show, stated plainly:** 4 of its 5 cases abstain, so only
**one** is answerable, and **no `no` was produced by any case**. The `child_support_ending_soon` case —
which *should* be `no`, with support ending in 14 months — returns `unknown`, because child support has
**no catalog document type at all** and the letter standing in for it falls outside the group's scope.
**The negative path is unobserved.** That is a weaker rate than the number suggests and the bar says so.

A second scoping finding: `income.type` comes from `income_amounts`, a DOCUMENT-subject group scoped to
`pay_stub / w2 / uniform_residential_loan_application`. An award letter can never produce it — **only the
1003 can state a non-employment income type**, so IN-13's trigger reaches the model through the URLA or
not at all.

## LF-6T3N

`837 -> 841`: IN-13 ×2 and IN-14 ×2 (per-borrower, two borrowers), DT-7 −1 (held). A per-rule diff of the
whole distribution confirms only those rows change. **`satisfied 21 · fired 2 · needs_review 4 ·
not_applicable 529` unchanged** — no live verdict moved, which is the evidence that widening the shared
producer was safe.

## Proven

- Ratification proven for both rules through a real evaluation, with **both seams** supplied (the
  materialization reasoners *and* the rule-time judgment reasoner — these are judgment rules and stubbing
  only the first lets the rule-time call reach the real provider).
- Neither has a `satisfied` path: LP-376-B routes every judgmental verdict to `needs_review`.
- Never satisfied on a missing document, by code path.
- IN-13's per-type table and IN-14's 75% factor pinned to the spec, so a future edit cannot re-flatten
  the table or quietly drop the citation.
- The widened `applies_to` pinned, so removing a type fails loudly rather than silently blinding the rule.

## Standing

**Live: IN-13, IN-14, OC-3.** **Held: DT-7** — built, measured (1.0000 over 4 cases), and blocked only by
its tag's two-value enum, which cannot be fixed in-repo.

One test moved rather than being weakened: `test_pending_checks_lp391` used IN-13 as its example of a
*blocked judgment rule*; DT-7 is now the only one left, and being loan-scoped it proves the
surfaces/stays-dark pair across two snapshots instead of two borrowers. That demo rule has now been IN-7,
then IN-13, then DT-7 — the rules keep going live, which is the series working.

**Model spend this ticket: $0.24 total** ($0.12 for the first cohort derivation, $0.12 for the second).
