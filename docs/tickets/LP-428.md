# LP-428 — Activate IN-8 + IN-9 on Priya's sign-off

## What this ticket is

LP-426 scored `income.voe_present` and `income.offer_letter_present` at **100% each** (12/12, two-sided, 0
disagreements) and PROPOSED 0.95 AUTO bars, `validated:false`, with the synthetic-fixture caveat recorded for
Priya to weigh. **Priya has signed off 0.95 for both.** Flipping `validated:true` clears each `calibratable-now`
gate (1.0 ≥ 0.95), so **IN-8** *(is a VOE present?)* and **IN-9** *(is an offer letter present?)* go live —
**ACTIVE 31 → 33**. Two rules only; IN-13 stays held.

## Acceptance criteria (status)

| criterion | status |
|---|---|
| both bars clean before the flip (calibratable-now, real 0.95, measured 1.0, tag named) | ✅ Phase 0 |
| eligibility reported for every bar-entry rule — proof only two activate | ✅ table below |
| `income_docs` fold-in confirmed; tags PRODUCED when the rules run (no LP-384 trap) | ✅ |
| IN-13 stays HELD (the sibling must not ride along) | ✅ asserted |
| the two `validated` flags flipped through the gate (the `_LP428_ACTIVATED` tuple) | ✅ |
| the real run on LF-6T3N + the labeling fixture, each verdict + reason | ✅ below |
| ACTIVE 31 → 33; the LP-389 invariant holds; the 31 prior rules identical | ✅ |
| her ruling AND the caveat she weighed recorded (not just 0.95) | ✅ in both bars |
| ruff + mypy + full suite green | ✅ 2887 passed |

## Priya's sign-off (recorded, not just the value)

| rule | tag | measured | her decision |
|---|---|---|---|
| **IN-8** | `income.voe_present` | 1.0 (12/12, two-sided) | **approve 0.95** |
| **IN-9** | `income.offer_letter_present` | 1.0 (12/12, two-sided) | **approve 0.95** |

**She signed KNOWING the synthetic caveat** (LP-426 recorded it in each bar for exactly this): the score is on
LP-418 SCENARIO documents with **hand-set `document_type`**, so it measures the tag's report GIVEN a correctly
classified document — **not** recognition from messy real content, and **not** a mis-classified document. Each
bar's rationale now records that her ruling weighed this synthetic-only basis (the LP-412 discipline: her ruling
recorded, not just the number). IN-9 also weighs its untested `n/a` value (outside its yes/no decision).

## Phase 0 — the eligibility check (proof only two activate)

Both bars are internally consistent: `calibratable-now` + a real threshold (0.95, not null) + `measured_accuracy`
1.0 + `load_bearing_ai_tags` naming the tag. The gate `is_eligible` over every bar-entry rule, before vs after
the flip:

- **IN-8, IN-9** — PRE: fail on `validated:false`. POST: **PASS** (validated + 1.0 ≥ 0.95).
- **AS-6** — the ONE other `calibratable-now` bar with the same 0.95/1.0 shape but `validated:false` (unsigned).
  **Stays held** — LP-428 flips only IN-8/IN-9, never every unsigned bar.
- **IN-13, OC-1, AS-4/5/7** — `not-calibratable-yet` → held. **IN-14** — `needs-producer` → held.
- Every other bar-entry rule is either already live (the 20 prior eligible candidates) or `no-ai-dependency`
  without input resolution — none moves.

Result: exactly IN-8 + IN-9 flip. **20 → 22 eligible candidates; ACTIVE 31 → 33.** The LP-389 invariant
`ACTIVE − BASE == eligible_rule_ids()` holds; the 31 previously-live rules are identical (the ordered tuple is
extended, not reordered).

## Phase 0 — the `income_docs` fold-in (no LP-384 trap)

Both tags come from the **`income_docs`** group, which only IN-8/IN-9 read. Before activation it was
PENDING-only (materialized on a throwaway snapshot for the blocked-rule check); activating IN-8/IN-9 folds it into
`_required_ai_groups()` (the LIVE set). Confirmed: `_ai_groups_for_rules(("IN-8","IN-9")) == {"income_docs"}` and
`income_docs ∈ _required_ai_groups()`. Materializing it PRODUCES both tags at the document subjects — so at rule
time the tags exist, and any `couldnt_check` is an honest DATA reason, never a missing-tag one.

## Phase 2 — the real run (reported, not predicted)

A point-in-time run of the live reasoner (non-deterministic; this is the run of record):

| fixture | IN-8 (VOE present) | IN-9 (offer letter present) |
|---|---|---|
| **LF-6T3N** | `couldnt_check` — *"no voe is in the file — request it from the borrower or originator"* | `not_applicable` — the gate; no `employment_offer_letter` document on the file |
| **labeling fixture** (docs exist) | **`satisfied` ×6** on the VOE docs (*"a valid Verification of Employment is present"*); `not_applicable` ×6 on the offer docs | **`satisfied` ×6** on the offer-letter docs; `not_applicable` ×6 on the VOE docs |

- **IN-8 on LF-6T3N — root of the `couldnt_check` NAMED:** an honest DATA reason (no VOE document on the file),
  **not** a tag-production failure. The `income_docs` fold-in produces the tag; the file simply has no VOE.
- **IN-9 on LF-6T3N:** `not_applicable` — LP-395 found no offer-letter document exists on LF-6T3N, so the
  per-document gate to `employment_offer_letter` never matches. Expected; the root is the honest document absence.
- **The satisfy/fire paths** (labeling fixture, where the documents exist): both rules `satisfied` on the
  documents they apply to, `not_applicable` elsewhere — the live path works end to end.

## The synthetic caveat both rules ship with

The 100% score is on **synthetic** LP-418 documents with hand-set `document_type` (always correct). It measures
`income_docs`' report GIVEN a correct classification — not recognition of a real VOE/offer letter from messy
content, nor a mis-classified document. Both bars carry this caveat on record; a real-file / classifier-error run
remains a follow-up (ongoing calibration, not a blocker — Priya signed weighing exactly this).

## Assumptions / decisions

- Activation goes THROUGH the gate: a `_LP428_ACTIVATED = ("IN-8","IN-9")` tuple + the two `validated:true`
  flips (the LP-412 pattern), never a hand-edit of the active set. `EXPECTED_ACTIVE_RULE_COUNT` bumped 31 → 33
  (one place, LP-407-3).
- IN-8/IN-9 are structural presence checks (deterministic) → **ships auto** per kind; below-bar/unknown routes to
  needs_review (LP-376-B).
- **Test-seam fix (in scope, not a producer change):** the keyless materialization stub emitted a blanket
  `unknown`, which is off-vocabulary for a presence enum (`voe_present` = yes|no) and coerced to a
  null-confidence fail-closed tag — spuriously flipping `run.degraded` once `income_docs` went live (applies to
  every document). The stub now emits the in-vocab honest `no` for a PRESENCE/boolean tag (a real model reads
  "this is a bank statement, not a VOE" → `no` too), while a MULTI-category enum with no honest default
  (`income.type`) keeps abstaining to `unknown` (its callers — IN-12's self-employment gate — rely on that). No
  producer/prompt/vocabulary changed.

## What remains (out of scope here)

- **The two candidate rules from LP-427** (award-letter documentation; child-support age arithmetic) — their own
  tickets; not built.
- **IN-13's two blockers** (the missing "has other income" scope gate, ADR-335; `income.type` unscored) — LP-427
  removed one (the uncalibrated verdict); IN-13 stays `not-calibratable-yet`.
- **AS-6 and the other calibratable-but-unsigned / not-calibratable-yet rules** stay held.
- **The synthetic caveat:** a real-VOE / classifier-error measurement would harden IN-8/IN-9 further.

## No ADR

Two activations through the established LP-389/LP-412 gate — no new architectural decision. The stub fix is a
test-seam correction (it makes the seam match real behaviour), not an architectural change; recorded above.
