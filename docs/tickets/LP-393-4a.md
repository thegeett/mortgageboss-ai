# LP-393-4a — Apply Priya's corrections, fix the fixture + liquidation prompt, and re-score

## What this ticket is

Priya reviewed LP-393-4's results and found they measured the WRONG thing: `liquidation_terms`' finding was
INVERTED (the AI under-restricts, not over-discounts), `is_declining` had 2 label slips, and
`same_line_of_work`'s 38% was mostly a FIXTURE gap (missing `occupation`), not a tag failure. This applies her
5 label corrections, fixes the fixture (adds `occupation`), fixes ONE prompt (`liquidation_terms` — her
precedence rule), and re-scores so bars rest on real numbers. **LP-393-4's scores are superseded.**

## Acceptance criteria (met)

- Her 5 label corrections applied EXACTLY (not re-derived); no other golden mutated.
- Every scenario borrower now carries `occupation` (unchanged per borrower where there is no job change); the
  clear-cut driving fields (wages/tax years/employers) are untouched; LF-6T3N untouched.
- The `asset_facts` prompt encodes her liquidation precedence; the other three prompts unchanged.
- Re-score reported with a before/after vs LP-393-4; a tag that does not improve as expected is reported as a
  finding.
- No rule/producer change; `ACTIVE_RULE_IDS` unchanged (20); full suite green.

## Phase 0 — report

- **Borrowers lacking `occupation` (from the fixture):** B3, B4, B5, B6, B9, B10, B12, B13, B14, B15 (only the
  intended job-change scenarios B7/B8/B11 had it). Priya's notes confirm the defect — **7 rows say
  "No occupation given"** (B3, B4, B6, B9, B10, B12, B13).
- **The liquidation prompt divergence:** the old wording defined `vested_usable` as "a vested, accessible
  retirement portion" — so the AI labeled a FULLY-vested 401(k)/Roth `vested_usable` (it UNDER-restricts). Her
  rule reserves `vested_usable` for PARTIAL vesting and puts any penalized retirement account (even fully
  vested) in `restricted`.
- **The one OPEN framing question (NOT decided here):** `has_2yr_history` B14 — contract ENDED 2026-06-30, two
  W-2s present; she labeled `no` ("currently unemployed… need a new offer letter + a paystub"). The tag asks
  HISTORY (which exists); she answered CONTINUATION. B14's label left AS SHE WROTE IT (`no`); the
  `has_2yr_history` prompt is NOT changed — it needs her explicit ruling.

## Phase 1/2/3 — what changed

- **Fixture (`income_scenarios`):** added a realistic `occupation` to every borrower that lacked one, UNCHANGED
  across that borrower's documents (no-job-change scenarios), so `same_line_of_work` is answerable. Only the
  missing occupation was added — wages / tax years / employers are exactly as built (asserted).
- **Prompt (`asset_facts` only):** `liquidation_terms` now follows her PRECEDENCE — *partial vesting →
  `vested_usable`; else penalties (401k/IRA/Roth, even fully vested) → `restricted`; else → `fully_liquid`*.
  The `has_2yr_history` / `is_declining` / `same_line_of_work` prompts are unchanged (hers confirm they're
  correct).
- **Labels (5 corrections, exactly as she listed):** `is_declining` B9 `no`→`yes`, B15 `unknown`→`no`;
  `liquidation_terms` inc-asset-4/5/6 `fully_liquid`→`restricted` (Principal 401(k) 100% vested, Schwab Roth,
  Betterment Roth). inc-asset-3 (Empower 401(k), 60% graded) stays `vested_usable`; both brokerages stay
  `fully_liquid`.

## THE RE-SCORE (real reasoners; before/after vs LP-393-4)

| tag | LP-393-4 | LP-393-4a | what changed |
|-----|----------|-----------|--------------|
| `income.is_declining` | 85% | **100% (13/13)** | the 2 label slips corrected; clear-cut passes. No prompt change. |
| `asset.liquidation_terms` | 50% | **100% (6/6)** | the prompt now encodes her precedence + the 3 labels corrected. AI = Priya exactly (restricted 3 / fully_liquid 2 / vested_usable 1). |
| `income.has_2yr_history` | 85% | **85% (11/13)** | unchanged; clear-cut passes. Disagreements: B14 (the open framing item) + B12 (her pay-stub-only-needs-a-W-2/1099 nuance). |
| `income.same_line_of_work` | 38% | **31% (4/13)** | **did NOT improve — a finding (below).** |

### The `same_line_of_work` finding — the stale-golden trap (reported, not explained away)

After the fixture fix, same_line_of_work went 38% → **31%**. Root cause: **Priya's goldens are STALE.** She
labeled those rows `unknown` ("no occupation given") on the occupation-LESS worksheet; the re-score runs the AI
on the occupation-PRESENT fixture, where it now answers `yes` ("same employer + same occupation, no job change"
— which MATCHES her stated rule "no job change → yes"). So the fixed AI is scored against her pre-fix `unknown`
labels — the number is invalid. **Fixing the fixture invalidated the labels made on the old one.** The B8
clear-cut cell also still stands (expected `no`, AI `no`, Priya `yes` — she reads warehouse→office as same
line, or as income continuity). **same_line_of_work needs a RE-LABEL round on the occupation-present
worksheet** before it can be measured — its prompt is confirmed correct; by her rule the AI's no-change→`yes`
answers are likely right, but that is her call, not a re-derivation here.

## The verdicts + which rules are ready for a bar

- **`is_declining` → calibration-passed (100%)** → **IN-10 ready for a bar** (LP-393-5, synthetic caveat).
- **`asset.liquidation_terms` → calibration-passed (100%)** → **AS-11 ready for a bar.**
- **`has_2yr_history` → calibration-passed (85%)** → **IN-11 ready for a bar**, with the B14 framing item
  flagged for Priya to settle.
- **`same_line_of_work` → needs a RE-LABEL round** (goldens stale after the fixture fix) → **IN-7 stays
  blocked** — NOT on the prompt (confirmed correct), on stale labels.

## The synthetic-data caveat (LP-393-5 must carry it)

These validate the AI's REASONING on CLEAN scenario data — NOT robustness to real-document messiness. A bar set
here must be paired with the LF-6T3N real-data result (n=2 — indicative).

## Assumptions, decisions & gaps

- **ADR-315** (revised) records her liquidation precedence rule (the AI was UNDER-restricting — LP-393-4's
  finding inverted), that same_line_of_work's 38% was a fixture defect (the prompt was right), the stale-golden
  trap, that LP-393-4's scores are superseded, and the open has_2yr_history/B14 framing question.
- **Applied her corrections EXACTLY** — the 5 cells she listed, verbatim; no other golden re-judged; her
  `unknown` / `no/unknown` left as she wrote them.
- **A NEW process gap:** a fixture fix mid-calibration stales the goldens labeled on the old fixture — re-label
  before re-scoring (the same_line_of_work case). The next step for IN-7 is a re-label round, then re-score.
