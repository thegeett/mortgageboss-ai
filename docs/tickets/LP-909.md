# LP-909 — Review and import

- **Ticket:** LP-909 · Phase 4.5 Stage 1 · API + service + the first UI in this stage
- **Date:** 2026-09-24
- **Branch:** `phase4.5-conditions`
- **Spec:** [`../phases/phase4.5-stage0-1-build-spec.md`](../phases/phase4.5-stage0-1-build-spec.md) §LP-909, §8, §11
- **Depends on:** every Stage 1 ticket — [`LP-904`](LP-904.md) (tables), [`LP-905`](LP-905.md) (upload/forward), [`LP-906`](LP-906.md) (readers), [`LP-907`](LP-907.md) (paste, enrich, fingerprint), [`LP-908`](LP-908.md) (AI split), [`LP-910`](LP-910.md) (the code map import step 4 updates)
- **Migration:** **one, and the header said otherwise until review caught it.** LP-904 did build every column, so the original "none expected" was right about columns and wrong about the ticket. Step 5's timeline entry needs a new `ActivityType` member, and those are VARCHAR + CHECK (ADR-037) — so `c4b8f1a72e95` swaps the `activity_type` constraint, listing all 34 values. Separately, `ConditionRound` gained the **model declaration** of `uq_condition_rounds_file_number`, which LP-904 created in SQL but never declared — no migration needed for that one, because a migrated database already has it (verified against the deployed definition).
- **Screens:** **all 13.** S1-01…S1-13 (Visual checks in sections 3 and 4)

## ⚠️ This ticket carries Stage 1's exit, not just its last feature

Every Visual check so far is recorded **NOT PERFORMED**, honestly, because no UI existed —
LP-905 §1/§2, LP-907 §1/§2 and LP-908 §1 all deferred theirs here. §11 does not pass until all 13 are
checked against their PNGs, and `docs/phases/phase4.5-progress.md` does not exist yet either.

So LP-909 owns: the last feature, the §8 acceptance run end to end, 13 visual checks, the progress
file, and CI green. **Do not merge** — the branch stays for review.

## Planned sections, reviewed between each

| # | Section | Status |
|---|---|---|
| 1 | read endpoints: `GET` rounds · `GET` round · `GET` conditions, with `round_numbers` | **done** — `f53edb37`, `1d9b2b43`; hardened by `878d6b5c`, `e712173d`, `218392b2` |
| 2 | import under `loan_file_needs_lock` · `PUT draft` · `POST import` · `POST discard` · `POST conditions` | **done** — `0b02531a` (service), `cbfbfddc` (review fixes), `2542d046` (the four endpoints) |
| 3 | hooks module · the Conditions tab · S1-01, S1-02, S1-03, S1-13 | **done** — foundation `f2015565`; S1-01/S1-02/S1-03 + dashboard `8c748ab2`, `61cf6c5c` (fixes `78b9d412`); the `needs_ai` orphan `6abafc75` and its review fix `0f4c9508`; the reparse route, the tab page replacing `TabPlaceholder`, and S1-13 in `23c3a53c`. **Visual checks still NOT PERFORMED** — they belong to §5 |
| 4 | the review screen and the imported list · S1-04…S1-12 | **done** — dialogs S1-06/S1-12 `a6ed62df` (fixes `7d83122f`); review screen S1-04/07/10/11 `d6335e27` (fix `f6fd206a`); imported list + strip + details sheet S1-05/08/09 `784b10e1` (fixes `48f33456`); `has_bytes` `f5636ebb`, `23106bd6`; events endpoint + History `9ac9ee85`. **Visual checks NOT PERFORMED** — §5 |
| 5 | §8 acceptance end to end · 13 Visual checks · `phase4.5-progress.md` · CI | planned |

⚠️ **This table said §2 and §3 were "planned" while both were committed**, and it stayed wrong through
several review rounds because nobody reads a status table to find out what has happened — they read
it to find out what is left, and a row that understates is invisible in a way a row that overstates
is not. Corrected when §1's status was queried directly. The commit hashes are here so the next
reader checks the claim against the log rather than against this sentence.

## What the survey establishes, before any code

**The import's two-pass match is already indexed for.** `conditions` carries
`ix_conditions_file_lender_code (loan_file_id, lender_id, lender_code)` and
`ix_conditions_file_fingerprint (loan_file_id, text_fingerprint)` — "the two lookups import does, in
the order it does them", per LP-904's own comment. The spec's order (code+fingerprint, then
fingerprint alone) and the indexes were designed together; the queries must use them rather than
scan.

**`fingerprint()` exists and is shared** (LP-907). The reader's rule-6 dedup and the stored
`text_fingerprint` normalise identically through `note_stripped`, with the notes as rule 6's third
element the only deliberate difference.

**⚠️ `round_number` is assigned here and nowhere else — and the INDEX is the mechanism, not the
lock.** An earlier draft of this line said `max + 1` "must be computed inside the lock or two
concurrent imports collide", which credits `loan_file_needs_lock` with a guarantee it does not
offer. Review found it; verified:

`loan_file_needs_lock` yields `bool(acquired)` and its own docstring says *"the caller proceeds
either way — a missed lock just means another worker is applying, and a re-run is safe"*. That is
sound for idempotent needs recomputation and is **not mutual exclusion**. All three existing call
sites (`tasks/needs.py:72`, `:92`, `services/verification_run.py:1499`) use a bare `async with` and
bind nothing, so under contention the body runs anyway and two imports can compute the same
`max + 1`.

What actually prevents two rounds sharing a number is
`uq_condition_rounds_file_number (loan_file_id, round_number) WHERE round_number IS NOT NULL AND
deleted_at IS NULL`. So **§2 must handle the unique violation** — catch `IntegrityError`, recompute,
retry — rather than assume the lock prevented it. The alternative, binding `as acquired` and
branching, would make this the repo's first such caller and needs its own justification; the
catch-and-recompute is the smaller claim and matches what the index already guarantees.

Nothing writes `round_number` today (`api/conditions.py:328` is a read), so §2 is its first writer
and this costs nothing to get right. LP-904's migration reasons carefully about *which predicate*
belongs in that index and never addresses concurrent assignment — the gap is real, not a
misreading.

**And branching could not remove the catch even if it were taken.** `loan_file_needs_lock` takes
`timeout: int = 30`, which auto-expires a **held** lock. An import running past 30 seconds — a large
sheet, a slow code-map pass — loses the lock *mid-transaction while still working*, and the next
import then proceeds legitimately. So an `as acquired` caller would still collide and would still
need the `IntegrityError` catch. The catch alone is sufficient; the branch would be load-bearing for
a guarantee it does not deliver.

**The code map has no service** — LP-910 shipped the model, the YAML seeds and
`scripts/seed_lender_codes.py::_upsert`. Import step 4 owns its own upsert: unknown `(lender, code)`
→ insert `OBSERVED_UNMAPPED`; known → `times_seen += 1`, `last_seen_at`, and apply `info_only`,
`canonical_type_id` and `default_owner_hint` only where no prefix/bucket hint was found.

⚠️ **`loan_file.lender_id` is nullable**, and `(lender, code)` is meaningless without it. A file with
no lender must still import — the conditions land, and the code-map step is skipped rather than
guessed at. That is a real state, not an edge case: LP-905 already notes a round takes the *file's*
lender and warns rather than blocks when the sheet disagrees.

**`loan_file_needs_lock` wraps the session, not the other way round** —
`async with loan_file_needs_lock(id), task_session() as db` is the established shape, and it yields
whether the lock was acquired rather than raising.

**Stage 2's event kinds do not exist.** `ConditionEventKind` has no `CONDITION_CLEARED`, no
`CONDITION_REMOVED`, no `ROUND_COMPARED` — "an enum member nothing writes is an invitation"
(ADR-404). Import may emit only `CONDITION_CREATED`, `CONDITION_SEEN_AGAIN`, `CONDITION_NOTE_ADDED`,
`CONDITION_EDITED`, `ROUND_IMPORTED`.

**The UI template is `components/file/needs/`** — a dashboard that lists and acts, with a test that
mocks the hook layer and drives the data. `frontend/lib/api/conditions.ts` does not exist; `inbound.ts`
is the nearest sibling for query keys and invalidation. The Conditions tab today is a 13-line
`TabPlaceholder`, so there is nothing to reconcile with.

## ⚠️ Two constraints this ticket inherits, already paid for

**1. The AI split cannot check boundaries, and the screen as specified cannot show the worse half.**
LP-908 measured it: a partition constrains coverage, not boundary placement, so *the correct split of
two conditions and the wrong merge of two conditions are the same shape to every check that exists*.
Of the two failure modes, one condition cut in two is **findable** on screen (two rows reading as
halves of one sentence); two conditions merged into one is **invisible** — one plausible, correctly
worded condition with a second demand inside it, nothing flagged, nothing unassigned.

§LP-909's review screen surfaces the input text nowhere except unassigned lines, so there is no view
in which boundaries are visible against the text they were drawn on. The data for one exists:
`ParsedRow.source_line_numbers` is populated and `_Coverage` knows each claimed span. **Rendering the
paste once with each row's span delimited, for AI-split rounds only**, is what turns the invisible
case visible. S1-10's Must-match list does not require it; this is the constraint arriving before the
layout is fixed rather than after.

**2. A `PARSING` round can still strand.** LP-905 recorded it and LP-908 narrowed it on the paste
door only (a failed enqueue marks `PARSE_FAILED`). The window is narrowed, not closed — the
`PARSE_FAILED` write is a second transaction — and the upload and forward doors are deliberately
unmitigated, each carrying a comment saying so. S1-02 must poll and S1-03 must offer a retry, because
neither is decoration.

### ⚠️ Discarded rounds are listed, and §2's discard endpoint owes two answers

`list_rounds` includes `DISCARDED` on purpose — a processor who threw a draft away should see that
they did, and a round silently vanishing reads as data loss. Keeping it forces two questions the
spec does not settle, and §2 is where they are decided rather than discovered:

1. **A discarded DRAFT has `round_number` NULL**, while the round strip card is specified as
   "number, date, source chips, completeness, counts". The strip must render a numberless card, and
   nothing says how.
2. **Discarding an IMPORTED round is a real state**, argued explicitly in LP-904's migration: such a
   round *keeps* its number, stays in the unique index, and its `ROUND_IMPORTED` event survives
   forever because the table is append-only. Combined with §LP-909's "never delete, never clear",
   discarding an imported round leaves **every one of its conditions alive and still chipped to
   it** — so "discarded" would mean the round is out of the workflow while its conditions remain.
   That is defensible, and it is a different meaning from a discarded draft, shown in the same list
   under one word.

So §2's discard endpoint must state whether an IMPORTED round is discardable at all, and if it is,
what becomes of its conditions — because the migration already guarantees they survive.

### ⚠️ The invariant belongs on the WRITERS, not on the read side

Review found that `condition_enrich.py::_merge_conditions` created conditions and emitted no
`CONDITION_CREATED` — so an enrich-added condition had empty `round_numbers` (no chips) while its own
NOT NULL `first_round_id` and `last_seen_round_id` both pointed at the round that created it, and the
round's count was short by exactly `result.added`. Not incidental: `_merge_conditions` runs *only*
when `status is IMPORTED`, and `rows_on_sheet` reads the event-derived count *only* when
`status is IMPORTED`, so the only path creating eventless conditions is the only path whose count
comes from events. Fixed in the enrich service, with the test that pins it.

**The general lesson is sharper than the bug.** A read side that derives a fact from events is only
as good as its enumeration of the writers — and the first version of §1 was correct about import and
blind to enrich. That is the same family as a field with no producer at all, one layer up:
`text_fingerprint` had none; `round_numbers` and `condition_count` had none; this would have had one
that covered half its writers.

So the rule is stated where it binds: **every writer of a `Condition` emits `CONDITION_CREATED`.**
There are two today (import in §2, enrich in LP-907) and §2 adds the third.

**⚠️ The third writer needs a decision, not a default.** `POST /api/loan-files/{id}/conditions` adds
one by hand into the latest imported round, and the spec does not say which event it writes. Emitting
nothing reproduces exactly the bug above. Emitting `CONDITION_CREATED` gives it a round chip for a
sheet **it was never printed on**, which is a different lie. `origin: ConditionOrigin` (`SHEET` /
`MANUAL`) already exists on the model and is the column that should decide: a manual condition is
real and belongs to the file, but it did not *appear on a sheet*, so the chips must be able to say
so. Settled in §2, not discovered in §4.

### The tenant gate is enumerated, not declared per handler

`ScopedLoanFileById` replaced the same six lines of hand-scoping in both existing POSTs, and the
three new reads declare it. But **a dependency each handler must declare is still a check that can be
forgotten** — the fourth read added in Stage 2 is the one that ships open, and every existing refusal
test passes, because they test the routes that exist.

So `tests/api/test_condition_route_gating.py` walks both routers and asserts every route declares one
of the two gates, detected by **dependency identity** (`dependency.call is ...`) rather than by name
or by reading source. The shape is `test_lender_endpoints_lp813.py`'s admin-gate test.

**⚠️ One deliberate inversion of that template.** The lenders version checks only
`methods - {"GET"}` and skips routes that mutate nothing — right there, because `GET /lenders` is
deliberately open. Here it is backwards: §1 adds three GET reads, and *an ungated GET is exactly what
leaks another tenant's data*. Copying the template unchanged would have passed while gating nothing.
There is no GET exemption.

Two accepted dependencies, not one: file-scoped paths gate on the file, the flat
`/condition-rounds/{round_id}` paths on the round — a flat route carrying `ScopedLoanFileById` would
be wrong rather than right, since it has no file id to scope.

Proven by mutation rather than assumed: adding an ungated GET to the real router fails **3 of 6**,
and swapping the flat route onto the file gate fails 1.

### Two single points of failure, recorded rather than discovered later

Review's independent mutation matrix showed what the counts do not:

- **`test_a_page_crossing_row_cites_both_pages` is the only guard for the Champions tail bug.** It
  fails under the y-coordinate mutation alongside two others, but under `range(len(leftover))` it is
  the *sole* failure. If this file is ever trimmed, that is the test to protect.
- **The enrich event's two distinct regressions share one assertion.** "Wrong event kind" and "no
  event at all" are both caught only by
  `test_a_condition_the_pdf_adds_to_an_imported_round_records_that_it_appeared`. That is the correct
  property and one correct assertion beats three vague ones — but it is a single point of failure,
  noted deliberately.

### ⚠️ Constraints on §2's `IntegrityError` retry, before it is written

This will be **the repo's first caught `IntegrityError`** — every existing mention is a comment
explaining why a pre-check avoids one (`lenders.py:62`) or narrating a race where the loser absorbs
it into a failed state (`document_processing.py:244`, `documents.py:438/720`). Neither is this shape:
mine must catch, recompute and *succeed*. So there is no precedent to copy and none to contradict,
and the shape stays minimal rather than becoming a retry framework.

Two things that must be right, the first from review:

1. **The savepoint has to be rolled back before any further statement on that session.** After the
   violation, asyncpg raises `InFailedSQLTransaction` on the *next* statement — so a recompute
   issued without unwinding the savepoint never runs at all, and the failure would look like the
   retry silently not happening. `db.begin_nested()` is this repo's savepoint idiom (eight call
   sites; `verification_run.py:1499` pairs it with the needs lock exactly as the import will).
2. **The retry bound is stated out loud.** `max + 1` recomputed under contention can lose twice, so
   "retry until it works" is not a bound. A small fixed number, and a typed failure when it is
   exhausted — never a silent give-up, and never an unbounded loop holding a lock that
   auto-expires at 30 seconds anyway. An unbounded retry there would outlive its own mutual
   exclusion, which is the failure it exists to prevent.

3. **⚠️ A savepoint rollback EXPIRES every object that was dirty when it was taken** — and this is
   the half that bites, because the symptom appears a line away from the code you were thinking
   about. Measured on this database rather than reasoned about:

   ```
   savepoint taken → object mutated → IntegrityError → savepoint.rollback()
   reading an attribute off that object → MissingGreenlet: greenlet_spawn has not been called
   after `await db.refresh(obj)`            → the attribute reads correctly again
   ```

   In async SQLAlchemy the expired attribute is a lazy sync load, so it *raises* instead of
   reloading. A retry that sets `round_.round_number`, flushes, catches and then reads anything off
   `round_` fails with a greenlet error that looks nothing like a unique violation.

   `finding_impact.py:141-156` is the shape to copy, and it uses the **explicit**
   `savepoint = await db.begin_nested()` / `await savepoint.rollback()` form rather than the context
   manager — which matters here, because the `async with` form rolls back *and re-raises*, and a
   retry needs to roll back and continue on the same session. Its comment states the rest: "The
   savepoint rollback reverted the DB AND expired the objects it mutated. Reload them (an awaited
   refresh, not a lazy sync access)."

   `verification_rules.py:151-156` states the general rule as bug-006's lesson: anything left
   unflushed must be settled **before** the savepoint rather than left inside the window the
   rollback discards — "a savepoint added to protect the commit being the one thing that could
   destroy it", which is a fair description of this retry's risk.

   So §2 takes the savepoint around **only** the insert that can violate, settles everything else
   first, and recomputes `max(round_number)` with a **query** rather than from an object that was in
   the window.

   ⚠️ **My own first probe measured the wrong half.** It issued `select(1)` and a fresh insert after
   the rollback, both of which succeed, and would have supported "the session is fine" — the
   conclusion that matters is about objects dirty *inside* the window, which it never touched.

### ⚠️ The timeline entry is not free either

Spec step 5 wants "Conditions imported: N new, M seen again", and `ActivityType` has only
`CONDITION_SHEET_RECEIVED`. Adding `CONDITION_IMPORTED` is VARCHAR + CHECK (ADR-037), so it needs a
**constraint-swap migration** — and `tests/test_activity_type_migrations.py` enforces that the swap
applied LAST lists the entire enum, because a swap DROPs the constraint and recreates it from its own
tuple. A value present in an earlier swap and omitted by a later one is revoked. LP-905's
`e5a2c7f31b84` is the precedent.

That guard exists because LP-637 added a member with no migration at all and the suite stayed green:
`conftest` builds the schema with `create_all`, which regenerates the CHECK from the current enum, so
the test database always agrees with the code whatever the migrations say.

## Open questions to settle in section 2, not later

- **A draft row edited on the review screen changes its fingerprint.** The spec's frontend test is
  "editing a row and importing sends the edited text", so an edited row imports as the processor
  wrote it — and therefore may match a different condition, or none. That is correct and worth
  stating: the fingerprint is of what is imported, not of what was read.
- **`possible_match` on a same-code/different-fingerprint collision** goes in the event detail for
  Stage 2 to interpret. It is an id, not a decision.
- **Optimistic concurrency on `PUT draft`** uses `expected_updated_at`, already on
  `ConditionDraftUpdate`. There is no other precedent in this repo — `draft-conflict.ts` is a
  different mechanism (a 409 carrying a whole plan), so this one needs its own conflict shape.

### Decisions taken while writing §2's import service

Recorded here because each was a choice the spec does not make, and a choice nobody writes down
becomes a thing the next person assumes was forced.

1. **An unknown code's `label` is the code itself.** `label` is NOT NULL — the spec lists it without
   the `?` that marks `canonical_type_id?`, `default_bucket_kind?` and `default_owner_hint?` optional
   — but an unknown code has no meaning by definition; that is what `OBSERVED_UNMAPPED` *says*. The
   alternatives are worse: the row's `lender_category` is the lender's category for the CONDITION,
   not a meaning for the CODE, and storing it would read as a mapping somebody made; an empty string
   reads as missing data rather than as absent meaning. The code is at least true, and `status` is
   what the review queue reads.
2. **A round with no rows is refused, not imported.** Importing nothing would consume a round number
   and leave a round claiming to be the lender's list while holding none of it. A processor who
   deleted every row on the review screen wants a discard, which has its own endpoint and its own
   event.
3. **`default_bucket_kind` is deliberately NOT applied.** Spec step 4 names three things to apply —
   `info_only`, `canonical_type_id` and the owner hint — and the bucket is not among them. The
   heading printed on the sheet is the lender's own statement of when the condition is due; a default
   from the code map would override the specific with the general.
4. **Event details stay counts, codes and names — no wording.** This one is a genuine tension rather
   than a clear rule. `ConditionEvent.detail` is classified NPI and dropped whole from the readonly
   views, and the model's own docstring says it holds "what CHANGED: an edited wording, a note that
   was appended" — so text here would be *permitted*. But the sibling writer on the same table
   (`condition_enrich.py`) keeps every detail to counts and codes, and one table written two ways is
   how a rule stops being a rule. Followed the sibling; noted so a later ticket can overrule it
   deliberately rather than by accident.
5. **The `IntegrityError` catch names its constraint.** Measured rather than assumed: `exc.orig`
   carries no `constraint_name` at all — it is on `exc.orig.__cause__`, the asyncpg
   `UniqueViolationError`. Reading the obvious attribute returns `None` and the comparison is quietly
   always False, so the retry would never fire while reading as though it did.
6. **Both match passes are lender-scoped, and `None` is not a lender.** Spec step 2 scopes the search
   to "the same file **and lender**" and that clause governs both sub-bullets. `by_code` was scoped by
   construction; `by_text` keyed the fingerprint alone, so a condition belonging to lender A matched
   lender B's sheet whenever the wording was identical — silently. Found in review, reproduced here:
   `created=0 seen_again=1`, one condition owned by lender A carrying lender B's round chip. The same
   import disagreed with itself three ways, since the code map recorded `(lender B, code)` as a new
   OBSERVED_UNMAPPED entry in the transaction where the wording pass called the condition identical.

   ⚠️ **The obvious fix is wrong, which is why this is a decision and not just a patch.** Strict
   equality would duplicate the demand the moment a file's lender was set, because
   `loan_file.lender_id` is nullable and mutable and a round takes the FILE's lender — so conditions
   legitimately exist under `None`. `None` means "not known yet", never "a different lender". The
   rule is therefore: match when either side is `None` or the two are equal; never across two
   DIFFERENT known lenders.

   ⚠️ **And matching across `None` is only half of it.** Measured: a lender-less round followed by a
   lender round matched correctly and left `lender_id = None` *permanently*, so pass 1 could never
   find that condition by `(lender, code)` again and it depended on identical wording forever. A
   match that adopts the lender is what stops the benign path degrading into the fragile one.
7. **The lock is taken in the handler so it spans the commit — and it is still not mutual exclusion.**
   An `async with loan_file_needs_lock(...)` inside `import_round` would release when the service
   returned, *before* the handler commits, leaving the commit outside the window the lock exists to
   cover. So `import` and `POST conditions` take it at the boundary; it is the repo's first
   handler-level use, every other call site being a task or a service that owns its transaction.

   ⚠️ **Nothing may read that as "the race is now closed".** The lock yields `bool(acquired)`, every
   call site in this repo binds nothing and proceeds either way, and `timeout=30` auto-expires a HELD
   lock, so a slow import loses it mid-transaction while still working.
   `uq_condition_rounds_file_number` is the mechanism and the `IntegrityError` retry is what makes
   concurrency safe; the lock is defence in depth. Written down because a later reader seeing "the
   lock now covers the commit" could reasonably conclude the retry is belt-and-braces and remove it.
8. **Refusing to discard an IMPORTED round leaves one of LP-904's justifications describing a state
   the product can no longer reach.** That migration deliberately omits a status term from
   `uq_condition_rounds_file_number`'s predicate and justifies it thus: an imported round later
   discarded *keeps* its number, because `condition_events` is append-only and its `ROUND_IMPORTED`
   event survives forever, so releasing the number would leave two different sheets both recorded as
   "round 2" in an immutable history.

   That reasoning is still correct. But §2's discard endpoint refuses exactly that transition, so the
   state the predicate defends against is now reachable only by direct database manipulation — or by
   a Stage 2 verb that does not exist yet.

   The predicate stays: a constraint that also holds under manual intervention is the right kind, and
   Stage 2 may well add the verb. What is recorded here is that its stated justification and the
   API's behaviour now describe different worlds — the sort of pair that drifts silently until
   somebody reads the migration and concludes it is defending a case the product cannot create.

### ⚠️ One shape, four times: the assertion is downstream of the property, not of its negation

Four times now in this ticket — the fourth being inside the fix for the third, detailed below — a
test has been named for a property while asserting something strictly weaker that is true *whenever
the property holds*, so nothing distinguishes the property from its absence:

* **the reach assertion that could not fail** — `should_reach` computed after the walk it was meant
  to check;
* **the exemption test that never invoked the walk** — an entry silenced one test and the other
  asserted unconditionally;
* **`test_the_retry_is_bounded`, blind to the bound** — it asserted only that a refusal carrying
  "Try again" came out, which is equally true of 3 attempts, 25, or 250. Measured at
  `MAX_NUMBER_ATTEMPTS = 25`: still green, and the only failure anywhere came from a *different*
  test noticing incidentally at a bound of 1.

The uncaught direction is consistently the harmful one: a vanished bound is loud, an inflated bound
is silent and outlives the advisory lock it is holding. The fix each time is to assert the negation —
count the attempts, compute the expectation before the thing under test runs — rather than to add
more assertions of the same kind.

⚠️ **And the corrected assertion immediately found something, which is the argument for this whole
class.** `assert len(calls) == MAX_NUMBER_ATTEMPTS` failed `4 == 3` on its first run — not because
the bound was wrong (the log shows exactly three attempts) but because `_next_round_number` ran once
before the loop and again after *each* failure, including the last one. So the final failed attempt
issued a query for a number nobody would ever use, while holding the lock the bound exists to
protect.

Two things were wrong and only one was the test's: the measurement counted computations while
claiming to count attempts, and the code did one computation more than it had attempts. Recomputing
at the top of a retry rather than in the `except` branch fixes both — the dead query goes, and the
two counts become the same number, so the test states the property instead of an off-by-one
restatement of it. Asserting `== 4` would have passed, preserved the wasted round trip, and encoded
the off-by-one as though it were intended.

#### ⚠️ And then the corrected assertion failed the same way — a fourth instance, in the fix itself

`assert len(calls) == MAX_NUMBER_ATTEMPTS` **references the constant it is checking**, so it is true
for any value of it. Re-measured after the correction: `MAX_NUMBER_ATTEMPTS = 25` left all 27 tests
green, including this one. The fix reproduced the defect it was fixing, one level down, in a file
whose ticket section already described the class.

Both obvious repairs are known-bad here. `== 3` is the guessed constant this ticket was already
corrected for (`e712173d`, "I replaced a guessed constant with a tautology and called it rigour"),
and it fails when someone legitimately moves 3 to 4. The constant itself is the tautology above.

So the test asserts the property the number exists to serve: **a ceiling**,
`MAX_NUMBER_ATTEMPTS <= 5`, justified by the 30-second advisory lock the retry holds — one attempt
is a flush plus a query, so a handful finishes well inside it and a hundred does not. It survives a
legitimate change and fails when the bound stops bounding.

The general lesson, sharper than the bug: **an assertion that names the thing it is checking cannot
falsify it.** Comparing a measurement to the constant that produced it tests the plumbing, not the
value — the same error as reading a status code from a pipeline that swallowed the real one.

#### ⚠️ One mutation is NOT caught, and it is recorded rather than papered over

Removing the `ORDER BY` from `_existing_conditions` leaves all 27 tests green.
`test_possible_match_names_the_oldest_condition_under_a_shared_code` does not catch it, and would not
reliably catch it however it were written: the clause removes a *determinism guarantee*, and with two
rows Postgres returns insertion order anyway. A behavioural test cannot observe nondeterminism in a
single run without forcing planner conditions a test has no business forcing.

So that ordering is currently protected by argument and not by a test. Said plainly here because a
green mutation is easy to read as coverage, and this one is its absence.

## Section 3 — the hooks module and the screens before review

Delivered: `lib/types/conditions.ts`, `lib/api/conditions.ts`, `CONDITION_ROUND_STATUS`, the
cross-stack type guard, and three screens — S1-01 (empty state), S1-02 (reading) and S1-03 (read
failed). Mutation-checked, each mutation breaking exactly one test:

    refusal sentence paraphrased            -> 1 of 13 fails
    a stranded round never recognised       -> 1 of 13 fails
    reader clause printed when none ran     -> 1 of 13 fails
    Try again offered when impossible       -> 1 of 13 fails

### ⚠️ S1-03 asks for three things the system cannot supply

Approved as a deviation by the product owner: **build the screen for the failures that actually
exist.** The three mismatches, each verified in the code rather than assumed:

1. **The scenario it depicts does not fail.** S1-03 shows a PDF that "has text, but no condition rows
   or lender headings". The parse task settles *unconditionally* to `DRAFT` with whatever rows it
   found (`tasks/conditions.py:192-214`) — there is no emptiness check anywhere — so that PDF becomes
   a draft holding ZERO rows, not a failure. The real failure kinds are `unreadable`, `no_text`,
   `bytes_unavailable`, `ai_unavailable`, `enqueue_failed` and the base `parse_failed`.
2. **`NO_CONDITIONS_FOUND` does not exist.** No such `failure_kind` is written anywhere.
3. **The readers-tried list does not exist.** `parse_report.reader` is singular and is written `None`
   on the failure path. Rendering "reader uwm v1, champions v1, generic v1" would be inventing
   provenance, which is a worse failure than showing less.

And a fourth, found while wiring it: **"Try again" has no endpoint.** `parse_condition_round.delay()`
is called from exactly two places, both creation paths (`api/conditions.py:272`, `api/inbound.py:352`).
A round that has already failed cannot be re-read through the API at all. `RoundFailed` therefore
takes `onRetry` as OPTIONAL and the dashboard passes none — a button that cannot work is worse than
an absent one, because it spends a processor's attention and returns nothing.

### ⚠️ The state with no screen: a draft holding zero rows

This is what the S1-03 scenario really produces, and nothing in the design pack covers it. A
processor who uploads the wrong PDF gets a *successful* round with nothing in it. Left unhandled it
renders as an empty review screen, which reads as a bug rather than as "this is not a condition
sheet". Handled in §3 because it is the honest destination of the case S1-03 was drawn for.

### Two decisions about which primitive to build from

**`EmptyState` is not used for S1-01.** Its `children` render inside a `<p>` capped at `max-w-xs` and
its `action` is documented as "the one action that fills it". S1-01 is four cards in a 2x2 grid, one
holding a drop zone — a grid of interactive cards inside a paragraph is invalid markup and fights a
width written for a sentence. Bending a one-action component into a four-action screen is the misuse
its own docstring warns about.

**Containers are built from `Card`, and the radius is guarded at the primitives rather than by a tree
scan.** Review proposed scanning for `rounded-md` on containers. Measured first: 73 `rounded-md`, 76
`rounded-lg`, and 51 of the 73 on a line that also names a container signal — but reading them shows
inputs and textareas, which are CONTROLS and correctly 5px. The verdict depends on what the element
IS, and no regex over class strings knows that; a guard with that false-positive rate lets someone
say "scanned and clean" when the scan cannot tell. `design-tokens.test.ts` can scan the tree because
a Tailwind palette scale is wrong *unconditionally*. Radius has no such property.

So `components/ui/radius.test.ts` pins the two lines everything else inherits — `card.tsx` at
`rounded-lg`, `button.tsx` at `rounded-md` — because "built from Card" is only correct by
construction while those hold, and one edit would silently give every card in the app the control
radius with every other test green. Mutation-proven: giving Card the control radius fails 1 of 3.

### ⚠️ Backend CI did not run the guard that watches the frontend

`test_condition_type_mirror.py` compares the TypeScript unions to the backend enums, but
`backend-ci.yml` triggered only on `backend/**`. A commit adding a member to a TS union starts no
backend workflow, so the guard was dark in exactly the direction §3 and §4 generate — and the suite
would be green because it did not run rather than because it passed. `frontend/lib/types/conditions.ts`
is now registered in both `paths:` lists, following the convention `frontend-ci.yml` has carried
since LP-900, with the same mapping comment.

### Visual checks: what was and was not done

⚠️ **The screenshots specified by the design README were NOT taken, and this is not a Visual check in
its own terms.** The procedure is: open the state at 1600px, screenshot it, place it beside the PNG.
There is no browser in this environment and the README explicitly forbids adding Playwright for it.

What was done instead: each screen was built against its **Must match** list, and the exact wording
was taken from the HTML mockups rather than transcribed from the PNGs — which is why the refusal
sentences, the four step names and the empty state's copy are verbatim and pinned by tests. That is a
weaker claim than "matches the PNG" and is recorded as the weaker one. A final pass by someone with a
browser is still owed.

### ⚠️ And the index the retry depends on did not exist in any test database

Found while measuring the above, and larger than the thing being measured.
`uq_condition_rounds_file_number` is created by raw SQL in LP-904's migration and was never declared
in `ConditionRound.__table_args__`. The suite builds its schema with `Base.metadata.create_all`, so
for the whole of Stage 1 two rounds numbered 2 on one file inserted cleanly in every test.

That is the mirror of the trap LP-904 documented and then fell into — its own ticket says `str_enum`'s
CHECK "only materialises through `Base.metadata.create_all` … precisely why an enum/database mismatch
is invisible to it". An index living only in a migration is invisible the same way, in the other
direction. `lender_contacts` does both halves and is the pattern followed.

No migration: the deployed definition was read back and matches the declaration exactly. The guard is
`tests/models/test_condition_round_number_uniqueness.py`, behavioural rather than a metadata
assertion — asserting the `Index` object is present would pass against a declaration that never
reaches the database, which is the failure being guarded.

### ⚠️ A state that was permanent on two of three doors — closed in `6abafc75`

**Read the section below as the diagnosis, not as the current state.** The gap it describes was real
and is fixed; it is kept because what it cost to find is the more useful record.

What changed: `parse_round` now branches on `needs_ai` and queues the split whatever door the sheet
arrived by. Three things had to be true together, and only the first is the one anybody would think
to write — the second is what made the "fix" a different dead end, and the third is what keeps the
split's own guard from missing:

1. the enqueue happens for every door, and only `if settled`, so a redelivery that lost the
   compare-and-set does not queue a second split;
2. `raw_text` is persisted on BOTH parse paths. `split_round` reads it and only the paste door ever
   wrote it — so queueing alone would have settled the round `PARSE_FAILED` with "This round has no
   text to read. Paste the conditions again.", telling a processor to paste a letter they had just
   uploaded;
3. the round STAYS `PARSING` and emits no `ROUND_PARSED`, because `split_round` reuses `_settle`
   whose compare-and-set is guarded on `PARSING`, and because two parses in one round's history for
   a single read is the defect LP-907 shipped and had to correct.

And the coverage claim below — *the only door with coverage is the only door that works* — repeated
itself one layer down, inside the fix. See finding 5.

The dashboard's "waiting for a reader that will not come" notice was **deleted** rather than
narrowed: the state it rendered is now unreachable, and a screen for an unreachable state looks
maintained while describing a bug that is gone. A split that genuinely fails settles `PARSE_FAILED`
with `ai_unavailable`, which `RoundFailed` already renders.

<details>
<summary>The original diagnosis, as written before the fix</summary>


`needs_ai: true` with `ai_used: false` is the pair LP-904's schema comment calls "waiting for the
split task — a state that has to be findable". On the paste door it is transient. On the upload and
forward doors **nothing will ever run that split**, so it is permanent.

Verified twice, independently. Six sites across four readers set the flag (`champions:285,364`,
`uwm:889`, `generic:43,104`, `paste:97`). `split_condition_round.delay()` is called from exactly one
place — `api/conditions.py:113`, inside `_enqueue_split_or_fail` — and that helper from exactly one
site, `:380`, inside `paste_conditions`. The parse task never branches on `needs_ai` at all.

⚠️ **The only door with coverage is the only door that works.** Every test of the flag is paste-side;
there are ZERO occurrences in `test_condition_upload_endpoint.py`, `test_condition_forward_endpoint.py`
or `test_condition_parse.py`. The reader-level tests pin that readers SET it, never what a door does
with it. So the suite is green and silent about two of three doors.

**Not LP-909's to fix** — recorded against LP-908, whose own section row scopes it to "a `needs_ai`
PASTE". **It is LP-909's to render**: the dashboard treats the pair as abandoned on those two doors
rather than waiting, or it shows "waiting for AI" forever, which is the stranded-round failure again
in a different costume.

</details>

### ⚠️ A HASH IN THIS TABLE HAD ALREADY STOPPED MEANING WHAT IT SAID

§3's row cited `cead214a` for the reparse route and the tab page. That commit was amended into
`23c3a53c` — and an amended commit stays reachable until gc, so `git show cead214a` returns
**superseded content with no error at all**. A dead hash is safe; one that silently resolves to the
wrong thing is the trap. Corrected above. Raised by the review session, which hit it while checking a
hash I had told them was gone.

### Deviations in §4, recorded because §11 checks against the design pack

1. **"Ignore this line" is not offered** on the review screen's unassigned lines, though S1-10 draws
   it. Nothing in the schema can REMEMBER a dismissal — there is no column for it — so the button
   would clear on reload and quietly re-raise the line. The line is already non-blocking: it never
   becomes a condition unless somebody adds it.
2. **Condition-level events are excluded from the round history.** `events_for_round` filters
   `condition_id IS NULL`, because without it a 30-row import renders thirty "A condition was added"
   lines and the README asks for "a SHORT history" — measured at NINE lines on a real
   paste → import → paste → import flow, six of them detail-less `CONDITION_SEEN_AGAIN`. **The cost:**
   a condition typed by hand into an existing imported round writes only `CONDITION_CREATED`, so it
   leaves no round-level trace and does not appear in that round's history.
3. **The S1-09 History section omits `failure_kind`**, so a failed round reads "Could not be read"
   without saying why. The kind is a closed vocabulary and could be projected; it was left out rather
   than add a field for one sentence, and that is a deferral rather than a decision.

### ⚠️ `ix_condition_events_condition_occurred` IS STILL UNJUSTIFIED

`9ac9ee85` gave `ix_condition_events_round_occurred (round_id, occurred_at)` its first reader — LP-904
declared it with the comment "the shape the round-details sheet reads (S1-09)" and nothing had ever
read it, so it paid a write on every event insert for a query that did not exist.

Its sibling `(condition_id, occurred_at)` orders one CONDITION's history. Nothing in this codebase
asks that question, and the events route does **not** cover it. Either something reads a single
condition's events in time order, or it should go in a follow-up migration. Open, and named in the
route docstring and in `events_for_round` so it is not read as settled.

### Review findings against §3

1. ~~**`round-reading.tsx:70` bypasses `resolveStatus`**~~ — **fixed in `78b9d412`.** It now calls
   `resolveStatus(CONDITION_ROUND_STATUS, round.status)`, and a repo-wide search for
   `CONDITION_ROUND_STATUS[` in production source returns nothing. Verified against the tree rather
   than against this list.
   Compile-time safe, runtime-fragile: a backend one deploy ahead sends a sixth status, the index
   returns `undefined`, and `StatusToken:92` evaluates `GLYPH[meta.tone]` — a TypeError that crashes
   the card instead of degrading to an honest unknown label. Counted before calling it a deviation:
   22 call sites use the helper; three index directly in production source (`dashboard/page.tsx:97`,
   `file-context-rail.tsx:421`, this one), and only this one is new. The enum-mirror guard makes
   compile-time drift unlikely, but it guards CI, not a browser tab.
2. ~~**`RoundReading`'s stranded copy promises a route that does not exist**~~ — **resolved in
   `cead214a` by building the route**, which was the honest fix rather than deleting the sentence.
   `POST /api/condition-rounds/{round_id}/reparse` hands the stored PDF back to the reader; the
   dashboard passes `onRetry` to both screens, and the paragraph and the button finally agree.

   ⚠️ The same commit corrected a test that was WORSE than a stale comment. `conditions-screens.test.tsx`'s
   "offers no Try again when re-reading is impossible" rendered an UPLOAD round and justified the
   absent button as "a pasted round has no stored PDF to re-read" — the fixture had no such property,
   so the button was missing only because the test omitted `onRetry`. It asserted the consequence of
   its own setup while reading as a rule about pasted rounds. The sixth instance in this stage of an
   assertion sitting downstream of the property rather than of its negation.
3. **The 20 MB ceiling is restated on the client against one env-overridable setting.** ⚠️ The
   original wording said "four client places" and that count is wrong — checked: the conditions
   ceiling is in ONE place, `conditions-empty.tsx:33` (`MAX_SHEET_BYTES`, used at `:183`). The other
   numbers found by the same search belong to different features with different limits (documents
   50 MB, MISMO 10 MB) and are not copies of this one. The finding itself stands: the comment "Kept
   in sync by the sentence" asserts a mechanism that cannot work, and the defence — a wrong client
   ceiling fails visibly — holds only while the client is LOWER. Lower `condition_sheet_max_bytes`
   and the client accepts an 18 MB file, the processor waits through the upload, and the server
   413s. Fix by having the server state its ceiling, or state the duplication with its configuration
   direction named and no synchronisation claim.
4. ~~**The dashboard must handle the permanent `needs_ai` state**~~ — **resolved by removal in
   `6abafc75`.** The state is unreachable now that every door queues the split, so the screen for it
   was deleted rather than written.
5. **⚠️ THE FIX FOR THE STRANDED ROUND REINTRODUCED A STRANDED ROUND, BY A THIRD ROUTE** — raised by
   the review session against `6abafc75`, fixed in the follow-up commit. `parse_round` called
   `split_condition_round.delay()` **bare**. The row is committed `PARSING` *before* the enqueue —
   correctly, since a worker that picked it up first would find no row — so a broker that is down
   left exactly the permanent-`PARSING` round the commit set out to eliminate.

   **And the coverage shape was the one this ticket had already named.** `OperationalError` appeared
   in exactly ONE test file in the whole backend suite, covering the one enqueue that was already
   guarded. *The only door with coverage was the only door that worked* — now true of the enqueues
   inside the fix for it.

   Fixed by `_queue_split_or_fail` in the task module: catches **kombu's** `OperationalError` (not
   `sqlalchemy.exc`'s — two unrelated classes share the name, and catching the wrong one is a guard
   that never fires while every test stays green) and settles `PARSE_FAILED` with
   `failure_kind: "enqueue_failed"` through `_settle`, so the compare-and-set still protects a round
   discarded in between. `ENQUEUE_FAILED_DETAIL` moved to `app/services/condition_rounds.py` because
   `tasks → services` is the one direction imports run here and two copies of one sentence drift.

   **It forgoes Celery's retry deliberately.** Letting the error escape reaches `retry_or_terminal`,
   which retries — and a retry would genuinely re-parse and re-enqueue. But `on_exhausted` only
   *logs*, so once retries run out the round sits `PARSING` forever with no reason recorded. A typed
   failure a processor can act on beats a spinner that ends in silence six minutes later.
