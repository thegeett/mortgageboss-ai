# LP-836 — One inbox domain per environment

- **Ticket:** LP-836 · Phase 4 follow-up, from processor feedback
- **Status:** Built — awaiting review
- **Date:** 2026-09-07
- **Scope:** `core/config.py`, `infra/envs/staging/main.tf`, `.env.example`, `docker-compose`
- **Migration:** none — but see "What this does to existing files"

---

## What was asked, and confirmed

| environment | domain |
|---|---|
| production | `imbox.mortgageboss.ai` |
| staging | `imboxstaging.mortgageboss.ai` |
| local | *this ticket's open question* |

Confirmed directly, with the reason: *"i dont want same name for both environment"*.

**Recorded once, plainly, because it is a domain and not a variable name:** the spelling is `imbox`,
with an **m**, in both values. It reads like `inbox` and is not. Both values were given deliberately
and the distinction asked for is between the environments, which the `imbox` / `imboxstaging` split
provides. It is one line of config in each environment and is reversible in a minute — but a wrong
mail domain is not something a test can catch, so it is written here rather than assumed.

## What is there today

| | value | set where |
|---|---|---|
| default | `inbox.mortgageboss.ai` | `core/config.py:278` — **production's domain, fails OPEN** |
| staging | `inbox.staging.mortgageboss.ai` | `infra/envs/staging/main.tf:472` |
| dev template | `inbox.dev.mortgageboss.ai` | `infra/envs/dev/main.tf` — never applied |

The default being production's domain is deliberate and documented: an environment that forgets to
set `INBOX_DOMAIN` advertises production's address, so the wiring in each environment is what keeps a
staging file from taking real borrower mail. **That reasoning survives this change**, and the default
moves with production's value.

## Localhost, which was asked about and has no answer today

There is no local value at all — a developer's app advertises **production's** inbox domain on every
file, because the default is production's and nothing overrides it locally. That is harmless while
nothing receives mail, and it is exactly the shape of the `UPLOAD_LINK_BASE_URL` defect: a plausible
value that is wrong and that nothing complains about.

Three options, and the third is what I would do:

1. **`imbox.localhost`** — obviously local, resolves nowhere, cannot be confused with a real domain.
2. **`imboxlocal.mortgageboss.ai`** — consistent with the others, but it is a real domain that could
   be made to resolve, which is the property we do not want locally.
3. **Refuse to start without it outside a test run**, the same treatment LP-827 gave the upload base
   URL. Then no environment can silently inherit another's mail domain, and the choice of local value
   stops mattering because it must be stated.

## What this does to existing files

`LoanFile.get_inbox_address()` composes the address from the **current** setting, so it is not stored
per file. Changing the domain therefore changes the address of **every existing file at once** —
including files whose address has already been given to a borrower in a sent email.

On staging that is nine files and no live mail, so it costs nothing. **It is stated because on
production it would not be**, and this ticket is where somebody should decide whether a file's inbox
address ought to be a stored value rather than a computed one before there is real mail to lose.

## Done when

- Production's default and staging's task definition carry the confirmed values.
- A local value exists and is not production's, by whichever of the three routes is chosen.
- `docs/` and `.env.example` say the same thing as the code. A grep for the old domain returns
  nothing but history.
- The DNS these names need does not exist yet, and neither does the SES receipt rule. **This ticket
  changes what the app advertises, not what receives it** — the address it produces will still reach
  nothing until the infrastructure is applied. That is a different ticket, on hold, and this one must
  not read as having fixed it.

## For the reviewer

The one-line check: grep the whole repository for `inbox.` and confirm every hit is either history, a
comment about history, or a test that pins the old value on purpose. A domain changed in the config
and left in a seed file, a fixture or a doc is the way half of an environment ends up on each.


---

## Built, 2026-09-07

### The values

| | |
|---|---|
| production (the default) | `imbox.mortgageboss.ai`, as `PRODUCTION_INBOX_DOMAIN` |
| staging | `imboxstaging.mortgageboss.ai`, via a new `inbox_domain` variable |
| dev template | `imboxdev.mortgageboss.ai` |
| local | `imbox.localhost` in `.env.example` |
| the test suite | `imbox.example.test`, autouse |

`.localhost` and `.test` are both reserved (RFC 6761) and resolve nowhere, which is the property
wanted in the two places an address must never be mistaken for a domain somebody could register.

### The guard, and where it is NOT

The default is still production's, so an environment that forgets `INBOX_DOMAIN` still starts. What
changed is that it can no longer *hand out an address*: `get_inbox_address` raises when the domain is
production's and `ENVIRONMENT` is not `production`.

**Not a `model_validator`, and that is LP-827's lesson rather than a preference.** A `Settings`
validator fires in every process — `alembic/env.py` imports the singleton at module scope, and
`scripts/deploy` runs migrations on the new image under the *currently deployed* task definition. A
boot-time guard would kill the migration step of the deploy that supplies the value.

### The suite was the first environment to fail it

164 tests failed on the first run, because the suite is an environment that never set
`INBOX_DOMAIN`. **Fixed by configuring it, not by exempting tests** — a test-shaped hole would put
the one place the guard is exercised outside the one place it applies. Autouse, same argument as
`_pin_ai_provider`: the suite must not inherit whatever the ambient `.env` says.

### THE CONSEQUENCE THIS TICKET DID NOT ANTICIPATE

The old name was `inbox.${var.domain_name}` — `inbox.staging.mortgageboss.ai` — which sits **inside**
the `staging.mortgageboss.ai` hosted zone Terraform already owns. `main.tf` said so in as many words:
*"STAGING NEEDS NO REGISTRAR STEP."*

`imboxstaging.mortgageboss.ai` is a **sibling label directly under the apex** `mortgageboss.ai`,
which is not in Route 53 — `phase4.md` §3: the apex stays with the existing registrar and is never
delegated. So staging now needs its own hosted zone and NS records entered by a person at the
registrar, **exactly as production does.**

That is a change to what the inbound infrastructure costs — a human and a propagation wait, on an
environment that previously needed neither. It is recorded in `main.tf` where the old claim was, and
here, rather than discovered at apply time by whoever is on hold for that work.

### One variable, because two would drift

`INBOX_DOMAIN` (what the app advertises) and `mail_domain` (what SES receives on) were separate
strings composed differently. They are now one `inbox_domain` variable feeding both. The app
advertising one name while the MX answers another is a borrower's documents going nowhere with
nothing to notice it — and the two expressions had already diverged the moment this value changed.

### Mutations

| mutation | result |
|---|---|
| the guard can never fire | 1 failed — the refusal case |
| the guard refuses every domain outside production | 2 failed — both positive controls |

The second is what earns the controls their place: a guard that refused everything would satisfy the
refusal test and break staging entirely.

### What this does NOT do

**The address still reaches nothing.** No DNS record exists for either name, `inbound_mail_enabled`
is false, and the SES receipt rule is not created. This ticket changes what the app advertises. What
receives it is a different ticket, on hold.

### The three standing checks

**Tenancy:** unchanged — the address is composed from the file's own token. **Logging:** the guard's
message names the domain and the environment, never a token or an address. **AI:** none.
