# LP-412 — Activate IN-6 and PC-7 (Priya signed off both bars)

## What this ticket is

Priya signed off the last two Bucket 2 bars, so flipping `validated: true` on each clears its gate and
activates it: **`ACTIVE_RULE_IDS` 25 → 27**. IN-6 and PC-7 **only** — OC-1 stays held on calibration, and no
other blocked rule is touched. The activation goes **through the gate** (a `_LP412_ACTIVATED` tuple), not a
hand-edit of the active set.

## Priya's rulings (recorded, not just applied)

| rule | her decision | encoded as |
|------|--------------|-----------|
| **IN-6** | **"Approve at 0.95 — same as IN-5"** (same tag `income.employer_normalized`, same 100% measurement she already approved for IN-5) | IN-6 bar `validated: true` (threshold 0.95, measured 1.0) |
| **PC-7 Q1 (past closing date)** | **"Flag any past date"** | `min_days_until_closing: 0` (fire on any strictly-past closing) |
| **PC-7 Q2 (far future)** | **"90 days"** | `max_days_until_closing: 90` |

Both PC-7 answers match the proposed defaults — now **confirmed** by her, recorded verbatim in the bar rationale
so a future reader knows they are her rulings, not unreviewed placeholders (the AS-5 lesson).

## Acceptance criteria (status)

| criterion | status |
|-----------|--------|
| Both bars verified clean BEFORE the flip (the AS-5 discipline) | ✅ Phase 0 below |
| IN-6 + PC-7 activate; no other rule newly-activates | ✅ eligible 14 → 16 (exactly these two) |
| Her rulings recorded in the rationales | ✅ IN-6 "same as IN-5"; PC-7 both answers |
| Real run reports actual verdicts | ✅ PC-7 satisfied; IN-6 needs_review per borrower |
| 25 → 27; the LP-389 invariant holds; 25 prior rules identical | ✅ |
| Tests + full suite + ruff + mypy green | ✅ |

## Phase 0 — the eligibility check (proof only two activate)

Both bars are internally consistent before the flip: **IN-6** `calibratable-now` + threshold 0.95 + measured 1.0
+ `load_bearing_ai_tags: [income.employer_normalized]`; **PC-7** `no-ai-threshold-pending` + `input_resolves:
true` (LP-411 corrected the earlier false `false`). PC-7's window values match her answers exactly (min 0 / max
90). The **LP-411 loader guard** (a validated `no-ai-threshold-pending` bar must have `input_resolves: true`)
passes — PC-7 has `input_resolves: true`.

**Eligibility for every bar-entry rule, before → after the two flips** (verified in code):

| result | rules |
|--------|-------|
| eligible before AND after (14) | AS-2, AS-8, AS-9, AS-10, AS-11, AS-12, ID-5, IN-1, IN-3, IN-4, IN-5, IN-7, IN-10, IN-11 |
| **newly eligible (the flip)** | **IN-6** (`validated ∧ 1.0 ≥ 0.95`), **PC-7** (`input_resolves ∧ validated`) |
| held before AND after (11) | OC-1 (not-calibratable-yet — its AI tag unscored), AS-3/4/5/6/7, IN-8/9/12/13/14 |

So flipping the two `validated` flags yields **exactly** `{IN-6, PC-7}` newly eligible — no other rule moves.

**IN-6's AI fold-in confirmed:** `income_employer` is in `required_ai_groups()` **via live IN-5**, so when IN-6
runs `income.employer_normalized` is produced and `income.employer_coverage` materializes — IN-6 reaches a real
verdict, not a missing-tag couldnt_check (not the LP-384 trap).

## Phase 1 — the flips

- **IN-6 bar:** `validated: false → true`; rationale records "PRIYA SIGNED OFF at 0.95 — same tag/evidence as
  IN-5, NOT an unreviewed default."
- **PC-7 bar:** `validated: false → true`; rationale records her two answers verbatim ("FLAG ANY PAST DATE" →
  min 0; "90 DAYS" → max 90).
- **`registry.py`:** `_LP412_ACTIVATED = ("IN-6", "PC-7")`, appended to `ACTIVE_RULE_IDS` (the gate mechanism,
  the LP-393-6 pattern). No other bar value touched.

## Phase 2 — THE REAL RUN (reported, not predicted)

Materialized LF-6T3N with `income.employer_normalized` produced per income doc (as live IN-5's `income_employer`
group produces it), then ran IN-6 + PC-7 through the evaluator:

- **PC-7 → `satisfied`** — "the loan's closing date is within a realistic window of the file date (not passed,
  not implausibly far ahead)." `contract.days_until_closing == "1"` (closes in 1 day). It ships **auto**, so
  this verdict goes out with no human check — and it correctly does **not** falsely fire on a near-term closing.
- **IN-6 → `needs_review`** for **both** borrowers. LF-6T3N's borrowers each have a **prior-year W-2 employer
  with no current pay stub** (Jordan: pay stubs Acme; W-2s Acme + Northgate → Northgate uncovered. Taylor: pay
  stubs Sterling; W-2s Sterling + Cafe Bluebird → Cafe Bluebird uncovered). `uncovered → needs_review` (surfaced
  for a human to confirm — ADR-325). **This is a REAL verdict** (the AI tag folded in; not couldnt_check, not
  the LP-384 trap) and a **legitimate observation** (a last-year employer with no current pay stub is worth a
  processor glance). *(Note: this is a genuine coverage gap — the employer names are distinct — not the
  fuzzy-residue false-positive case, which does not arise here.)*

**Activation:** `is_eligible(IN-6) == is_eligible(PC-7) == True`; both in `ACTIVE_RULE_IDS`; `len == 27`; the
LP-389 invariant `ACTIVE − BASE == eligible_rule_ids()` holds; the 25 previously-live rules are unaffected
(IN-6/PC-7 read their own tags; they change no other rule's inputs).

## PC-7 is the first rule live via `no-ai-threshold-pending`

PC-7 is the **first** rule to activate through LP-411's new eligibility status. The path worked end-to-end
(`input_resolves ∧ validated`), and the loader guard held — **no gap revealed.** So no ADR (LP-411's ADR-327
already recorded the status; this ticket is two activations through the established gate).

## The caveats each ships live with

- **IN-6** — its `uncovered` branch routes to `needs_review` (ADR-325), so short-form employer names ("Acme" vs
  "Acme Freight Co") will surface a `needs_review` a human clears; a fuzzy-coverage refinement would reduce
  those (a separate ticket). On LF-6T3N the needs_review is a *real* gap (distinct employers), not that residue.
- **PC-7** — LF-6T3N closes in 1 day (satisfied), so both fired paths (past + far-future) ship **unexercised on
  real data** (the AS-2/AS-8 one-sided-trigger shape); a past-date and a far-out-date fixture would exercise
  them (a fixture ticket). PC-7 must not falsely fire on a near-term closing — verified (satisfied, not fired).

## Every assumption / decision

- **Two rules only, through the gate** — `_LP412_ACTIVATED`; the eligible set proves exactly `{IN-6, PC-7}` flip.
- **Her rulings recorded, not just the numbers** — both rationales carry her decisions verbatim.
- **PC-7 ships auto** — its satisfied verdict goes out unchecked; confirmed no false fire on the 1-day closing.
- **IN-6's needs_review is correct**, not a bug — a real coverage gap surfaced for a human.
- No spec/tag/producer/prompt changed; the 25 previously-live rules are byte-identical.

## What remains

- **OC-1** — the one Bucket 2 rule still **held**, on **calibration** (its AI tag
  `occupancy.consistent_with_signals` is unscored — `not-calibratable-yet`). It needs a scoring pass + Priya's
  bar, not a flip.
- Optional follow-ups (unchanged, out of scope here): the IN-6 fuzzy-residue refinement; fixtures to exercise
  PC-7's fired paths; a `loan.purpose` tag (PC-7 refi scoping); the `rule_tags.csv` regen (AS-8/IN-6/PC-7 point
  at their derived tags).

## The Bucket 2 arc, complete

Census (LP-405) → four Phase-0 stops/OC-1 (LP-406-1/2/3/4) → the producer wave (LP-410) → the rules
(LP-406-1b/2b/3b + OC-1) → the honest-hold fix (LP-411) → **activation on Priya's sign-off (LP-412)**. **Three
of the four rules are now live (AS-8, IN-6, PC-7); OC-1 remains held on calibration.**
