# LP-125R — AS-1 large-deposit sourcing (build-to-spec, per-account, threshold-confirmed)

**Type:** Story · **Epic:** Phase 3.5 / Epic B · **Depends:** LP-122R (pattern), LP-123R/round-5 (grouping
token, shared normalizer, typed facts, Verdict enum, confidence_mode-from-evaluator) · **Status:** complete
· **Date:** 2026-07-08

## What this ticket is

AS-1 (`xsrc.asset.large_deposit_unsourced`) checks that a LARGE deposit into a bank account is SOURCED —
an unexplained large deposit may be undisclosed borrowed funds or an undocumented gift. Built via the
LP-122R pattern, reusing the AS-8/employer_count primitives.

## STEP 0 findings (verified against the code + the recorded Priya decision — not memory)

- **(a) Live status:** **DORMANT.** `_check_large_deposit_unsourced` reads `facts.unsourced_large_deposits`,
  which is only ever defaulted to `()` (`facts.py:86`) — never populated. No live-firing behaviour →
  **build-to-spec**, no parity anchor, no double-firing risk (P8).
- **(b) Threshold:** a single deposit exceeding **50%** of monthly income. **Priya-confirmed** —
  `decisions.md:7697` ("only Priya-confirmed thresholds, e.g. large-deposit >50%… seed true") + seed
  `_VALIDATED_PARAMS` (`large_deposit_pct: 50`). Read from `params["large_deposit_pct"]` (tunable). → P10.
- **(c) Qualifying-income basis:** there is **no computed qualifying-income fact** in the snapshot. The
  income basis is summed STATED `borrowers[].income_items[].monthly_amount`. Absent/zero → couldn't-check.
- **(d) "Sourced":** the only sourcing signal in the data is **file-level** documents (gift letter, gift
  funds, donor statement, sale-of-asset proof, VOD, EMD receipt). **A specific deposit cannot be matched to
  a specific document** (no per-deposit linkage). See the divergence note below.
- **(e) "Deposit":** a transaction whose `transaction_type` normalizes to `"deposit"`; payroll/direct
  deposits are EXCLUDED (already income) via description signals.
- **(f) Scoping:** deposits per-account (reused AS-8 grouping token); income summed file-level (stated).

## The applicability (validates under `extra="forbid"`)

```json
{
  "scope": {},
  "triggers": { "all": [ { "kind": "entity_exists", "collection": "documents", "field": "document_type", "op": "eq", "value": "bank_statement" } ] },
  "required_inputs": [
    { "kind": "data_field", "path": "transactions[].amount" },
    { "kind": "data_field", "path": "borrowers[].income_items[].monthly_amount" }
  ]
}
```

Triggers on a bank statement existing (empty `documents` → doesn't-apply, inherits AS-8 FIX 5 — P3);
requires BOTH the deposits and the income basis (either absent → couldn't-check — P1/P2). Threshold +
sourcing are evaluator logic. Saved via reversible migration `f3d9b6a1c8e2` (validated + confidence_mode
were already correct — only applicability was authored).

## Verdict mapping (per-deposit, rolled up)

| case | verdict |
|---|---|
| large non-payroll deposit, **no** sourcing doc anywhere on file | **FINDING** (determinately unsourced) |
| large non-payroll deposit, sourcing docs **present** on file | **COULDN'T-CHECK** (indeterminate — can't match; verify) |
| large deposit whose description matches payroll / an employer | excluded (already income) |
| large deposit with **no description** (can't tell payroll) | **COULDN'T-CHECK** for that deposit (P2) |
| large deposit whose **account can't be determined** | **COULDN'T-CHECK** for that deposit (P6) |
| deposit ≤ threshold, or no large deposits | **SATISFIED** |
| no stated monthly income (basis absent) | **COULDN'T-CHECK** (rule-level) |

Rollup: any unsourced → FINDING; else any indeterminate → COULDN'T-CHECK; else SATISFIED.

## The "sourced" decision — an intentional departure from a literal AC (P1/P2)

The ticket's ACs assume a large **sourced** deposit → SATISFIED. But the data only reveals whether
sourcing docs exist *somewhere on the file* — a specific deposit **cannot** be matched to a specific doc.
Per **P2** (a REQUIREMENT: indeterminate sourcing → couldn't-check) and **P1** (no false-green), a large
deposit with sourcing docs present is **COULDN'T-CHECK** ("verify"), NOT satisfied — because file-level
presence is not per-deposit proof, and treating it as "sourced" would be a false-green (the doc might
source a different deposit / a different account). SATISFIED means "no large deposits to source."

**PRIYA-FLAGGED:** (1) should file-level sourcing-doc presence be allowed to CLEAR a deposit (satisfied)
rather than require per-deposit verification (couldn't-check)? (2) the payroll exclusion is a heuristic
(description keywords + employer-name match) — confirm the signal. (3) the income basis is *stated* total
monthly income (no qualifying-income fact yet) — a future computed qualifying-income fact would refine it.
The data limitations mean many large deposits land in couldn't-check; that is honest (no false-green, no
over-flag), and useful (each couldn't-check surfaces a specific deposit for review).

## Acceptance criteria — and how each was met

- [x] **STEP 0 done** (live status, threshold + SOURCE, income basis, sourcing/deposit defs, scoping) — above.
- [x] **Applicability validates + saved via reversible migration** (`f3d9b6a1c8e2`).
- [x] **Evaluator:** per-account (reused AS-8 `_grouping_token`, no cross-owner), payroll excluded,
  threshold computed from `params`, sourcing checked; Verdict enum; typed facts; declares `confidence_mode`;
  registered (`LargeDepositEvaluator`).
- [x] **The pinning fixture set passes** (12 tests): unsourced→finding, sourcing-docs→couldn't-check,
  sub-threshold & payroll→not-flagged, missing-income & no-description→couldn't-check, ungroupable-account→
  couldn't-check, empty-docs→doesn't-apply, no-PII-in-outcome, end-to-end.
- [x] **Both-directions guard (P1) + no-false-green (P2) + no cross-owner (P6)** — verified by the fixtures.
- [x] **Primitives REUSED (P4):** `_grouping_token` (AS-8), `normalize_text` (shared), `Verdict`, typed
  `TransactionFacts`/`BankStatementFacts` — no new normalizer/coercer/grouping copies. `confidence_mode`
  from the evaluator (P5).
- [x] **Validated (P10):** `validated=true` — the 50% threshold is Priya-confirmed (STEP 0b). Already true
  in the seed/DB; unchanged.
- [x] **SCOPE HELD** — only AS-1; engine/runner/live path unchanged.

## How each P-guard was satisfied

- **P1 (both directions):** a missing sourcing/income signal is neither auto-fail nor auto-pass — income
  absent → couldn't-check; sourcing indeterminate → couldn't-check; only a determinate no-sourcing → finding.
  `account_type` is a fallback (via the reused AS-8 token), not a hard gate.
- **P2 (no false-green):** enumerated "can't tell" cases (unclassifiable payroll, indeterminate sourcing,
  absent income, ungroupable account) each → couldn't-check, never silent satisfied/finding.
- **P3:** empty `documents` → doesn't-apply (AS-8 FIX 5).
- **P4:** reused primitives; named the imports.
- **P5:** evaluator declares `ConfidenceMode.DETERMINISTIC`; the seed reads that.
- **P6:** deposits grouped by the reused account token; a deposit is never sourced/compared across
  accounts; ungroupable account → couldn't-check.
- **P7:** the full fixture set was written alongside the evaluator (this file).
- **P8:** confirmed dormant → no gate needed.
- **P10:** validated only because the threshold is genuinely Priya-recorded.

## Pattern feedback

- **STEP 0 earned its keep again:** it revealed that the data (freeform `transaction_type`/`description`,
  no per-deposit sourcing linkage, no qualifying-income fact) genuinely under-supports a crisp "sourced"
  judgment — so the honest build leans on couldn't-check, and the ticket's optimistic "sourced→satisfied"
  AC had to be reconciled with the P2 no-false-green guard (documented + Priya-flagged).
- **Reuse worked cleanly** — the AS-8 grouping token + shared normalizer dropped in with no new copies.
  (One smell: `_grouping_token` is imported underscore-private across evaluator modules; if a third
  document-account rule needs it, promote it to a shared `evaluators` helper.)
- **The P-guard preamble was valuable** — pre-empting the recurring finding CLASSES (both-directions,
  no-false-green, reuse, source-of-truth, no-cross-owner, pin-fixtures-up-front) shaped the build so a
  round-6 review shouldn't re-find them.

## Follow-ups

- **Priya review** of the three flags (file-level sourcing → clear?; payroll heuristic; income basis).
- **A computed qualifying-income fact** + **per-deposit sourcing linkage** would let AS-1 move cases out
  of couldn't-check into satisfied/finding — a fact-builder enhancement, not a rule change.
