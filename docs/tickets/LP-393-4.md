# LP-393-4 — Calibrate the income + asset tags against Priya's scenario labels

> **⚠️ SUPERSEDED by LP-393-4a.** On review Priya corrected these results: the `is_declining` "materiality gap"
> was a label slip (now 100%), the `liquidation_terms` finding was INVERTED (the AI under-restricts — now 100%
> after a prompt fix), and `same_line_of_work`'s 38% was a FIXTURE defect (missing `occupation`), not a
> definitional divergence. This doc is the first-pass record; **the valid numbers are in `LP-393-4a.md`.**

## What this ticket is

Priya labeled the LP-393-2 scenario worksheet BLIND (no predictions shown). This scores the real AI against her
45 goldens at the new n (11-13, up from 2) — the first measurement of these tags at a n that can support a bar
— and reports accuracy + every disagreement with the AI's reasoning, **clear-cut and ambiguous scored
SEPARATELY** (they mean different things).

**These are a point-in-time LIVE run** (real model, non-deterministic — LP-390-5's precedent); the verdicts
(which tag passes / needs a fix) are robust to that noise, the exact per-cell numbers are not.

## Acceptance criteria (met)

- Score the 4 tags against her labels by the stable `(tag_id, subject_id)` key; an unjoinable label is
  reported, never guessed (0 unjoinable here — all 45 joined).
- Clear-cut (B3-B8, known expected) and ambiguous (B9-B15 + assets, her label defines) scored separately.
- Priya's `unknown` handled as a valid label, not a skip.
- A per-tag verdict + which rule each unblocks/holds + the synthetic-data caveat for LP-393-5.
- No tag/rule/producer/prompt/snapshot change; `ACTIVE_RULE_IDS` unchanged (20); full suite green.

## Phase 0 — the join report

All **45 goldens joined** by `(tag_id, subject_id)`; **0 unjoinable**, 0 unmatched (every label has a
prediction). Filled n per tag (she labeled every row; `unknown` is her valid "couldn't tell"):

| tag | filled n | her distribution |
|-----|----------|------------------|
| `income.has_2yr_history` | 13 | yes 9, no 3, unknown 1 |
| `income.is_declining` | 13 | no 9, unknown 3, yes 1 |
| `income.same_line_of_work` | 13 | unknown 6, yes 5, no 1, **no/unknown 1** (a hedge) |
| `asset.liquidation_terms` | 6 | fully_liquid 5, vested_usable 1 |

The scoring runs the REAL reasoner (`income_stability` + `asset_facts`), not a stub.

## Phase 1/2 — per-tag accuracy (clear-cut and ambiguous separately) + verdict

### `income.has_2yr_history` — 85% (11/13) → **calibration-passed**
- **Clear-cut:** B5 (`no`) OK, B6 (`yes`) OK — AI = Priya = expected.
- **Ambiguous disagreements (2):** B12 (pay stubs, 2 years) AI=`yes` / Priya=`unknown`; B14 (fixed-term
  contract, 2 W-2s) AI=`yes` / Priya=`no`. Edge cases (pay-stub-only span; a fixed-term contract's two years) —
  notes for the bar, not a systematic gap.
- **Verdict: calibration-passed** → **IN-11 ready for a bar** (LP-393-5, with the synthetic-data caveat).

### `income.is_declining` — 85% (11/13) → **calibration-passed, with a MATERIALITY finding**
- **Clear-cut:** B3 (`yes`) OK, B4 (`no`) OK.
- **The material divergence — B9 (−2%, $70,000 → $68,500):** AI=`yes` ("income decreased $1,500"), **Priya=`no`.**
  The AI calls ANY year-over-year decrease "declining"; the underwriter does not treat an immaterial drop as
  declining. In production the AI's definition over-flags stable income. (B15, rising income, AI=`no` /
  Priya=`unknown` — a benign abstain gap.)
- **Verdict: calibration-passed** → **IN-10 ready for a bar** — but the bar must carry the **materiality
  caveat**, and a prompt-fix (a materiality threshold) is a follow-up (ADR-315).

### `income.same_line_of_work` — 38% (5/13) → **needs-prompt-fix**
- **Clear-cut:** B7 (nurse→nurse, `yes`) OK. **B8 — the three-way:** expected `no`, AI=`no` (matched the
  expectation), **Priya=`yes`** (she considers Warehouse Picker → Office Administrator the same line, OR reads
  the question as income continuity). Her judgment overrides our expectation — so B8 is ALSO a needs-scenario
  finding.
- **The systematic ambiguous divergence:** the AI followed the prompt ("one employer throughout = yes") and
  answered `yes` for EVERY no-change scenario (B3/B4/B6/B9/B10/B12); **Priya answered `unknown`** for those (she
  does not read "no change" as evidence of same line of work). Her labels are hedged (6 `unknown`, one
  `no/unknown`).
- **Verdict: needs-prompt-fix** → **IN-7 STAYS BLOCKED.** The tag's definition diverges from the underwriter's
  and the question may need reframing (ADR-315) before it can be measured.

### `asset.liquidation_terms` — 50% (3/6) → **needs-prompt-fix**
- **The systematic divergence (all 3 disagreements):** a 100%-vested 401(k) and two Roth IRAs — AI=`vested_usable`
  (discounting for "retirement access restrictions / early-withdrawal"), **Priya=`fully_liquid`.** The
  graded-vesting 401(k) and the brokerage accounts agreed. The AI over-discounts FULLY-vested retirement funds;
  the underwriter treats fully-vested retirement + Roth as usable reserves at face. (LP-390-5 saw the same Roth
  disagreement — now confirmed at n.)
- **Verdict: needs-prompt-fix** → **AS-11 STAYS BLOCKED.** Target: reserve `vested_usable` for a PARTIAL vesting
  schedule, not a fully-vested account (ADR-315).

## The synthetic-data caveat (LP-393-5 must carry it)

These numbers validate the AI's REASONING on CLEAN scenario data — NOT robustness to real-document messiness
(OCR noise, odd layouts, missing fields). A bar set on `has_2yr_history` / `is_declining` here must be **paired
with the LF-6T3N real-data result (n=2 — indicative)** as the messiness check, and stated as
scenario-reasoning-validated, not production-validated.

## Phase 3 — tests + equivalence

- The scoring joins by `(tag_id, subject_id)`; an unjoinable label is reported upstream (LP-390-5), never
  dropped; clear-cut and ambiguous are split + scored separately; Priya's `unknown` is a valid cell (both-
  unknown = agreement), not a skip — all keyless (`test_income_scenario_scoring_lp393_4`; the live numbers are
  in this doc, not asserted).
- **EQUIVALENCE:** no tag/rule/producer/prompt/snapshot change; `ACTIVE_RULE_IDS` unchanged (20); the LF-6T3N
  fixtures + worksheets untouched; full suite green (ruff + mypy clean).

## Assumptions, decisions & gaps

- **ADR-315** records the three definitional divergences (is_declining materiality; liquidation_terms
  vested-retirement; same_line_of_work definition) — each becomes the TARGET for its own prompt-fix ticket,
  with Priya's definition as the goal.
- **Two tags pass, two hold:** IN-11 + IN-10 are ready for bars (LP-393-5, with caveats); IN-7 + AS-11 stay
  blocked on a prompt fix, not on n.
- **B8 is a needs-scenario finding too:** the clear-cut expectation (warehouse→office = different line) was
  wrong per the underwriter — either the scenario/expectation or the tag's definition needs revisiting.
- **Never guessed a golden:** every label is Priya's own, taken verbatim; her `unknown` and `no/unknown` are
  reported as she wrote them.
