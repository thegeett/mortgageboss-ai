# LP-393-6 — Re-score has_2yr_history under Priya's ruling; validate + activate the four bars

## What this ticket is

Priya settled all four open items from LP-393-5. Applying them required RE-SCORING `has_2yr_history` (not
hand-editing its number) and then validating + activating IN-7, IN-10, IN-11, AS-11. `ACTIVE_RULE_IDS` 20 → 24.

## Acceptance criteria (met)

- B12 + B14's `has_2yr_history` goldens updated per her ruling, her originals preserved in the worksheet Note.
- `has_2yr_history` RE-SCORED with the real reasoner (not asserted); the new number reported.
- The four bars `validated: true` at her heights; IN-11's `measured_accuracy` set to the RE-SCORED value and
  it clears its own gate.
- IN-7's judgmental-vs-AUTO conflict REPORTED and resolved (ships stays ratify); not silently forced.
- Nothing hand-edited from 0.85 → 1.0; the four activated via the gate (validated + measured ≥ bar).
- ruff + mypy + full suite green (**2609 passed**).

## Priya's rulings (applied exactly)

1. **Bar heights:** IN-7 0.90, IN-10 0.95, IN-11 0.90, AS-11 0.90 — confirmed.
2. **All four ship AUTO** — she knowingly overrode the ratify-only recommendation on a synthetic-only basis
   (recorded in each rationale). *(See the IN-7 exception below.)*
3. **B14 framing SETTLED:** a TERMINATED job's two years DOES count as HISTORY. `has_2yr_history` is about
   history only.
4. **The documentation standard is a SEPARATE check:** the W-2/1099/offer-letter requirement is NOT part of
   `has_2yr_history` (it becomes two new candidate rules).

## Phase 0 — the report

- **The two disagreements were exactly B12 + B14** (LP-393-4b), both the documentation-standard class, not
  reasoning errors. Under rulings 3 + 4 both are out-of-scope for the tag → their goldens become `yes`:
  - **B12** `unknown` → `yes` (pay-stub-only; history exists, the W-2/1099 ask is a separate check).
  - **B14** `no` → `yes` (terminated contract, two W-2s present; history exists, continuation is separate).
  Her original labels + notes are preserved in the worksheet Note as the record of the change.
- **The gate enforces ratify-by-kind for IN-7 — but at EVALUATION time, not in the bars loader.** `judgment.py`
  hard-codes `ratification_pending` for a judgment rule (LP-376-B); `activation_mode` returns `ratify` for
  `ships: ratify`. The `activation_bars` loader does NOT cross-check `ships` against the kind, so it would
  *accept* `ships: auto` — but the runtime would still ratify, making the bar a lie. So IN-7 stays ratify.
- **`is_eligible` requires `measured ≥ threshold`** (confirmed), and the LP-389 invariant ties eligibility to
  the active set — so validating the four activates them (no validate-without-activate). Geet chose validate +
  activate now.

## Phase 1 — the two golden updates + the re-score

- B12 + B14 `has_2yr_history` goldens set to `yes`; originals (`unknown` / `no`) + her notes preserved, with a
  `LP-393-6 RE-LABEL per Priya's ruling…` marker naming the SEPARATE check.
- **RE-SCORE (real reasoner, point-in-time): `has_2yr_history` = 100% (13/13), zero disagreements.** Under the
  ruling the two former misses now agree (goldens `yes`, AI `yes`). The other tags are unchanged this pass
  (is_declining 100%, same_line_of_work 100% from LP-393-4b; liquidation 100% carried). **The number changed by
  RE-SCORING, never by editing 0.85 → 1.0.** Had it not reached ≥ 0.90, the lower number would stand (a finding).

## Phase 2 — the four validated bars

| rule | tag | measured (source) | threshold | ships | validated |
|------|-----|-------------------|-----------|-------|-----------|
| **IN-7** | same_line_of_work | 1.0 (LP-393-4b) | 0.90 | **ratify** ⚠️ | true |
| **IN-10** | is_declining | 1.0 (LP-393-4b) | 0.95 | auto | true |
| **IN-11** | has_2yr_history | **1.0 (RE-SCORED, LP-393-6)** | 0.90 | auto | true |
| **AS-11** | liquidation_terms | 1.0 (LP-393-4b) | 0.90 | auto | true |

Activated via `registry._LP393_ACTIVATED = ("IN-7","IN-10","IN-11","AS-11")` → `ACTIVE_RULE_IDS` 20 → 24. The
gate invariant holds: `eligible_rule_ids()` = 13 = `ACTIVE − BASE`. Their AI groups (`income_stability`,
`asset_facts`) fold into `_required_ai_groups` automatically (it derives from `ACTIVE_RULE_IDS`). Every
rationale keeps the synthetic-data caveat and records her sign-off + AUTO override.

## THE IN-7 CONFLICT (judgmental vs her AUTO instruction — how resolved)

Priya asked for AUTO, but **IN-7 is JUDGMENTAL** (`rule_kinds.csv: ai_judgment`) and LP-376-B forbids a judgment
rule from auto-shipping (enforced in `judgment.py`). **IN-7 stays `ships: ratify`**: active, but surfaces every
verdict to needs_review for human ratification — her AUTO is NOT achievable through the bar. Making IN-7 truly
auto would require RECLASSIFYING its kind (a separate ticket). **Reported, not silently forced** (ADR-316). The
three calculative rules ship auto, matching both their kind and her call.

## The two new candidate rules her ruling spun off (NOT built here — their own tickets)

1. **pay-stub-only → require a W-2/1099** before using the income (the B12 standard).
2. **lapsed / terminated employment → require an offer letter + a pay stub** (the B14 continuation check).

These are the "separate check" ruling 4 carved out of `has_2yr_history`.

## The synthetic-data caveat (still riding every bar)

All four were measured on CLEAN scenario data (LP-393-1) — this validates REASONING, not robustness to
real-document messiness. The only real-data check is LF-6T3N (n=2, indicative). Priya accepted this basis for
her AUTO call; it stays recorded in each rationale so the trust decision is never silently upgraded.

## Phase 3 — tests + equivalence

- `test_activation_bars_lp393_6.py` (keyless): the two goldens became `yes` with originals preserved; the four
  are validated at her heights; IN-11 clears its gate (measured ≥ threshold) — a re-score that failed would fail
  here; validating activated the four (ACTIVE 24, invariant intact); IN-7 ships ratify + `activation_mode`
  routes it to ratify despite the AUTO sign-off, while the three calculative rules route to auto.
- Updated for the activation: `test_activation_gate_lp389` (eligible 9→13, held 14→10, ACTIVE 20→24),
  `test_activation_bars_lp380` (all 9 calibratable validated; the active tuple), `test_activation_bars_lp393_5`
  (the proposal now validated), `test_income_scenario_relabel_scored_lp393_4b` (B12/B14 now `yes`),
  `test_generic_evaluators` / `test_dormant_probe` (income_stability + asset_facts now live) /
  `test_assets_family_eval` / `test_income_family_eval` / `test_lp384_stuck_rules` / four
  `test_no_rule_activation_changed` guards / `test_pending_checks_lp391` (IN-12 / IN-13 now stand in for the
  now-live IN-11 / IN-7 as the blocked examples). Added curated reason labels for the four newly-live tags
  (`reasons.py`).
- **EQUIVALENCE:** the only rule/tag change is the intended activation (registry) + the two goldens; no
  producer/prompt/fixture change; the 20 previously-live rules are all still live (now 24). Full suite green
  (**2609 passed**, ruff + mypy clean; yaml loads; CSV LF).

## Assumptions, decisions & gaps

- **ADR-316** records: Priya's B14 ruling (history vs continuation), the documentation standard as a separate
  check (two new candidate rules), her four heights + AUTO override on a synthetic-only basis, the
  validate==activate coupling (no decouple; Geet chose validate+activate now), and the IN-7 judgmental/auto
  conflict (stays ratify, reclassification is a separate ticket).
- **RE-SCORED, never hand-edited** — the discipline the whole system rests on.
- **Nothing beyond the four was activated**; IN-7 ships ratify pending a kind reclassification.
- **Open / deferred:** the two new candidate rules; IN-7's kind reclassification; IN-11's spec over-fire
  limitation (fires on any income lacking history, not just variable) — all their own tickets.
