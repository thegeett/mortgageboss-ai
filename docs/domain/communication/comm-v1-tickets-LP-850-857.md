# Communication v1 — draft only · LP-850 … LP-857

All eight tickets plus the epic index, in one file, for handing to Claude Code.

**Read the epic index first** — it carries the dependency order and the one coupling that will
break the feature if the tickets are split across sprints (LP-851 + LP-853).

**Appendix A at the end is the authoritative UI description** — every screen, every string, every
button order. The tickets reference it by screen number. Build the UI from it.

Companion spec: `docs/domain/communication/comm-v1-draft-only-spec.md`.
When splitting this file back out: the epic index goes to
`docs/domain/communication/v1-epic.md`, each ticket to `docs/tickets/LP-8xx.md`, and Appendix A to
`docs/domain/communication/v1-screens.md`.

---

## This file has been split

It was the hand-off bundle. The pieces now live where the repo keeps them, and **those copies are
the ones to edit**:

| Piece | Where it lives now |
|---|---|
| Epic index | [`v1-epic.md`](v1-epic.md) |
| LP-850 … LP-857 | [`docs/tickets/LP-850.md`](../../tickets/LP-850.md) … [`LP-857.md`](../../tickets/LP-857.md) |
| Appendix A — the screens | [`v1-screens.md`](v1-screens.md) |

Kept as the original bundle for provenance; nothing reads it.
